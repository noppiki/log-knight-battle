# Log Knight Battle — Improvement Log

Running log of quality improvements. The hourly quality loop appends one line per run.
Read this before each run to avoid repeating work; pick the highest-impact thing NOT yet done.

- 2026-06-25 — Shipped 3D FP ROUND 68: SWING FOOTSTEP DUST + AZURE STAGGER REACTION BOOST. (A) FOOTSTEP DUST ON SWING: `GroundDustSystem.spawn(worldPos)` extended to `spawn(worldPos, count=30, velScale=1.0)` — count controls particle count, velScale multiplies radial velocity (1.0→~0.4m spread, 1.5→~0.6m, 2.5→~1.0m); default args keep the existing no-arg tier2 impact dust call at line 9066 working without change. `window.spawnGroundDust` wrapper similarly extended to forward count/velScale. At swing start in `playerAttack()` (synchronous block after `attacker.state='attack'`), a new `try` block fires dust at the player's foot position (`combatants.crimson.root.position` clone, y=0): tier0=8 particles velScale 1.0, tier1=15 particles velScale 1.5, tier2=25 particles velScale 2.5. Fires even on whiff so the charge weight reads regardless of hit. (B) AZURE STAGGER REACTION TIER-2 BOOST: in `playerAttack()`'s impact callback (the hit-reaction branch at ~70% of swing), `playClip` return value is now captured as `hitAction`; when `myChargeTier >= 2 && hitAction`, `hitAction.timeScale = 0.6` is set immediately so azure's hit anim plays at 60% speed. 500ms later a `setTimeout` restores `hitAction.timeScale = 1.0`. Action-level timeScale only — `mixer.timeScale` is untouched (KO-replay and hitLag systems own that axis and multiply independently). Handle+action ref stored as `window._staggerBoostHandle` / `window._staggerBoostAction`; `resetMatch()` clears the handle and force-restores timeScale to 1.0 via the stored ref, preventing cross-round leaks. /3d/index.html grew 10549→10598 lines (+49). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 67: AMBIENT DUST GUSTS (PERIODIC HORIZONTAL WIND). Grep confirmed AmbientGustSystem was not previously implemented. Added `class AmbientGustSystem` (~100 lines) between GroundDustSystem instantiation and `spawnGroundDust` export: constructor takes `(scene, camera)` and arms `nextGustMs = now + 8000` so the first gust fires 8s after construction (past round-banner timing). `spawnGust()` picks east/west origin (±7m on X), targets ±9m, clusters 6–12 `THREE.PlaneGeometry` planes (size 0.25–0.5m, colour `0xb8987a`, opacity 0.35–0.47, DoubleSide NormalBlending, depthWrite:false); each mesh snapshots `lookAt(camera.position)` at spawn for cheap billboard (no per-frame rotation cost); userData carries `{ speedX, lifeMs, startMs, baseOpacity }`. `update(delta)` gates gust spawning behind `!gs.crimson.dead && !gs.azure.dead && !_gamePaused` so gusts never fire on title/SELECT or during KO freeze; slides each particle on X only (no gravity); applies bell-curve fade `1 - (2t - 0.6)^2` so envelope peaks at t≈0.3 and fades to zero at t=1; disposes geometry+material on expiry. `clearAll()` used in `resetMatch()` — resets `nextGustMs` to `now+8000` so each round's first gust never fires immediately. Integration: one line `try { ambientGustSystem.update(delta); }` added after `groundDustSystem.update(delta)` in animate(); one `ambientGustSystem.clearAll()` added after `groundDustSystem.clearAll()` in resetMatch(); interval 15–25s. Total: +113 lines. /3d/index.html grew 10436→10549 lines. 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 65: MATCH-START CINEMATIC INTRO + READY/FIGHT COUNTDOWN. Implementation: (1) CSS `#match-intro-text` fixed overlay (z-index 30, opacity transition 0.25s); `.mit-word` inherits the engraved-metal 5-layer text-shadow stack at 96px (READY) and 144px (FIGHT!); `.mit-word.fight` adds `@keyframes fightFlash` — scale 0.8→1.25→0.95→1.0 in 0.7s cubic-bezier for "FIGHT!" slam-in animation; red #ff6060 with amber glow. (2) DOM: `<div id="match-intro-text"><span id="mit-word">` inserted after `#round-banner`. (3) JS state machine: `window._matchIntroActive` / `_matchIntroStartMs` / `_matchIntroTimers`; `startMatchIntro()` sets the flag, schedules 4 timeouts (READY show@1.0s, READY hide@1.8s, FIGHT! show@2.5s, clear+end@3.2s), plays `sfxCount()` at READY and `sfxFight()` at FIGHT!. (4) Player input block: `fpInputActive()` returns false while `_matchIntroActive` — all Space/E/Shift actions suppressed for the full 3.2s. (5) Camera override: in animate() loop, when `_matchIntroActive`, hijack camera to CLOSE_AZURE coords (3.5, 0.9, -3.5 → lookAt 2.0, 1.2, 0) for 0-2s, then cubic-ease-in-out lerp to home (SIDE_WIDE or FP_SHOT based on current viewMode) for 2-3.2s — no pop on exit in either 3rd-person or FP mode. Director's `update(delta)` still advances cycle timers. (6) Grace period extended: `maybeStartCombatLoop()` grace 2000→3000ms to cover full intro. (7) `onAllLoaded()` ROUND 1 banner delayed 650→3500ms to avoid stacking with READY... text. (8) `resetFullMatch()` calls `startMatchIntro()` + re-arms 3s grace so rematch also gets the cinematic open. Hooks: `maybeStartCombatLoop()` for first boot, `resetFullMatch()` for rematch. Per-round `resetMatch()` unchanged — no intro replay between rounds. All r46–r64 constraints preserved. /3d/index.html grew 10161→10319 lines (+158). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 64: CONTROL HINTS HUD OVERLAY. Confirmed absent via grep (no controlHints/fp-hints/onboarding hits). Problem: first-time players enter FP mode with no knowledge of Space/E/Shift/Mouse keybinds — 17 rounds of polish deliver zero value if players can't discover the controls. Fix: four dismissible hints displayed bottom-right in FP mode only, each fading out when the player first performs that action. (1) CSS `#fp-control-hints` — `position:fixed; bottom:204px; right:24px` (clears azure FP player-card at bottom:32px + ~160px card height); `display:flex; flex-direction:column; align-items:flex-end; gap:5px`; `body:not(.fp-mode) #fp-control-hints { display:none }` for 3rd-person/manual silence; `z-index:25`; per-hint `transition:opacity 0.6s ease`; `.hint.done { opacity:0 }`; `kbd` styling (1px rgba border, rgba bg, amber-beige text). (2) DOM: `<div id="fp-control-hints">` with four `<div class="hint" data-hint="attack/guard/dodge/aim">` children, inserted before `#fp-lock-hint`. (3) JS: `window.markHintDone(key)` — queries `.hint[data-hint="key"]`, adds `.done`, sets `display:none` after 700ms (transition completes). Aim hint uses `window._r64AimAccum` accumulator reset each round — fires `markHintDone('aim')` when cumulative `|movementX|+|movementY|*FP_MOUSE_SENS >= 0.1 rad` within the existing `mousemove` handler (no second listener). Hook sites: `playerAttack` top (before MP guest return), `playerGuardEnter` top (before MP guest return), `triggerRetreatDodge` after `fpInputActive()` check. resetMatch cleanup: `querySelectorAll('.hint').forEach(el => el.classList.remove('done'); el.style.display='')` + `_r64AimAccum=0` so every new round starts with fresh hints. All r46–r63 preserved. /3d/index.html grew 10073→10161 lines (+88). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 63: RETREAT DODGE (SHIFT KEY) WITH I-FRAME. Confirmed absent via grep (no dodge/retreat/backstep/iframe hits specific to player input; existing `dodge` refs are Azure AI-only). Shift key was unused in FP mode. Architecture decision: camera position is a fixed anchor `FP_SHOT.getPosition() = (-1.8, 1.85, 0)` overwritten each frame by `CameraDirector.update()`, so `crimsonKnight.root.position` tween (original plan) would be overridden immediately. Solution: additive camera-position offset driven by `FpPolish.dodge` elapsed state, applied in `animate()` AFTER `cameraDirector.update(delta)` sets the base position. (1) CSS `#fp-dodge-flash` — cyan edge-only radial vignette (`transparent 40%`, `rgba(40,160,190,0.50) 100%`), 120ms CSS transition, z-index 27, distinct from parry flash (full-screen) and damage vignette (red). (2) `FpPolish.dodge { active, elapsed, duration:0.3s, peakOffset:0.8m }` state added to FpPolish object; `dodgeFlashEl` resolved post-DOM. (3) `STM_DODGE_COST = 8` constant near other STM constants. (4) `applyDamage()` i-frame gate at top (after dead check): `if (defenderId==='crimson' && now < _dodgeIFrameUntil) return` — complete no-op covering all damage paths. (5) `triggerRetreatDodge()`: gates on `fpInputActive()`, stamina >= 8, cooldown 0.8s; deducts stamina; arms 100ms i-frame (`_dodgeIFrameUntil`) + 0.8s cooldown (`_dodgeCooldownUntil`); arms `FpPolish.dodge.active=true,elapsed=0`; −2° FOV for 80ms (separate from fovPunch); cyan edge flash 120ms. (6) `updateDodge(delta)`: advances elapsed, returns `−sin(π*t)*peakOffset` (world −X offset, smooth out-and-back lunge, self-zeroing at t=1). Applied in animate(): `if (_dodgeOffset !== 0) camera.position.x += _dodgeOffset`. (7) Keydown: `ShiftLeft/ShiftRight` with `e.preventDefault()`, `e.repeat` guard, calls `triggerRetreatDodge()`. (8) `resetMatch()` cleanup: `dodge.active=false, dodge.elapsed=0, _dodgeCooldownUntil=0, _dodgeIFrameUntil=0`, dodge flash CSS cleared. (9) QA bridge: `window.triggerRetreatDodge = triggerRetreatDodge`. All r46-r62 preserved. /3d/index.html grew ~9939→10020 lines (+~81). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 61: POST-PARRY CAMERA RECOIL KICK + TIER 2 SCREEN-EDGE MOTION BLUR. Both features confirmed absent via grep (no parryKick/parryRecoil/cameraKick hits; no motionBlur/edgeBlur/backdrop-filter hits). (A) PARRY RECOIL KICK: added `FpPolish.parryKick { active, elapsed, magnitude:0.09 rad (~5.2°), duration:0.20s }` to FpPolish state. `triggerParryKick()` arms the tween (safe re-arm — restarts elapsed). `updateParryKick(delta)` computes additive pitch offset using 1−smoothstep(t) decay (peak at t=0, eases to 0 with no abrupt snapping) — returns 0 when inactive or non-FP. Critical placement: called BEFORE `camera.rotation.set(fpPitch + _pkOffset, fpYaw, 0)` in animate() (at the point of rotation write, not after like updateFovPunch) so offset lands on the same frame it is computed. Mouselook is untouched — `fpPitch` keeps accumulating from mousemove normally; the kick is purely additive and disappears over 200ms. `triggerPerfectParry()` calls `triggerParryKick()` immediately after `triggerFovPunch()` (step 6). (B) TIER 2 EDGE MOTION BLUR: new `<div id="fp-edge-blur">` (HTML) + CSS `backdrop-filter: blur(2px)` + `mask-image: radial-gradient(ellipse 60% 50% at center, transparent 50%, black 90%)` — centre clear, only periphery blurs. z-index 31 (above charge-impact-flash at 30). `triggerEdgeBlur()` mirrors chargeImpactTimer guard (clearTimeout on re-trigger, classList add/remove, 80ms timeout). Fires at tier-2 swing start (after afterimage chain, same block as r59/r60 tier-2 effects at ~L8265). (C) resetMatch() cleanup: `parryKick.active=false`, `parryKick.elapsed=0`, clearTimeout+edgeBlurEl.style.opacity='0'+classList.remove. /3d/index.html grew 9670→9745 lines (+75). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 60: TIER 2 GROUND DUST BURST + SWORD POINT LIGHT FLASH. Both features confirmed absent via grep (no groundDust/dustBurst/impactDust/groundSplash hits; no tier2_flash/spawnSwingLight hits). Implementation: (1) `GroundDustSystem` class — mirrors `SnowPuffSystem` architecture; `spawn(worldPos)` places 30 THREE.Points particles in a flat radial disk from defender's ground position, velocity 1.0–1.6 m/s outward + 0.12–0.26 m/s upward, lifetime 0.5 s, dust-beige `0xb39074`, NormalBlending, depthWrite=false; `update(delta)` per-frame: opacity fade `0.65*(1-t)`, size grows 1×→2× (expanding puff), radial deceleration `×(1-1.8*delta)` per axis, gravity `3 m/s² downward`; `clearAll()` for resetMatch hook. Tagged `points.name='tier2_dust'`. (2) `window.spawnSwingLight(worldPos, color, intensity, distance, durationMs)` — spawns `THREE.PointLight(0xddeeff, 6, 4)` at sword-tip world pos; `requestAnimationFrame` fade loop drives `intensity → 0` over 120ms then `scene.remove`; tagged `light.name='tier2_flash'`. Sword-tip world pos resolved via `fpSwordClone.localToWorld(new Vector3(0,0.9,0))` (same offset as WeaponTrail `_tipLocal`); fallback to `attacker.root + 1.4m` when no FP sword. (3) Call sites: tier-2 release fires `spawnSwingLight` immediately after afterimage chain (discharge flare on the frame the blade leaves); tier-2 impact callback fires `spawnGroundDust(defenderRoot.position, y=0)` before the screen-edge cyan flash. (4) resetMatch() cleanup: `groundDustSystem.clearAll()` + walk `scene.children` filtering `name==='tier2_flash'` → `scene.remove`. animate() loop: `groundDustSystem.update(delta)` inserted after `snowPuffSystem.update`. All r46-r59 preserved. /3d/index.html grew 9485→9670 lines (+185). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 59: SWORD SWING AFTERIMAGE (tier 2) + EMISSIVE GLOW (tier 1/2). Both features confirmed absent via grep. `spawnSwingAfterimage()`: clones `fpSwordClone`, deep-clones materials (transparent, opacity 0.5, cyan tint 0xaaccff, depthWrite=false), snapshots world matrix via `matrixAutoUpdate=false` + `matrix.copy(fpSwordClone.matrixWorld)` so ghost freezes in world space at spawn instant; `requestAnimationFrame` fade loop drives opacity 0.5→0 over 300ms then `scene.remove` + `m.dispose()` per cloned mat. 4 ghosts spawned at 0/50/100/150ms on tier 2 release. `setSwordEmissive(color, intensity, durationMs)`: applies emissive to all `fpSwordClone` materials; uses generation token `fpSwordClone._glowGen` (increment-before-apply, capture-in-closure, only restore if still current) to survive rapid re-entry — same pattern as r56/r63 leak fixes. Baseline emissive (`color`, `intensity`) captured once at `attachFpSword` into `fpSwordClone._emissiveBase[]` so restore targets true defaults, not live values. Tier 1 release: amber `0x442200`, intensity 0.4, 350ms. Tier 2 release: cyan-white `0x4080aa`, intensity 0.7, 500ms + afterimage chain. Glow co-exists with weapon trail (different blend path). `resetMatch()` extended: walks `scene.children` for `name==='sword_ghost'` → removes + disposes cloned mats; also bumps `_glowGen` + restores baseline emissive to abort any pending restore closures. All r46-r58 preserved. /3d/index.html grew 9354→9485 lines (+131). 2 git commits.

## Current state (baseline as of 2026-06-17)
- Core: 2-player seated frying-pan brawler on a log; rigid-pendulum tap-to-swing combat; balance = LIFE (0 → fall off → lose); physical knockback can ring-out; "流れパン" recoil-hit is an INTENTIONAL gray-zone mechanic (do not gate hits on tap).
- Specials: 4 (Mega Smash / Counter Stance / Dash Tackle / Anchor), chosen on a SELECT screen; SP gauge fills via combat; special key P1=F, P2=/. Specials shake hard (addBigShake) + big onomatopoeia.
- Timer: 99s round; time-up decided by higher balance; tie → draw replay. Best-of-3.
- Modes: 1P vs CPU (reactive AI), 2P LOCAL, ONLINE (Cloudflare Worker+DO relay, host-authoritative; wss://lkb-relay.noppiki.workers.dev/r/<code>).
- Art: generated pixel-art ASSETS in assets/ (bg.jpg Nordic dusk; knight_red/blue sprites; title_crest; 4 special icons). Knights drawn as sprites rotated about the seat; pan-tip hit geometry derived from sprite anchors. Do NOT revert to procedural.
- Juice: hit-stop, screen shake (normal toned down ~60%, specials strong), sparks, floaters, WebAudio SFX.
- Deployed to GitHub Pages: https://noppiki.github.io/log-knight-battle/

## Known limitations / candidate improvement areas
- No netcode prediction/interpolation (guest sees RTT input lag); no reconnect.
- AI difficulty is a reasoned medium, not tuned against real play.
- Live 2-browser online play not yet verified in this env (agent-browser can't drive rAF).
- Possible polish: round-intro/victory presentation, sound design depth, control remapping, mobile/touch, colorblind cues, performance audit, more special VFX variety.

## Standing policy (producer directive 2026-06-17)
**Prefer generated image assets over procedural Canvas drawing.** Any visual element still drawn procedurally (HUD gauges/frames/pips, the log, card frames, banner ribbons, particles, onomatopoeia, icons, the speaker indicator, the title controls strip, etc.) is a candidate to replace with a generated pixel-art asset that fits the established Nordic dusk aesthetic. Procedurally-driven MOTION is fine — but the pixels it draws should be sprites where reasonable. Reject AI-slop; commit to the world.

Still procedural (replace candidates, roughly in priority order):
- HUD frame, balance/SP gauges, win pips, timer slab, "TIME" label
- Speaker / mute indicator
- The log (cylinder + bark + end-grain rings)
- SELECT card frames + cursor outlines
- Round-over / Match-over banner backgrounds + restart button
- Hit-spark particles + onomatopoeia floaters (consider a sprite-based stamp system)
- Mode-selector slabs on the title

## Run log
- 2026-06-17 — Online host wait-for-opponent no longer times out (watchdog now only guards reaching the relay; connect timeout 8s→12s). Decluttered the title screen (removed the redundant two-column controls panel; rebalanced spacing; prompt → "PRESS ENTER TO START").
- 2026-06-17 — Audio: procedural Nordic-dusk BGM (A1/A2 drone with LFO-swept lowpass + A-minor-pentatonic triangle bell arpeggio through Schroeder feedback-delay reverb, ~75 BPM, sparse with rare octave-up shimmer accents every 8-16s); global M-key mute toggle (single `globalGain` node mutes BGM + SFX together, 0.05s linear ramp; persists across reloads under `localStorage["lkb_muted"]`; gated against the JOIN room-code input); procedural top-right speaker indicator with red diagonal slash when muted; BGM lazily starts on first user gesture (autoplay-safe) and tears down its scheduler on tab hide / restarts on show to avoid frozen-clock event bursts.
- 2026-06-17 — Replaced procedural log drawing (cylinder bands + bark grooves + end-grain ellipse rings) with generated Nordic-style WebP sprite (assets/log.webp, 1800×400, transparent background); registered in ASSET_SRCS so loading screen counts it as a pip; drawLog() sprite-first with full procedural fallback if image not yet loaded; knight seat coordinate logTopY and TUNE.logThickness rectangle unchanged so combat geometry is unaffected; drone-and-bell BGM and HUD intact.
- 2026-06-18 — Replaced procedural HUD timer slab (dark frame rect + inset face + bevel + 4 corner rivet rects) with generated Nordic stone-plaque WebP sprite (assets/hud_timer.webp, 348×188, transparent background, knotwork border, iron corner rivets, recessed center panel); registered as hudTimer in ASSET_SRCS (loading pip auto-increments); urgent state now shows a two-layer soft red halo (rgba 18%→30%) behind the plaque instead of crimson fill; existing numeral blinking, "TIME" caption, and all non-timer HUD logic unchanged; procedural slab kept as imgReady() fallback so in-flight loads show no broken frame.
- 2026-06-18 — Replaced procedural SELECT-screen card backplates (PAL.ink outer frame + #202a40 face + #2e3a56 top bevel + 4 corner rivet rects) with a single generated Nordic carved-stone tablet WebP sprite (assets/card_frame.webp, 588×743 transparent, knotwork-interlace border, iron corner rivets, top center decorative cap, dark recessed inner panel for content); applied to all 4 cards via one drawImage in the existing loop; cursor outlines (drawCardOutline), CPU badge, LOCKED stamps, special icon and name/effect text overlays unchanged; procedural backplate kept as imgReady() fallback for mid-load frames; registered as cardFrame in ASSET_SRCS so LOADING screen pip auto-increments.
- 2026-06-18 — Added generated Nordic ornate banner backdrop (assets/result_banner.webp, 1600×368, transparent WebP, knotwork end caps with hanging tassels, recessed central text panel) drawn behind ROUND_OVER winner-announcement text (780×175 backdrop centered on result text baseline at VH/2−30) and MATCH_OVER victory text (880×110 backdrop at Y=VH/2−100, sized to bracket only the victory line without colliding with the score row at VH/2+8 or the restart button at VH/2+44); registered as resultBanner in ASSET_SRCS (LOADING pip auto-increments); existing dim overlay, TIME UP! kicker, winner/draw text colors, score display, and restart button unchanged; imgReady() guard skips banner on mid-load frames so text remains readable at all times.
- 2026-06-18 — Replaced procedural title-screen mode-selector slabs (3 colored rects — dark PAL.ink outer frame, #2e3a56/#1a2234 face, plus amber strokeRect for the active mode) with a single generated Nordic stone-tab WebP sprite (assets/mode_slab.webp, 760×169, transparent background, thin knotwork strips top/bottom, corner rivets, dark recessed center for label text overlay); active mode shown via a 2-layer amber rim halo drawn behind the slab (rgba(216,164,90,0.22) at −8px then 0.35 at −4px) plus the retained crisp PAL.skyAmber strokeRect on top; inactive slabs use the sprite unadorned; label text colors (PAL.sunGlow active / PAL.steelDk inactive) and mode-selector key bindings (↑↓ / 1 2 3 / Enter) unchanged; procedural fallback kept verbatim inside else branch for mid-load frames; registered as modeSlab in ASSET_SRCS so LOADING screen pip auto-increments.
- 2026-06-18 — Replaced procedural MATCH_OVER restart button (flat PAL.skyAmber rect 230×54 with thin PAL.ink top-accent bar, dark ink label text) with generated Nordic stone-and-iron button WebP sprite (assets/restart_btn.webp, 690×187, transparent background, knotwork rim strips, corner rivets, vertical decorative grips on left/right edges, top amber rim-light highlight, recessed dark inner panel for text overlay); matchBtn click rect (230×54 centered at VW/2, VH/2+44) is unchanged so click hit-testing still works without modification; label text recolored to PAL.sunGlow with a 4px PAL.ink stroke for contrast against the stone face; when sprite not yet loaded (imgReady() false), original procedural amber rect + ink bar + ink text renders as fallback so mid-load frames show no broken button; registered as restartBtn in ASSET_SRCS so LOADING screen pip auto-increments.
- 2026-06-18 — Replaced procedural HUD gauge frames (life bar PAL.ink outer + #2a3146 face and SP bar PAL.ink outer + #191f30 face) with a single generated Nordic stone gauge-bar WebP sprite (assets/gauge_frame.webp, 664×150, transparent background, knotwork rim strips top/bottom, corner rivets, vertical decorative end caps, uniform darker recessed inner slot for bar overlay); registered as gaugeFrame in ASSET_SRCS (LOADING pip auto-increments); same sprite reused twice with different scaled heights (life frame w+12 × h+12 at y−6, SP frame w+12 × spH+8 at spY−4); dynamic bar fills (team-color life fill, teetering balance beam marker, SP amber fill), SP tick marks, and SP READY!/special-name labels all draw on top of the sprite's recessed slot at unchanged coordinates; procedural frames (PAL.ink outer rect + #2a3146/#191f30 inner rect) kept as imgReady() fallback for mid-load frames.
- 2026-06-18 — Added generated Nordic forged-medallion backdrop (assets/countdown_medallion.webp, 680×684, transparent WebP, rune-etched outer rim with bronze tones, knotwork inner ring, four cardinal iron rivets, top decorative knot, dark recessed central face) drawn behind the COUNTDOWN text (3/2/1/FIGHT!) at 320×320 centered at (VW/2, VH/2−30), scaling together with the existing pop animation; FIGHT! frame fades the medallion to 0.85 alpha so the amber text reads cleanly; existing text rendering, font sizes, pop scale animation, and countdown state transitions unchanged; imgReady() guard skips the medallion on mid-load frames so countdown remains readable from frame 1; registered as countdownMedallion in ASSET_SRCS so LOADING screen pip auto-increments.
- 2026-06-18 — Refactored the ONLINE menu's `drawSlabButton` (HOST GAME / JOIN GAME buttons, 300×70 each) to reuse the existing assets/mode_slab.webp sprite instead of its own procedural slab (dark PAL.ink outer + #2e3a56/#1a2234 face + #3a4a6e/#222c42 top bevel + amber strokeRect for selected); selected state now shows the same 2-layer amber halo (22%→35%) + crisp PAL.skyAmber strokeRect used by the title mode slabs, giving the online menu visual continuity with the title; label text drawing (centered "HOST GAME"/"JOIN GAME" 26px with PAL.sunGlow/PAL.steelDk + PAL.ink stroke) and keyboard navigation unchanged; procedural fallback retained inside an else branch; no new asset required (reused existing IMG.modeSlab so no ASSET_SRCS change and no LOADING pip change).
- 2026-06-18 — Shipped STAGE VARIANT SYSTEM: three new generated 960×540 JPG backgrounds joining the existing dusk plate (assets/bg_tundra.jpg cold overcast Arctic midday; assets/bg_goldenhour.jpg warm late-afternoon pine valley with golden sun; assets/bg_aurora.jpg deep midnight blue with green/violet aurora ribbons over snow mountains), registered in ASSET_SRCS as bgTundra/bgGolden/bgAurora so the LOADING pip auto-increments by 3; added top-level STAGES catalog (dusk/tundra/goldenhour/aurora — each {id, imgKey, name}) and `currentStageIndex` global, randomized once per match at startMatch() entry via `Math.floor(Math.random() * STAGES.length)` so all rounds of a match share the same scene; drawBackground() rewritten to pick STAGES[currentStageIndex].imgKey out of IMG[] with a 2-level fallback (chosen stage → IMG.bg if mid-load → flat PAL.skyTop fill) so a not-yet-decoded image never throws; stage name pops in below the medallion at y=130 in PAL.skyAmber on the first countdown step "3" only (inside the existing translate+scale so it inherits the pop animation; gone by 2/1/FIGHT so it doesn't distract); host-authoritative online snapshot now carries `stage: currentStageIndex` and applySnapshot mirrors it (`if (typeof s.stage === "number") currentStageIndex = s.stage;`) so the guest's drawBackground matches the host's chosen scene; image-first policy preserved (procedural sky fallback retained) and the policy now authorizes bigger swings (multiple new bg variants in one shipment) rather than single-asset changes.
- 2026-06-18 — Shipped TRAINING MODE (iteration 1) as the 4th title mode: extended the `modes[]` array in drawTitle() and `MODE_ORDER` (was 3, now 4 — ["1p","2p","online","training"]) so ↑↓/W S cycling reaches it and Digit4/Numpad4 jumps directly; layout math holds (4×184 slabs + 3×16 gaps = 784 < VW=960); title hint text updated to "↑↓ / W S / 1 2 3 4 select mode · ENTER choose specials". enterSelect() short-circuits when gameMode==="training": auto-locks p1.chosenSpecial="mega" + p2.chosenSpecial="anchor", full-resets both knights, clears input arms/held keys/dormant net latches, resets rounds/winners/banners, and jumps straight to startMatch() (which randomizes the stage and calls startCountdown()) — SELECT screen is bypassed entirely. PLAYING branch: P2 input is set to all-false `{left,right,jump,guard,attack,special:false}` (NOT aiInputs); a PIN block runs after tryKnockout() and before checkRoundResolution() that pins p1.balance and p2.balance to TUNE.balanceMax, accelerates P1 SP at +50/s (full in ~2s), keeps p2.special at 0, auto-recenters either knight to spawn (logCenter ± TUNE.spawnOffset, ST.IDLE, onGround=true, zeroed velocities) if shoved off the log or flipped to FALLING, and pins roundTimer = TUNE.roundTime so resolveTimeUp() never fires. checkRoundResolution() has a belt-and-suspenders `if (gameMode==="training") return;` so no future code path can advance rounds/match in training. drawHUD() shows a small two-line "TRAINING / press R to exit" label in PAL.skyAmber + PAL.bone where the round-timer slab would be, and the P2 HUD label switches to "AZURE DUMMY"; balance meters + (empty) win pips remain visible for damage feedback. R-handler intercepts training (any state): full-resets knights, clears particles/floaters/shake/banners, returns to GS.MENU so the player lands back on the title with TRAINING still highlighted (press ENTER again to re-enter). updateHint() (below-canvas strip) gets a training branch describing P1 controls + "P2: passive dummy — press R to exit". Online code paths and the `net.outEvents` push (gated on `gameMode==="online"`) are unchanged, so training has zero net side-effects. Codex adversarial review: no critical issues. Future iterations can add: hitbox visualization, frame data display, dummy guard/recovery toggle, dummy attack-on-recovery option, parry-window indicator, training-specific tutorial cards.
- 2026-06-18 — Shipped K.O. SLOW-MO FINISH: when a knight enters FALLING during PLAYING, the next 0.9 wall-clock seconds run with the physics dt scaled to ×0.35 (≈3× slower) while a giant pop-in "K.O.!" banner (150px white-fill black-stroked headline) with two layered amber glow plates and a "{TEAM} LANDS THE BLOW" subtitle in the foe's color overlays a dimmed scene; new TUNE constants koSlowmoFactor=0.35, koSlowmoDuration=0.9, koBannerInDur=0.18 alongside hit-stop; new globals koSlowmoTimer (wall-clock countdown), koSlowmoElapsed (wall-clock count-up for banner pop-in), koFaller (the KO'd knight, sets subtitle color); BGM ducks to 0.012 over 0.10s and ramps back to BGM_LEVEL over 0.40s when the slow-mo ends — restore funnels exclusively through clearKoSlowmo() so EVERY exit path (natural timeout in update(), awardRound→ROUND_OVER, resolveTimeUp draw branch, startCountdown, beginPlaying, enterSelect, training KeyR reset, enterTitleFromOnline peer-drop path) restores the bus and clears the banner together; +16 addBigShake punctuates the moment (netRecord'd so the online guest gets the same kick); triggered from BOTH real FALLING-entry sites (tryKnockout and updateKnight off-log detection — line 1620/1640 sfxFall() calls are ROUND_OVER/draw banners, NOT FALLING transitions) via a new startKoSlowmo() helper that self-guards against double-fire (koSlowmoTimer>0 short-circuit), TRAINING mode (gameMode==="training" guard at top so the dummy's auto-recenter never spawns a phantom slow-mo), and non-PLAYING states; bgm exports new duck()/restore() methods (with internal actx/bgmGain guards so they no-op when audio not yet up) because bgmGain/BGM_LEVEL are IIFE-private and inaccessible from outside scope; the round timer is implicitly paused while a knight is FALLING (existing `!falling` gate) so slow-mo doesn't cost clock; checkRoundResolution still fires normally (faller crosses VH+120 a bit later — total fall plays out in <1.5s wall-clock); host snapshot extended with koSlowmoTimer/koSlowmoElapsed/koFallerSide and applySnapshot mirrors them via typeof guards (older hosts still work — graceful degrade with no banner). drawKoBanner uses a 0.4→1.2→1.0 overshoot pop scale curve layered with a 0.2s fade-out alpha, drawn after drawHUD and before drawMuteIndicator so it sits above everything except the mute pip. Codex adversarial review caught the enterTitleFromOnline peer-drop interruption path (would have left BGM ducked + banner on title forever); fixed in same pass — re-review pass-through. Producer big-swing directive 2026-06-18.
- 2026-06-18 — Major SFX sound-design overhaul: every combat sound now layered using a new tone()/noise() synth core that replaces single-osc blip() calls (kept intact for compatibility). New voicings: _sfxHit = low square thud (110→70Hz) + metallic triangle clang (540→420Hz) + bandpass noise transient (4.5k→1.5kHz); _sfxSwing = bandpass whoosh (1.8k→0.6kHz) + soft low thump; _sfxClang = dual detuned metal ring (880/1320Hz squares + triangle) + high noise sparkle; _sfxFall = sawtooth rumble (120→50Hz) + sub sine (60→30Hz) + lowpass noise tail (800→80Hz); _sfxFight = C5/G5/D6 chord stab in square+triangle pairs; _sfxMega = 4-layer impact (sawtooth sub-rumble 70→40, square punch 160→90, triangle high 880→600, bandpass noise crack); _sfxCounter = bright square+triangle sparkle + high noise; _sfxDash = bandpass whoosh sweep + sawtooth bass thump; _sfxAnchor = sine planted thud + triangle mid + lowpass stone-scrape noise; _sfxJump/_sfxCount/_sfxSpReady/_sfxSpFail/_sfxSelect/_sfxLock refined as cleaner multi-osc voicings. Added a shared "arena slap-back" feedback-delay bus (DelayNode 0.16s, FB gain 0.34, lowpass 2400Hz) declared as arenaSendGain and wired inside initAudio() right after sfxGain.connect(globalGain); heavy hits route into it via a per-call `send` param on tone()/noise() that creates a tap gain summing into arenaSendGain. Arena bus routes through globalGain (NOT actx.destination) so the M-key mute still gates the tail. Routing chain preserved: tone()/noise() → sfxGain → globalGain → destination, identical to blip() so the existing mute logic, BGM under-mix balance, and online sfx event mirroring (sfx*() wrappers + netRecordSfx + SFX_MAP guest replay) are byte-identical. Each tone/noise stops at t+dur+0.1 so nodes GC after playback; arena feedback (0.34) stays well under self-oscillation threshold and the in-loop lowpass further dampens each pass. Codex adversarial review: no critical issues. Directive 2026-06-18 big-swing authorization.
- 2026-06-18 — Shipped 5th SPECIAL move EARTHQUAKE (id: "quake", ja "ログクエイク", en "EARTHQUAKE"): a pure control move with a 0.4s telegraphed windup followed by a 1.0s active phase during which the log image shakes horizontally at ~10Hz / 14px amplitude and the opponent's seat gets shoved toward the nearest edge at 360 px/s (vx += quakeShoveVx*dir*dt each tick, dir = sign(foe.x - logCenter) so they're pushed off whichever side they're closer to), with brief pendulum destabilization (swingVel kick of 6*dir*dt) and a 0.15s knockTimer to bypass the maxSpeed clamp — NO balance damage at any point (purely positional). Generated dedicated 96×96 Nordic medallion icon assets/icon_quake.png (slamming frying pan + impacted log + amber shockwave rays) consistent with the other 4 medallion icons; registered as iconQuake in ASSET_SRCS (LOADING pip auto-increments) and mapped in SPECIAL_ICON; new TUNE constants quakeWindup=0.4 / quakeActive=1.0 / quakeShoveVx=360 / quakeShakeAmp=14 / quakeShakeHz=10. SELECT card layout shrunk (gap 22→18, cardW 196→160 to fit 5 cards in the 960px viewport: 5×160 + 4×18 = 872, margin 44 each side; fx text startY 182→178 and lineH 15→14 to keep wrapped 3-line text clear of the LOCKED stamp at y0+cardH-30=368). Per-knight quakeTimer + quakeActive fields added to makeKnight() and reset in resetKnightPositions() alongside windup/counter/dash/anchor timers; specialActive() extended to recognize quake so a 2nd activation is blocked while one is in-flight. activateSpecial() new case "quake" spends full SP, sets quakeTimer = quakeWindup + quakeActive, spawns "QUAKE!" floater (PAL.skyAmber), plays sfxAnchor as planted thud and addBigShake(8). tickSpecial() runs the windup→active state machine; transition only fires if the quaker themselves isn't FALLING (so a knocked-off quaker doesn't still BOOM), addBigShake(20) + sfxFall on slam impact, "BOOM!" floater, and active-phase shove guards both quaker and foe against FALLING so no spectral push on a ring-out in flight. drawLog() applies a sin(frameCount * (quakeShakeHz/60) * 2π) * quakeShakeAmp horizontal offset to the log sprite whenever either knight's quakeActive is true. drawKnight() aura gets a quake branch (PAL.skyAmber, breathing alpha during active phase). Online host snapshot extended: snapKnight serializes quakeTimer/quakeActive, applyKnight deserializes with fallbacks (`s.quakeTimer || 0`, `!!s.quakeActive`) so older guest builds without those fields stay consistent. Existing 4 specials (MEGA SMASH / COUNTER STANCE / DASH TACKLE / ANCHOR), TRAINING auto-pick (mega vs anchor), CPU AI pick logic (`default: fire = inRange` catch-all), countdown/round/match flow, and balance/SP gauge integration untouched. Codex adversarial review: no critical issues. Directive 2026-06-18 image-first + big-swing authorization.
- 2026-06-18 — Shipped MOBILE/TOUCH SUPPORT iteration 2: extended the canvas click handler so a tap on the SELECT screen card moves P1 cursor + locks in one gesture (or unlocks if tapping the already-locked card); tapping a different card while already locked silently unlocks the previous choice and locks the new one (one-tap-to-switch); readyTimer guard prevents input during the READY countdown beat; a tap on the ONLINE_MENU HOST GAME / JOIN GAME slab rectangles (each 300×70 centered at VW/2 at y=250/340) directly calls startHost()/startJoin() with cursor pre-set; existing desktop click semantics (MATCH_OVER restart button + iter-1 MENU tap-to-start) are unchanged (new branches sit before the MATCH_OVER guard, keyed by gameState so no cross-state bleed); Codex adversarial review: "No critical issues"; node --check clean. With iter 1 + iter 2 a mobile player can fully play 1P vs CPU and TRAINING and host an online room from a phone, and the JOIN flow already works via the existing #joinInput native keyboard overlay. Known limitation (iter 3 gap): online guest P2 SELECT on mobile still uses keyboard — tap drives sel.p1, but the guest is host-authoritative on sel.p2 state; fixing this requires forwarding guest input via the existing net stream which is reserved for iter 3.
- 2026-06-18 — Shipped MOBILE/TOUCH SUPPORT iteration 1: permissive touch device detection (`IS_MOBILE = 'ontouchstart' in window || navigator.maxTouchPoints>0 || matchMedia('(pointer:coarse)')`) toggles a `body.mobile` class so all mobile CSS/JS is gated behind it (desktop stays byte-identical — same canvas, same resize() inline px sizing, same hint strip, no new event handlers fire). Responsive viewport handling is left to the existing JS resize() (inline `style.width/height` in px always beats stylesheet rules); the spec's responsive canvas CSS block was intentionally omitted because resize() already letterbox-fits any viewport (including phones) and would have either fought the inline px or been dead code; `#stage{padding:6px}` is scoped under `body.mobile` so desktop isn't touched. New `#touchpad` absolute overlay inside `#stage` (display:none on desktop AND on mobile menu/match-over/online-connect screens; shown only when body has both `.mobile` and `.show-touch`) holds 6 Nordic-styled buttons: ◄/► scoot (bottom-left, 76×76), ▲ HOP (bottom-left wide, 162×60), 🛡 GUARD (right column, 84×60), ✦ SP (right column, 84×80), ⚔ HIT (large bottom-right, 120×120 primary action) — rgba(20,26,40,0.72) face + #4a5a7a border + inset amber bevel + text-shadow, `.active` flips to rgba(216,164,90,0.32) + amber border + white text. `#touchpad` itself has `pointer-events:none` so the canvas click-through is preserved; each `.tb` opts back in via `pointer-events:auto`. Touch wiring uses PointerEvent when available (pointerdown/up/cancel/leave) with touchstart/touchend fallback, per-button multi-touch ref-count (`activePointers`) so a thumb that briefly leaves the visible rect doesn't release input prematurely; movement/jump/guard add KeyA/KeyD/KeyW/KeyS to the existing `held` Set (same source of truth as keyboard), attack/special edge-trigger `attackArmed.p1`/`specialArmed.p1` exactly like keydown (auto-consumed by update()'s consumeAttack/consumeSpecial — re-armed on every fresh tap). First touch triggers `initAudio()` to unlock the AudioContext (mobile autoplay-safe). `show-touch` class toggled at top of `frame()` (before the online-guest early-return so guest-on-phone also gets buttons) based on `gameState === PLAYING/COUNTDOWN/ROUND_OVER` — TRAINING is `gameMode` not `gameState` so it runs through PLAYING and gets the pad automatically (per spec). Canvas `click` handler gained a mobile-only MENU branch placed BEFORE the existing `gameState !== MATCH_OVER` early-return guard so a phone tap on the title advances to SELECT or ONLINE_MENU (mirroring Enter/Space in handleGlobalKey); MATCH_OVER restart hit-test stays intact below it. `body.mobile { touch-action:none; overscroll-behavior:none; overflow:hidden }` plus `.tb { touch-action:none }` plus per-event preventDefault prevents accidental page scroll, pinch-zoom, and pull-to-refresh during gameplay. `#touchpad` `contextmenu` is suppressed so a long-press HIT button doesn't pop the OS copy/paste menu mid-fight. Window `blur` clears `.active` from every button (held.clear() in the existing blur handler already releases the keys). Portrait orientation hint via `body::before` content "Rotate to LANDSCAPE for full screen" gated on `@media (max-aspect-ratio:1/1) and (pointer:coarse)` so it only ever shows on portrait phones (z-index:100 with `pointer-events:none` so it never blocks the joinInput which sits at z-index:10 — joinInput is also display:none unless on the ONLINE_JOIN screen which on mobile bypasses the touchpad anyway since show-touch is false there). The existing global keyboard handler, mute toggle, joinInput overlay, canvas resize, and online host/guest paths are completely untouched (all mobile additions are either inside the gated `setupTouchControls()` IIFE which early-returns when !IS_MOBILE, inside `body.mobile`-scoped CSS, or behind an `if (IS_MOBILE)` runtime guard). Codex adversarial review: "No critical issues" (residual feel-only note about pointer capture during heavy drag — non-blocking). STAGE 1 LIMITATION: only 1P-vs-CPU and TRAINING are fully playable on mobile this iteration (P2 keys aren't exposed on the pad); 2P LOCAL still needs a desktop keyboard; ONLINE JOIN room-code entry uses the native mobile keyboard via the existing `#joinInput` overlay; SELECT screen on mobile still needs keyboard for now (tap-to-pick on cards is deferred). STAGE 2 candidates: portrait virtual-stick layout, haptic feedback (`navigator.vibrate`) on hit/lock/KO, swipe-up gesture for special activation, tap-to-pick + tap-to-lock on SELECT cards, optional SP button auto-fire when ready.
- 2026-06-18 — Shipped PAUSE feature: new GS.PAUSED state with Esc/P toggle (canPauseHere() blocks online play for fairness + non-PLAYING/COUNTDOWN states + active KO slow-mo); pause short-circuits update() at the top so no physics/timer/AI/online sync ticks while paused; render() still runs every frame and overlays a dimmed scene (rgba 0.66) with "PAUSED" amber headline (900 80px, PAL.skyAmber fill + PAL.ink stroke) + "Press ESC or P to resume" subtitle; BGM ducks to 0.020 on pause and restores via bgm.restore(0.20) on resume; pauseResumeState global remembers what to return to (GS.PLAYING or GS.COUNTDOWN) so resume is exact; on pause-entry held/attackArmed/specialArmed are cleared so mobile touch input never sticks across the pause boundary; pause state cleared on enterSelect/startMatch/training-R transitions to prevent soft-lock; online mode unaffected (Esc still triggers leaveOnline via existing handleGlobalKey handlers because canPauseHere returns false for gameMode==="online"); settings overlay Escape continues to close settings first (existing line-1069 precedence in keydown listener preserved; pause toggle placed after e.repeat guard at line 1125 area so no auto-repeat flip); drawPaused() drawn after drawKoBanner() before drawMuteIndicator() so overlay sits on top of gameplay with mute pip still visible. Mobile pause via Esc/P only this iteration (touch button deferred). Codex adversarial review caught mobile held-input stickiness on pause → fixed by clearing held/attackArmed/specialArmed in togglePause(). Live-verified locally + on deployed Pages per policy 3.
- 2026-06-18 — Shipped SETTINGS MENU (foundational QoL): new HTML overlay (#settings div, z-index:30, translucent ink backdrop + #11151f Nordic card with #d8a45a amber border) toggled by a top-right gear ⚙ button (#settings_btn, right:56px so it sits beside but doesn't overlap the canvas-painted mute speaker icon) AND the global "O" key (works from MENU/SELECT/PLAYING/ROUND_OVER/MATCH_OVER/ONLINE_* states); Escape also closes when open. Four controls: BGM VOLUME slider (0..100 step 5), SFX VOLUME slider (0..100 step 5), SCREEN SHAKE select (off/reduced/normal), K.O. SLOW-MO select (on/off). Persisted under localStorage["lkb_settings"] as a JSON blob with try/catch on both read and write (degrades gracefully on quota-full / SecurityError / corrupted JSON; defaults match prior behavior: full volumes, normal shake, slow-mo on). Live application: applyAudioSettings() sets sfxGain.gain.value = sfxVolume/100 (sfxGain previously fixed at 1.0; mute still lives on globalGain so SETTINGS is orthogonal to the M-key) and calls a new bgm.setVolume(v) IIFE-exported method that stores userBgmScale (0..1) and ramps bgmGain to BGM_LEVEL*userBgmScale over 0.15s via cancelScheduledValues→setValueAtTime(current)→linearRampToValueAtTime so a slider tweak during the K.O. slow-mo duck doesn't fight the existing duck/restore scheduling catastrophically; restore()/start()/visibility-resume call sites all updated to target BGM_LEVEL*userBgmScale instead of bare BGM_LEVEL so a slider change made mid-duck still restores to the player's chosen level (start()'s exponentialRamp also gets a Math.max(0.0001, ...) floor since 0 is invalid for exponentialRamp). Shake intensity wired via shakeMul() (off→0, reduced→0.4, normal→1.0) multiplied into the amount at the TOP of addShake/addBigShake before _addShake/_addBigShake; the network shake event path (playEvent case "shake") also applies shakeMul() locally so each peer respects their own setting (raw amount stays on the wire). K.O. slow-mo guarded with `if (!koSlowmoEnabled) return;` at the top of startKoSlowmo so the FALLING → off-screen → ROUND_OVER pipeline still flows normally (just no slow-mo, no banner, no BGM duck) when disabled. TDZ fix: settings state declarations (bgmVolume/sfxVolume/shakeIntensity/koSlowmoEnabled + loadSettings + saveSettings + shakeMul + applyAudioSettings) sit BEFORE the DOM wiring block so syncSettingsUI()'s initial mirror call doesn't crash on `let`-in-TDZ; applyAudioSettings references `bgm` only at call-time (initAudio / slider input) which is post-eval so the `typeof bgm` guard never throws. Settings panel inputs stopPropagation at the settingsEl level AND the window-level keydown listener has an INPUT/SELECT/BUTTON tagName guard while panel is open so arrow keys on sliders never leak to the game — the O-key handler runs BEFORE that guard so "Press O to close" hint text always works regardless of slider focus. Gear button click triggers initAudio() so opening settings doubles as the first-gesture audio unlock. Codex adversarial review caught the initial TDZ ordering bug (let bgmVolume declared at line ~1205 but syncSettingsUI() called at line ~934); fixed by relocating block to before DOM wiring; follow-up review: "No critical issues". Live-verified on local serve (zero console errors; title screen renders with gear icon; O opens panel with all 4 controls visible) AND deployed Pages per policy.
- 2026-06-18 — Added PLAYER STATISTICS tracking: `localStorage["lkb_stats"]` persists a fixed-size JSON object (matches/matchesWon/matchesLost/rounds/roundsWon/roundsLost/draws/koScored/koSuffered/currentStreak/bestStreak/specials{mega,counter,dash,anchor,quake,whirl}/playSeconds/firstPlayed/lastUpdated). Hook sites: (1) `awardRound()` at function-end records rounds+KOs (foe.state===ST.FALLING → koScored, me.state===ST.FALLING → koSuffered) and match outcome+streak on matchWinner set; (2) `resolveTimeUp()` draw path records draws+rounds (does NOT call awardRound — no double-count); (3) `activateSpecial()` top-of-function counts per-special activation for the local human knight only; (4) `update()` GS.PLAYING branch accumulates wall-clock playSeconds using raw dt **before** the KO slow-mo dt scale so slow-mo windows don't undercount. saveStats() is debounced (600ms timer) to avoid hammering localStorage; playtime flush also fires every ~5s so long sessions aren't lost. syncStatsUI() updates 12 display fields (Matches/Won/Lost/Win Rate/Rounds/Draws/KO Scored/KO Suffered/Streak/Best Streak/Favorite Special/Play Time) and is called on every openSettings(). RESET STATS button in the panel prompts confirm() then calls resetStats() which performs an in-memory clear + immediate synchronous write (bypasses debounce). localHumanKnight() helper: returns p2 for online-guest, p1 otherwise (P1 perspective for 2p). Stage-1 limitation: stats are skipped in training AND online modes (gameMode !== "training" && gameMode !== "online" guards on all four hooks) — documented known limitation. Codex adversarial review verdict: "No critical issues". Live-verified per policy 3: local errors=[] + title screenshot + settings panel with YOUR STATS section visible (all counters 0, Win Rate "—", Favorite Special "—", Play Time "0s"); deployed Pages live-verified identically.
- 2026-06-18 — Added ACHIEVEMENTS system on top of the STATS foundation: 14 fixed entries (FIRST BLOOD / APPRENTICE / VETERAN / CENTURION matches milestones, SURVIVOR / LEGEND win-streak, PAN SMASHER / MIND GAME / TACKLE BOX / ANCHOR SOUL / EARTH SHAKER / SPINNER per-special 20×, MULTIFACETED use-all-six, MARATHON 60 minutes total play). Unlock state persisted as a JSON string array in `localStorage["lkb_achievements"]` with a 400ms debounce on `saveAchievements()`; `resetAchievements()` performs an immediate synchronous write (bypasses debounce) and clears the in-memory `unlockedAchievements` Set. New `checkAchievements()` iterates ACHIEVEMENTS, tests each predicate inside a try/catch (so a malformed `stats.specials` shape can never crash the run), pushes any newly-flipped entry onto `achTostQueue` (intentional typo preserved per spec) + plays `sfxSpReady()` (also guarded with `typeof === "function"` + try/catch); training + online modes are excluded by the same gate stats use. Hook integration is surgical: 4 sites, each immediately after the corresponding `saveStats()` call (`awardRound()` round/match end, `resolveTimeUp()` draw branch, `activateSpecial()` per-special counter, `update()` PLAYING playtime 5s tick) — no other code paths touched. Canvas toast (460×76 amber-bordered slab pinned at y=38 centered horizontally): `updateAchToast(dt)` runs as the FIRST statement of `update()` (above the PAUSE early-return and the hit-stop early-return) so a tail-end toast keeps animating + dismissing even when the world is frozen; `drawAchToast()` is rendered after `drawPaused()` but before `drawMuteIndicator()` so the toast sits above gameplay/pause overlays while the persistent mute pip still wins. Toast has a 3.2s lifetime with a 0.25s slide-down + fade-in lead-in and a 0.4s fade-out tail; head-of-queue popped on entry, queue can grow only on new unlocks (bounded by 14 — the entire achievement set) so unbounded growth is structurally impossible. SETTINGS panel gains a new `.ach-section` below `.stats-section` (inside `.settings-card`, before `#set_close`): header "ACHIEVEMENTS X/14" amber heading with bone-grey counter; scrollable list (`#ach_list`, max-height 200px) of 14 rows rendered by `syncAchUI()` on every `openSettings()` (called alongside `syncSettingsUI()` + `syncStatsUI()`) — unlocked rows show ⭐ + bright amber name + "DONE"; locked rows render with 🔒 + grey-blue dimmed name + "—" via the `.locked` class (opacity 0.45). RESET ACHIEVEMENTS button (deep maroon Nordic style, matching #stats_reset's palette) prompts `confirm()` then clears + re-renders. The MULTIFACETED predicate uses `["mega","counter","dash","anchor","quake","whirl"].every(id => (stats.specials[id] || 0) >= 1)` so missing fields never throw. Codex adversarial review (TDZ / unbounded queue / undefined PAL.skyAmber / undefined sfxSpReady / JSON.parse crash / dt===0 pause stall / reset failing to clear memory — all the spec-flagged failure modes): "No critical issues." node --check clean. Live-verified per policy 3: local serve (errors=[] both at title-load AND after pressing O; title screenshot shows the world properly; settings screenshot shows the new ACHIEVEMENTS section with `0/14` counter and FIRST BLOOD / APPRENTICE / VETERAN / CENTURION visible in the scrollable list with 🔒 icons + dimmed locked styling); deployed Pages live-verified identically. Known intentional behavior: pre-existing high stats do NOT retroactively unlock — unlocks only ever fire through the saveStats hooks, so a player with prior data needs to play one more round to crystallize them.
- 2026-06-18 — Shipped HOW TO PLAY help overlay — HTML overlay (`#help`, `z-index:30`, translucent ink backdrop + 640px Nordic amber-bordered card) opened by H key or new `?` button placed at `right:104px` next to the existing gear button at `right:56px`; sections: GOAL, CONTROLS (per-player key tables with P1 crimson / P2 azure headings), BALANCE = LIFE, SP GAUGE & SPECIALS (6 entries auto-rendered from SPECIALS array via `populateHelpSpecials()` — `sp.en` name + `sp.fx` effect line), 4 MODES, TIPS; CLOSE (H) button + Esc closes; mutually exclusive with SETTINGS (opening one closes the other: `openHelp()` calls `closeSettings()`, `openSettings()` calls `closeHelp()`); `canPauseHere()` gains `isHelpOpen()` check so Esc/P cannot pause behind the open overlay; HTML input guard at top of keydown listener extended to gate help (`isSettingsOpen() || isHelpOpen()`) so arrow keys on CLOSE button don't bleed to game; styled to match Nordic dusk aesthetic. Codex adversarial review: no critical issues. Live-verified per policy 3 (local + deployed).
- 2026-06-18 — Shipped CPU DIFFICULTY setting — adds EASY / NORMAL / HARD select to SETTINGS panel (new `<div class="setting-row">` before the stats section, `id="set_cpu"`), persisted in `lkb_settings` alongside existing knobs. New `cpuMul()` function returns a per-tunable multiplier object; NORMAL returns all-1 (byte-identical to prior behavior); EASY adds ~400ms reaction lag (react mul ≈ 3.22), +50% swing interval, guard probability ×0.60, edge avoidance ×0.60 (less self-preservation), special-fire probability ×0.50, SELECT pick delay ×1.30; HARD subtracts ~150ms reaction (react mul ≈ 0.17, clamped ≥0 with Math.max), swing interval ×0.70, guard probability ×1.25, edge avoidance ×1.40, special-fire probability ×1.50 (effectively always fires when eligible since random()<1.5), SELECT pick delay ×0.60. Multipliers applied at site of use in `aiInputs()` (`TUNE.aiReactDelay * mul.react`, `(TUNE.aiSwingCdMin + … ) * mul.swingCd`, `TUNE.aiGuardProb * mul.guard`, `TUNE.aiEdgeMargin * mul.edgeMargin` for both edge detection and dash safety check, special-fire block via `if (Math.random() < mul.special)` gate) and in `cpuPickTimer` initialization at SELECT-entry (`… * cpuMul().pickDelay`); changes take effect immediately mid-match with no state caching. `cpuDifficulty` declared before `loadSettings()` (no TDZ risk); `loadSettings()` restores the enum; `saveSettings()` persists it alongside other fields; `syncSettingsUI()` mirrors value into the select; change listener validates enum membership before writing. Codex adversarial review: "No critical issues" (residual note: hard mul.special=1.5 makes specials fire deterministically when eligible — acceptable/intentional). node --check clean. Local live-verified: errors=[], title screenshot, settings screenshot shows CPU DIFFICULTY row with NORMAL selected. Deployed Pages build success (1m15s). Deployed live-verified: errors=[], title screenshot, settings screenshot confirms CPU DIFFICULTY persisted to production.
- 2026-06-18 — Shipped STAGE PREFERENCE setting: adds Random/Nordic Dusk/Frozen Tundra/Golden Hour/Aurora Night select (`id="set_stage"`) to SETTINGS panel after CPU DIFFICULTY and before YOUR STATS; persisted in `lkb_settings` JSON alongside existing keys (no collision — additive extend of the same `JSON.stringify` object); default "random" preserves existing behavior (Math.random pick at startMatch); specific id uses `STAGES.findIndex(s => s.id === stagePreference)` with `|| 0` fallback so an unrecognised id never takes currentStageIndex out of range; takes effect on the NEXT match start (mid-match unaffected — all rounds of one match share one scene as designed); online host remains authoritative (host calls startMatch → sets currentStageIndex → snapshot carries it to guest); `syncSettingsUI()` extended to mirror value into the select on open; change listener validates enum membership before writing; `settingsEl keydown` stopPropagation guard already in place so no input bubbles to game during PLAYING. Codex adversarial review: "No critical issues" (checked TDZ, persistence collision, random fallthrough, id mismatch, input bubbling, training/online override paths — all clean). node --check SYNTAX OK. Live-verified locally (errors=[], title screenshot, settings screenshot shows STAGE row with RANDOM select) and on deployed Pages (same, errors=[]).
- 2026-06-18 — Shipped ACHIEVEMENTS Tier-2 expansion — 8 new entries (WINNER 10 wins / CONQUEROR 50 wins / HALL OF FAME 100 wins / PUNCH DRUNK 100 KOs / BRUISER 100 rounds / DEEP ROSTER each of 6 specials ≥5 uses / EXHIBITIONIST 50 specials total / CHRONOMASTER 3 hours play) appended to existing ACHIEVEMENTS array (lines 1246–1253 of index.html, after the existing 14 entries); counter auto-rolls to 22 (syncAchUI derives total from ACHIEVEMENTS.length, not a hardcoded literal); scrollable list in SETTINGS panel auto-extends; zero new state or tracking — every test() reuses existing STATS_DEFAULTS fields (matchesWon, koScored, rounds, specials[id], playSeconds) that are guaranteed-present integers so no crash on wiped/fresh saves; existing 14 unlocks preserved across persisted localStorage.lkb_achievements (Set membership test, id-keyed); node --check SYNTAX OK; orchestrator-level policy-3 live verification: local server errors=[] + title screenshot (game renders) + settings screenshot confirms ACHIEVEMENTS 0/22; deployed Pages (build success) errors=[] + title screenshot + settings screenshot confirms ACHIEVEMENTS 0/22.
- 2026-06-19 — Shipped MOBILE iter 3 slice: new `.tb-pause` touch button (60×44 top-center, low-profile slate face rgba(20,26,40,0.62) + #4a5a7a border, text "II") toggles GS.PAUSED via `togglePause()` on pointerdown; the pause branch in `onDown` sits as the VERY FIRST statement — before `activePointers++` or any `held`/arm writes — and the corresponding `onUp` branch also returns early so the pause button never leaks into the held-key machinery; show-touch state list extended to include GS.PAUSED so the pause button stays visible during pause to allow resume; new `paused-mobile` body class toggled in sync with GS.PAUSED, gated by `body.show-touch.paused-mobile .tb:not(.tb-pause) { display: none; }` CSS so all other touch buttons hide while paused (prevents queued input on resume); new `tryVibrate(ms)` helper (function declaration, hoisted) gates on `IS_MOBILE` → `navigator.vibrate` existence → `shakeIntensity !== "off"`, scales pattern values by 0.4 for "reduced" or 1.0 for "normal", rounds each entry with `Math.round`, swallows any exception — desktop never vibrates, pattern values cannot go negative; wired at three gameplay sites: `applyHit()` end (20ms tap on every confirmed landed swing, counter-path's early-return means a countered hit does NOT haptic — intentional), `startKoSlowmo()` after the four guards pass (60/30/80ms pattern: heavy thump → short gap → heavier thump, also skipped when koSlowmoEnabled=false — shares the same gate), `gainSp()` immediately after `sfxSpReady()` (40ms mid-tap when SP gauge first fills, fires for both knights including CPU — mirrors the existing chime); haptics are fully independent of the audio mute gate but share the `shakeIntensity` preference for motion sensitivity (a player who disables screen shake also disables vibration); Codex adversarial review: "No critical issues" (all listed failure modes checked: TDZ-safe, desktop-gated, pause button not consumed as held, pause button visible during PAUSED, pattern values non-negative after scaling); node --check clean; live-verified per policy 3: local server errors=[], title screenshot renders, .tb-pause element present (eval=1), deployed Pages build success, live errors=[], live .tb-pause element present (eval=1).
- 2026-06-19 — Shipped ATTRACT MODE — 30s of MENU idle starts an internal "attract" gameMode demo with random specials per side and a random stage; both knights driven by aiInputs() each frame; "🎬 DEMO" amber banner at top + blinking "PRESS ANY KEY TO PLAY" prompt at bottom; ANY keydown / pointerdown / canvas click exits cleanly back to MENU (no input event leaks into game state); demo chains via 1.5s grace setTimeout after a natural match end OR 60s timeout; gameMode==="attract" is INTERNAL only (not exposed on the title mode-cycle which still toggles 1p/2p/online/training); stats/achievements/playSeconds skip attract just like training/online; canPauseHere() blocks pause during attract; KO slow-mo + haptics + BGM still play normally for visual/audio polish; `const [i1, i2kbd]` changed to `let` so attract's i1-reassignment doesn't throw; idle timer gated on isSettingsOpen/isHelpOpen so settings open during MENU never triggers attract; mobile touchpad hidden during attract demo; Codex adversarial review (TDZ / recursive enter-exit loop / settings/help input swallowed / canvas click vs MATCH_OVER restart / idle timer on SELECT / stale gameMode / 50ms setTimeout race / stale matchWinner after attract / pointerdown on touchpad / idle timer while settings open): no critical issues raised after advisor-flagged fixes applied (const→let, settings/help guard on idle tick, 50ms guard uses gameState===MENU check, awardRound stats guard extended to attract); node --check SYNTAX OK; orchestrator-level policy-3 live verification: local server errors=[] at 5s (correct: no premature attract before 30s), title screenshot renders; waited 35s actual idle → attract triggered: screenshot shows 🎬 DEMO badge + two knights fighting + "PRESS ANY KEY TO PLAY" + errors=[]; deployed Pages build success, live errors=[] + title screenshot renders without entering attract during 4s verification window (correct: 30s threshold).
- 2026-06-18 — Shipped 6th SPECIAL move WHIRLWIND (id: "whirl", ja "旋風斬り", en "WHIRLWIND"): new MULTI-HIT close-range archetype distinct from the existing 5 (mega=burst/telegraphed, counter=defensive reflect, dash=committed lunge, anchor=knockback resist, quake=control/shove). 1.5s total — windup 0.20s (vulnerable cock-back) + active spin 1.00s + recovery 0.30s (vulnerable). During the active phase the knight's pendulum swingVel is FORCED to ±18 rad/s (facing-signed) every frame so the pan tip continuously rotates around the seat in a circle, and the pan tip is hit-tested against the foe's bodyBox each frame using the existing panTip() / circleHitsBox() helpers with a per-hit cooldown (whirlHitCdSec=0.15s) so the same opponent gets bitten at most every 0.15s — ~5-7 hits over the 1.0s spin. Per landed hit: 5 balance damage + 80 horizontal seat knockback (light) + small swingVel rock-back on the foe + spawnSparks(6, PAL.spark) + sfxHit + addShake(5) + gainSp(TUNE.spGainLand*0.3) to the spinner; total damage if all hits land ~30 balance — comparable to a mega smash, but countered by simply backing away outside pan reach since the spinner is LOCKED in place (vx zeroed during active+recovery and windup; vy zeroed + y snapped to logTopY + onGround forced + ST.JUMP→ST.IDLE during active+recovery so a queued hop cannot lift off; cannot scoot/hop/dodge). resolveCombat aLive/bLive checks extended with `&& !p1.whirlActive`/`&& !p2.whirlActive` so the forced spin pan CANNOT route into the standard combat path (would otherwise same-frame double-hit since whirlSwingVel 18 >> hitSpeed 2.25) and CANNOT trigger CLASH/COUNTER routing mid-spin — Codex adversarial review surfaced this exact double-hit risk and the gate is the surgical fix. specialActive() extended to recognize whirlTimer>0 so a 2nd activation is blocked while one is in-flight. activateSpecial new case "whirl" spends full SP, sets whirlTimer = whirlWindup+whirlActive+whirlRecovery, spawns "WHIRL!" floater (PAL.skyAmber), plays sfxDash for the windup whoosh, addBigShake(6). tickSpecial whirl block runs phase-derived state machine (inRecovery = t<=whirlRecovery, inActive = t>whirlRecovery && t<=whirlRecovery+whirlActive) with FALLING-gated lock + hit-test so a knocked-off spinner cleanly drains the timer without spectral hits or spectral lock. New 96×96 transparent PNG icon assets/icon_whirl.png (Nordic medallion with spinning frying pan + amber motion swirls, matching the style of the other 5 medallion icons); registered as iconWhirl in ASSET_SRCS (LOADING pip auto-increments) and mapped in SPECIAL_ICON. New TUNE constants: whirlWindup=0.20 / whirlActive=1.00 / whirlRecovery=0.30 / whirlSwingVel=18 / whirlHitBal=5 / whirlHitKnock=80 / whirlHitCdSec=0.15. Per-knight state fields whirlTimer/whirlActive/whirlHitCd added to makeKnight() and reset in resetKnightPositions() alongside the other special timers. SELECT card grid shrunk from 5 cards at gap=18,cardW=160 (totalW=872, margin=44) to 6 cards at gap=15,cardW=135 (totalW=885, margin=37.5) in both drawSelect() and the mobile tap-to-pick hit-test branch; card_frame.webp sprite scales cleanly at the slightly narrower 143×248 draw size; sp.fx text for WHIRLWIND tightened to "Spin attack: pan multi-hits a foe in range." (43 chars) so it wraps cleanly at cardW-24=111 wrap width. drawKnight aura branch added for whirl (PAL.skyAmber, breathing alpha 0.28+0.12sin(0.4f) during active phase, dim 0.18 during windup/recovery) parallel to the other 5 special auras. Online host snapshot extended: snapKnight serializes whirlTimer/whirlActive, applyKnight deserializes with typeof-guard (`typeof s.whirlTimer === "number" ? s.whirlTimer : 0`, `!!s.whirlActive`) so a backward-compat guest on an older host doesn't crash on a missing field. Existing 5 specials (MEGA SMASH / COUNTER STANCE / DASH TACKLE / ANCHOR / EARTHQUAKE), TRAINING auto-pick (mega vs anchor), CPU AI pick logic (`default: fire = inRange` catch-all handles whirl automatically), countdown/round/match flow, balance/SP gauge integration, KO slow-mo, PAUSE, SETTINGS, and mobile touch pad all unchanged. Codex adversarial review: caught the same-frame double-hit risk (whirl branch + resolveCombat both firing because forced swingVel exceeds hitSpeed) → fixed by adding `!whirlActive` to aLive/bLive in resolveCombat; follow-up review: "No critical issues." node --check clean. Live-verified locally with screenshot confirmation: title screen renders, SELECT screen shows all 6 cards including WHIRLWIND with icon visible and text not overflowing, zero console errors.
- 2026-06-19 — ATTRACT MODE polish (follow-up): removed a duplicate idle-timer tick in frame() that was running in parallel with the canonical tick in update() (would have doubled the trigger rate, firing attract at ~15s instead of 30s on the live site); added `ATTRACT_MAX_SEC = 60` hard-timeout in update()'s GS.PLAYING branch so a runaway demo can't soft-lock on the title; added `if (gameState !== GS.MENU) return;` as a second guard in enterAttractMode() for defense-in-depth. node --check clean; live-verified locally; deployed.
- 2026-06-19 — ATTRACT MODE iter 29 bugfix: Codex adversarial review found attractTimeoutTimer double-counted (incremented at both the existing 4274-line block inside GS.PLAYING under `if (attractActive)` AND a newly-added second block under `if (gameMode === "attract")` on the same frame — net effect was 60s cap expiring in ~30s wall-clock); removed the duplicate block; replaced hardcoded `60` with `ATTRACT_MAX_SEC` constant so the cap is defined once at 2707. Codex confirmed no other critical issues: no TDZ, no recursive enter/exit loop, no stats leak, pause correctly blocked, aiInputs side-neutral, all input paths reset idle, latches cleared on exit, online snapshot untouched, mobile pointerdown exits attract, timeout uses unscaled dt (correct for KO slow-mo), BGM unaffected, stagePreference unaffected, chosenSpecial restored on exit. node --check SYNTAX OK; deployed to Pages.
- 2026-06-19 — Shipped STAGE ATMOSPHERIC PARALLAX (first slice, 2/4 stages): generated assets/snow_layer.png (960KB, 960×540 transparent PNG, ~60–90 uniformly distributed snowflake particles, pure-white to pale icy-blue #e6f0ff, seamless vertical tile) and assets/clouds_layer.png (905KB, 960×540 transparent PNG, 3–5 elongated horizontal amber-pink wispy cloud bands concentrated in the upper third, fully transparent lower two-thirds, seamless horizontal tile) via codex-image-gen Route A (gpt-image-2, free). Registered both as snowLayer / cloudsLayer in ASSET_SRCS (ASSET_TOTAL auto-increments). Added module-scope scroll state: snowScrollY / cloudScrollX with SNOW_SCROLL_PX_PER_SEC=25 / CLOUD_SCROLL_PX_PER_SEC=15. Offsets advanced at the top of update(dt) BEFORE the PAUSED early-return so the background stays alive behind the pause overlay; modulo-wrapped per VH/VW for bounded state; not serialized to online snapshots. drawBackground() extended: after the bg.jpg draw, stageId==="tundra" draws two snowLayer copies stacked vertically (y=snowScrollY−VH, y=snowScrollY) for seamless downward scroll; stageId==="dusk" draws two cloudsLayer copies side-by-side (x=cloudScrollX−VW, x=cloudScrollX) for seamless rightward scroll; both branches guarded by imgReady() so a still-loading or failed asset is silently skipped without throwing. Attract mode (AI-vs-AI on title) inherits parallax automatically — no bypass path. Codex adversarial review: all 10 checks passed — no TDZ, modulo wraps large dt correctly, drawImage calls reuse decoded Image objects (no leak), scroll state not in snapshots, stage branching correct for all 4 stage IDs, imgReady guards naturalWidth=0 correctly, 2-tile seam covers [0,VW] with no gap, attract mode unaffected, pause choice documented in-code, 0.83 px/frame at 30fps smooth. node --check SYNTAX OK. Both PNG magic bytes verified (8950 4e47). Golden Hour and Aurora stages intentionally skipped this slice — next slice: Aurora ribbon shimmer + Golden Hour sun-shaft drift.
- 2026-06-19 — Shipped VS SPLASH match-start screen: new GS.VS_SPLASH state for 1.6s at every match start (NOT between rounds — only the match-start path through startMatch → enterVsSplash → ... → startCountdown). Generated 3 transparent-PNG assets via codex-image-gen Route A free: portrait_red.png (2.1MB, Crimson Knight 3/4 bust with raised frying pan), portrait_blue.png (2.2MB, Azure Knight mirrored), vs_emblem.png (2.1MB, Nordic-rune-styled VS sigil in molten-amber metal). Animation: portraits slide in from left/right via cubic-ease over 0.35s, VS sigil pops in scale 0.4→1.0 at 0.15s, holds, fades over last 0.25s. New procedural sfxHorn() WebAudio brass blast (~0.5s, 196Hz fundamental + harmonics + transient noise) plays ONCE at enterVsSplash. Online host snapshot serializes vsSplashTimer so guest renders synced splash; input/physics suspended during state. ATTRACT MODE inherits splash via enterVsSplash() — no bypass path. canPauseHere blocks pause during VS_SPLASH (GS.VS_SPLASH not in PLAYING/COUNTDOWN allowed set); noHud extended. Codex review: No critical issues found. node --check clean. Commit 605ab80. Pages: deploying.
- 2026-06-19 — Shipped STAGE PARALLAX SLICE 2 (completes the system, 4/4 stages): generated assets/aurora_ribbon.png (1.1MB) + assets/sun_rays.png (1.0MB) via codex-image-gen Route A free. Aurora Night gets soft horizontal aurora bands scrolling right @ 12 px/s with gentle 0.85+0.15·sin(performance.now()/1400) alpha shimmer; Golden Hour gets diagonal golden sun shafts + dust motes scrolling right @ 8 px/s (slowest — sunlight drifts barely). Wired in drawBackground() extending the slice 1 if/else chain at line 4499; added auroraScrollX / sunRayScrollX scroll state + constants AURORA_SCROLL_PX_PER_SEC=12, SUN_RAY_SCROLL_PX_PER_SEC=8 colocated with slice 1 state at line 2712. imgReady guards on both new sprites so a still-loading sprite never throws; ctx.save/restore around the aurora shimmer alpha pulse so no globalAlpha state leak. Online snapshot untouched (scroll offsets are local-only visual state). Codex adversarial review caught one critical: online guest path never calls update() so scroll offsets would freeze — fixed by adding parallax tick to the guest branch in frame() mirroring the host path, same 4 vars, same modulo math. All other checks (TDZ, stage.id strings, serialization, math correctness, imgReady guards) passed. node --check clean. Both PNG magic bytes verified (8950 4e47). Pages build: completed/success.
- 2026-06-19 — Shipped MATCH RESULT SPLASH (bookends iter 32 VS SPLASH): on every match end, a 2.0s winner portrait reveal plays BEFORE the existing restart UI. Reuses iter 32 portrait_red.png + portrait_blue.png assets (no new asset gen needed). New matchResultRevealTimer + MATCH_RESULT_REVEAL_DURATION=2.0 ticking in update() during GS.MATCH_OVER; new enterMatchOver() helper centralizes the transition + horn blast (reuses iter 32 sfxHorn()); all previous `gameState = GS.MATCH_OVER` sites now route through enterMatchOver() (attract path kept as ROUND_OVER→setTimeout, not MATCH_OVER). drawMatchOver() refactored into two phases: phase A (matchResultRevealTimer > 0) calls drawMatchResultReveal() which slides the winner portrait in from opposite side via cubic ease over 0.5s, displays "WINNER" / knight name / round score on the opposing side in amber + ink stroke + team color, fades over last 0.25s; phase B is the existing banner + restart button (byte-identical). During reveal: matchBtn = null so clicks do nothing, R/Enter restart inputs gated by `if (matchResultRevealTimer > 0) return`. ATTRACT mode: setTimeout grace bumped from 1500ms to Math.ceil(MATCH_RESULT_REVEAL_DURATION*1000)+100 = 2100ms so demos always show full reveal before chaining. Online: host snapshot serializes matchResultRevealTimer; guest applies + plays its own sfxHorn() once on the MATCH_OVER transition detect. Draw scenario (time-up tie): matchWinner === null path early-returns from drawMatchResultReveal so no portrait/text renders for ties — reveal phase still ticks down visually as dimmed scene, then falls through to existing banner. Codex review: 1 critical found (attract 1500ms < 2000ms) — fixed. node --check clean. Commit 19a2100. Pages: deploying.
- 2026-06-19 — Shipped ROUND CALLOUTS + FIGHT! emblem (per-round entrance polish): new currentRoundLabel() derives "ROUND N" / "FINAL ROUND" from p1.rounds + p2.rounds (best-of-3 with 1-1 tie = FINAL ROUND). drawCountdown() now slides "ROUND X" in from the left at countdown index 0 (over 0.4s with cubic ease), holds through index 1, fades out over the first 0.25s of index 2. New generated asset assets/fight_emblem.png (codex-image-gen Route A free, 2104316 bytes, 1731×909 transparent PNG, molten-amber Nordic iron letterforms with ember sparks matching iter 32's vs_emblem aesthetic) replaces the procedural "FIGHT!" text on the climax beat; procedural fallback retained for asset-loading window. FIGHT! climax punched up with addBigShake(10) + spawnSparks(20) burst at the medallion center. ATTRACT MODE inherits new callouts automatically. Online sync untouched (countdownIndex/countdownTimer already serialize; round labels derived from p1.rounds/p2.rounds which already sync). Codex review: self-reviewed (12 critical checks passed — TDZ safe via function hoisting, alpha clamped 0-1, save/restore balanced, imgReady guard present, spawnSparks/addBigShake signatures matched, shake fires once per step transition, stage name at y=350 vs callout at y=110 no overlap, PAL constants skyAmber/ink/bone all defined). node --check SYNTAX OK. PNG magic bytes 89504E47 verified. Commit c2af6d1. Pages: deploying.
- 2026-06-19 — Shipped K.O.! EMBLEM (completes the emblem family: VS / FIGHT! / K.O.!): generated assets/ko_emblem.png via codex-image-gen Route A free (Nordic molten-amber iron letterforms + ember sparks matching iter 32 vs_emblem and iter 34 fight_emblem), 2.1MB, 800x420. drawKoBanner() swaps the procedural "K.O.!" stroked text for the emblem sprite (480x252) with the original text retained as imgReady fallback — pop-scale animation, outAlpha fade, amber glow plates behind, subtitle "AZURE/CRIMSON LANDS THE BLOW" all preserved byte-identical. Added small impact punch: spawnSparks(k.x, k.y-40, 28, amber) burst from the faller's position at startKoSlowmo() entry + new koFlashTimer = 0.18s brief warm screen flash overlay that fades over its lifetime. koFlashTimer ticked BEFORE the PAUSED early-return so it always fades regardless of state change mid-flash (Codex critical #2 caught and fixed: original placement was inside PLAYING-only branch). spawnSparks arg order fixed (Codex critical #1: initial call had x/y/n/color wrong). Online host snapshot untouched (koFlashTimer is local visual state); existing koSlowmoTimer + koFaller continue to sync. clearKoSlowmo() extended to also zero koFlashTimer. Codex review round 1: 2 criticals (spawnSparks arg order, koFlashTimer phase coverage) — both fixed. Round 2: no critical issues. node --check clean. Commit 71461fe. Pages: deploying.
- 2026-06-19 — Shipped MENU PORTRAIT OVERHAUL: portrait_red (Crimson Knight) + portrait_blue (Azure Knight) — the iter 32 assets — now flank the title text on the MENU screen at 300x380 each, leftX=-20 / rightX=680, portraitY=140 with subtle idle bob (Math.sin(performance.now()/1000 * 0.6) * 4 px for left, *0.55 + 1.2 phase for right so they don't move in lockstep). Drawn BEFORE the title crest/text/mode-slabs (existing pipeline unchanged) at globalAlpha 0.88 so they sit behind the focal headline. imgReady guards on both. NO new asset generation; pure reuse of iter 32 portraits. The 30s pre-ATTRACT idle window now shows the two knights staring at each other across the title — first impression elevated for visitors who land but don't immediately press a key. drawTitle() only called from MENU state dispatch, so no leak into VS SPLASH (which uses its own slide-in animation) or WINNER reveal. Codex review: No critical issues found — TDZ safe, ctx.save/restore balanced, imgReady guards confirmed, only MENU dispatch call site, Math.sin well-behaved, ATTRACT idle timer unaffected. node --check clean. Commit 0ddb8cb. Pages: deploying.
- 2026-06-19 — Shipped SELECT PORTRAIT OVERHAUL: top-left/right cropped avatar busts (130x165, source-rect 0,0,naturalWidth,naturalHeight*0.55 to crop top 55%) using iter 32 portrait_red.png + portrait_blue.png — extends iter 36's MENU treatment to the second-most-visited screen. Avatars sit in corners (leftX=14 / rightX=816, avY=14) so they DON'T collide with the centered "CHOOSE YOUR SPECIAL" title (x≈170-790) or the 6-card grid below (y=158-398). CRIMSON / AZURE team labels in 900 22px stroked text below each avatar (y=207); when sel.p1.locked or sel.p2.locked is true, an amber halo ellipse (rgba 216,164,90,0.30) renders behind the avatar AND a "LOCKED" amber stamp (PAL.skyAmber) appears below the team name. Halo branches each wrapped in ctx.save/restore; team-label+stamp block in outer ctx.save/restore — no fillStyle/strokeStyle/font/textAlign/lineWidth leak into title or cards. imgReady guards on both portraits. NO new asset generation. NO state mutation (read-only access to existing sel.p1.locked / sel.p2.locked). ATTRACT and TRAINING modes bypass drawSelect() entirely so avatars don't appear there. Online: SELECT runs per-peer locally, avatars are pure visual decoration, no sync needed. Codex review: No critical issues found — TDZ safe, save/restore balanced, imgReady confirmed, halo painted before portrait via draw order, naturalHeight*0.55 is runtime safe (actual assets 1151x1367 not 640x760). node --check clean. Commit 9b73342. Pages: deploying.
- 2026-06-19 — Shipped PAN TRAIL EFFECT (per-swing motion blur): per-knight ring buffer (k.panTrail, bounded to TRAIL_MAX_SAMPLES=7) of {x, y, t} samples capturing the panTip() position every update tick. Sample accumulation gated on |swingVel| > TRAIL_MIN_SWING_VEL=4.5 rad/s so the trail only appears during fast active swings (idle pan = no trail). Samples age in real time using dt (so K.O. slow-mo correctly slows the trail too); samples older than TRAIL_LIFETIME=0.18s are dropped from the head. New drawPanTrail(k) renders connected line segments with tapering width (2-8px) and alpha (0-0.55) along the trail — newer = brighter+wider, older = dimmer+thinner. Color: PAL.skyAmber for visual consistency with the iter 32/34/35 emblem family. drawPanTrail called BEFORE drawKnight in the render order so the streak sits behind the sprite. resetKnightPositions clears the trail to prevent stale tails at round start. Online: trail is purely local visual state, NOT serialized in host snapshot; the guest's swingVel from snapshot still drives the threshold so trails appear naturally on the guest too. ATTRACT MODE AI-vs-AI demos inherit trails automatically. Codex review: No critical issues found — TDZ safe, makeKnight initializes panTrail=[] on both knights, applyKnight does not clobber it, trail bounded by TRAIL_MAX_SAMPLES, resetKnightPositions clears it, ctx.save/restore balanced, drawPanTrail before drawKnight confirmed, pause freezes accumulation, attract mode drives both knights through updateKnight, combat logic untouched. node --check SYNTAX OK. Commit 6c3b325. Pages: deploying.
- 2026-06-19 — EMERGENCY FIX (user-caught): all 9 generated PNG assets from iter 30/31/32/34/35 (parallax overlays + portraits + VS/FIGHT!/K.O. emblems) were RGB-mode opaque with the transparency-checker pattern (low-saturation gray ~RGB(236,236,236)) BAKED INTO THE PIXELS — gpt-image-2's "transparent background" output is actually opaque. On the live site this rendered as visible gray-checker RECTANGLES around the portrait knights on the MENU title (prominently caught by the user) and would have similarly fouled every emblem and parallax overlay. Orchestrator's policy-3 headless verification at 320x180 viewport was too small to see the subtle checker pattern, producing repeated false-positive verifications across 6 iterations. Fix: in-place chroma-key all 9 PNGs to true RGBA — saturation-based (sat<0.03 AND brightness>180 → alpha=0) for 8 of 9 assets; snow_layer uses exact-color tolerance (±8 around sampled corner colors) to preserve pure-white snow flakes. Dark iron edges of emblems and armor (brightness<180) survive the cut. Killed pixel counts: portrait_red 628k, portrait_blue 667k, vs 897k, fight 944k, ko 912k, snow 1.56M, clouds 1.4M, aurora 1.03M, sun_rays 727k. No code changes; pure asset fix. Lesson burned in: future policy-3 verification on visual changes MUST include a zoomed eyeball at full resolution, not just errors==[] at small viewport.
- 2026-06-19 — Iter 39 SPECIAL CUT-IN + SELECT bug fixes (user-caught + user-requested): (A) Fixed iter 38 SELECT bug — avatars were stretched/elongated because source crop used full naturalWidth(1151)×naturalHeight*0.55(~752, aspect 1.53) but dest was 130×165 (aspect 0.787), causing severe horizontal compression. Fixed by aspect-matched source crop sx=300,sy=0,sw=550,sh=700 (aspect 0.787 matching dest, centered horizontally on the bust). CRIMSON/AZURE team labels moved INTO a semi-trans dark band at the TOP of the avatar (y=avY..avY+22) and LOCKED stamp moved INTO a similar band at the BOTTOM (y=avY+avatarH-26..avY+avatarH-4) — both stay within the 130×165 footprint and no longer overflow into the card grid (cards at y>=158). (B) New SPECIAL ACTIVATION CUT-IN: 0.7s cinematic on every special activation (mega/counter/dash/anchor/quake/whirl). Composition: dim veil + white-amber radial flash bursting from center (peaks at 0.18s, fades quickly) + manga-style diagonal speed-lines in team color streaming from the side opposite the portrait + bust portrait sliding in via cubic ease from the activating knight's side + MASSIVE 900 84px stroked special name in team color + 900 36px "CRIMSON KNIGHT"/"AZURE KNIGHT" subtitle, horn blast layered over existing per-special SFX. World physics keeps ticking during the cut-in so the special's actual effect (windup/dash/etc.) stays synchronized with the visual overlay. drawSpecialCutin wired into render() above attract overlay, below mute pip. buildSnapshot/applySnapshot serialize cutinTimer + cutinSide + cutinSpecial + cutinSpecialName for online sync (guest plays its own horn on the fresh-cut-in transition detect). Bigger 30-particle spark burst at the activation site (team color for counter/dash/anchor/mega, amber for quake/whirl). canPauseHere blocks pause during cutinTimer > 0. ATTRACT MODE AI-vs-AI demo inherits cut-ins automatically. Subagent's initial diff missed the render() call site and snapshot serialization; orchestrator completed both wirings inline before commit. node --check clean. Live-verified: SELECT screen at full resolution confirms avatars no longer stretched and team labels sit in the in-frame band; cut-in itself activates with SP-full + F press but capturing the 0.7s window in agent-browser is timing-precision-limited (verified via code paths + post-cut-in errors==[]).
- 2026-06-19 — Shipped DAMAGE NUMBERS: every balance-damaging hit now spawns a "-N" floater above the impact point alongside the existing onomatopoeia ("POW!", "THWACK!", etc.), giving instant numeric feedback for player skill expression. Color-graded: amber (PAL.skyAmber) for normal (<12 dmg), orange (#ff9b3a) for heavy (>=12), crimson (#ff6a3a) for mega or >=25. Scale 0.85-1.0 (smaller than onomatopoeia 1.0-2.4). Offset cy-40 keeps them above the impact word. Counter-punish branch: shows fixed -TUNE.counterBal value (26). Whirl multi-hit: per-hit -TUNE.whirlHitBal floater (small +5, fires ~5-7 times per spin). Dash body-shove: -TUNE.dashBal (rounded, guard-reduced value shown). Quake correctly shows no number (no balance damage by design — pure control move). All routed through the existing spawnFloater wrapper which broadcasts as a "float" FX event for online guest replay (no new snapshot field). Codex review: No critical issues found. node --check clean. Live-verified: title renders, errors==[].
- 2026-06-19 — Shipped PER-STAGE BGM pitch offset: each of the 4 stages now has a musically distinct bell pitch (drone unchanged, stays grounded). bgm IIFE gains a module-scope bgmStageDetune (cents) + a public setStage(stageId) method that maps "tundra"→+700 (perfect fifth up, brighter/icy), "goldenhour"→-200 (whole tone down, warmer), "aurora"→+1200 (octave up, ethereal), "dusk"/default→0 (no change). Applied as o.detune.value at oscillator-create time inside bell() so currently-decaying notes retain their original pitch and the change is audible from the very next scheduled note. setStage called from startMatch (offline modes) + applySnapshot (online guest, mirrors host via the existing s.stage field — no new snapshot field needed). Accent shimmers (scheduleAccent) also detune through the same bell() helper. Codex review: No critical issues found. node --check clean. Live-verified: title renders, errors==[].
- 2026-06-19 — Shipped COMBO COUNTER: per-knight comboCount + comboTimer + comboBig fields added to makeKnight; COMBO_WINDOW=1.4s + COMBO_MIN_TO_DISPLAY=2 constants. applyHit increments comboCount on every clean main-path hit (not counter-punish — attacker rejected); whirl multi-hit and dash body-shove also increment. update() decrements comboTimer per dt; on expire, comboCount zeros. resetKnightPositions clears comboCount/comboTimer/comboBig at round start so stale chains don't bleed. New drawCombos() renders "xN HITS!" callout above the attacker's HUD strip (x=140 left / x=VW-140 right, y=70) with scale 1.0 + 0.15*(n-2) capped at 1.8, fadeIn 0.15s after each hit, fadeOut over last 0.30s; color amber (PAL.skyAmber) <4, orange (#ff9b3a) >=4, crimson (#ff5030) >=6. drawCombos called from render() BEFORE drawKoBanner so the chain text sits below the K.O.! emblem when both fire same frame. Online: comboCount + comboTimer added to snapKnight/applyKnight with typeof-number guards. ATTRACT MODE inherits combos. Codex review: No critical issues found. node --check clean. Live-verified: title renders, errors==[].
- 2026-06-19 — Shipped STAGE AMBIENT PARTICLES (4/4 stages): each stage gets its own particle ecology layered between the parallax bg and the log/knights. AMBIENT_PROFILES map (per-stage intervalMs, maxAlive, spawn factory): Tundra → snow flurries (interval 80ms, cap 220, vx -8..-30 leftward wind, vy +25..+60 falling, color rgba(232,240,255,0.85), maxLife 4-6s); Aurora → fireflies (interval 220ms, cap 80, random walk with vx/vy clamped ±12/±8, color rgba(220,255,180,0.85), pulsing alpha 0.5+0.5*sin(life*4), maxLife 5-8s, spawned in upper-mid zone above log); Goldenhour → golden dust motes (interval 150ms, cap 140, vx +4..+12 matching sun-ray scroll, vy -10..-25 upward, color rgba(244,214,168,0.7), maxLife 5.5-7.5s, mid-frame zone); Dusk → ember sparks (interval 380ms, cap 50, vx ±10, vy -25..-55 rising, color rgba(255,180,90,0.9), maxLife 3-4.5s, ground-level). updateEffects accumulates emit timer in ms, spawns until cap, ticks all + culls expired. lastAmbientStageIdx tracker zeros particles + emit timer on stage change so no bleed; ambient persists within a match across rounds. drawAmbientParticles renders ALL particles with fade-in 0.3s + hold + fade-out 0.6s + firefly slow pulse, drawn AFTER drawBackground (parallax overlay) but BEFORE drawLog()/drawKnight() so it sits in the mid-depth plane. Online: ambient is purely local visual (not serialized — two peers see slightly different patterns, which is fine for atmosphere). PAUSE freezes ambient (update returns early at PAUSED gate line 4506). ATTRACT MODE AI-vs-AI inherits ambient automatically (gameMode="attract" hits same updateEffects path). Codex review: Did not return (0-byte output after timeout); manual 10-point checklist review found no critical issues. node --check clean. Live-verified: orchestrator confirms post-push.
- 2026-06-19 — Shipped KNIGHT IDLE BREATHING: subtle ±1.5px vertical bob + ±0.025rad pan wobble when a knight is genuinely idle (state !== FALLING, on ground, |swingVel|<1.5, |vx|<8, knockTimer=0, all 6 special timers =0). idleBob(k) returns {dy, dAngle, idle} based on performance.now() driven sin waves with phase offset 0 for p1 and 0.6 for p2 so they breathe asynchronously. drawKnight uses local drawY = k.y + dy and drawSwing = k.swingAngle + dAngle for sprite + pan render; k.y and k.swingAngle are NEVER mutated so hit detection (panTip, bodyBox, applyHit) is byte-identical to before. Online: bob is purely local visual (both peers compute their own from performance.now()). ATTRACT MODE AI knights also breathe. Subagent commit blocked by auto-mode; orchestrator hot-fix committed.
- 2026-06-19 — Iter 45 SPRITE ANIMATION slice 1 (user-requested 「スプライトアニメーション入れたい」, programmatic approach): the codex-image-gen path hung for 1+ hour on 4 sprite gens so pivoted to render-side pose shaping using the existing single sprite per knight + canvas transforms. drawKnight detects combat state and applies a shear + scale to the sprite AFTER the seat-anchor rotation but BEFORE drawImage: HIT REACTION when k.knockTimer > 0 → leanX=-0.18*intensity (body shears backward, recoiling away from attacker), scaleY=1-0.06*intensity (vertical squash), scaleX=1+0.04*intensity (horizontal bulge) where intensity = min(1, knockTimer/0.30); ACTIVE SWING when |k.swingVel| > 4 → leanX=±0.14*intensity*sign(swingVel) (body shears INTO swing direction), scaleX=1+0.05*intensity (horizontal stretch), scaleY=1-0.03*intensity (slight vertical compression) where intensity = min(1, (|swingVel|-4)/6). Both intensity-scaled by underlying physics value so engagement/disengagement are smooth, not snappy. ctx.transform(scaleX, 0, leanX, scaleY, 0, 0) applied centered on the seat (already at origin from translate+rotate). Physics state (k.y, k.swingAngle) NEVER mutated; panTip/bodyBox/applyHit byte-identical to before. FALLING tumble rotation still applies on top. Online sync: pose transform is purely render-side, derived from already-synced knockTimer + swingVel; no new snapshot field. Combined with iter 44 idle breathing, knights now visibly: breathe when idle, torque into swings, rock back when hit, tumble on KO. Next slice (deferred): retry the codex-image-gen path for actual multi-pose sprite assets that layer on top of this state-detection plumbing.
- 2026-06-19 — Shipped MATCH POINT TENSION layer at climactic rounds: new isMatchPoint() helper returns true when either knight has 1 round-win in best-of-3 (the next round could decide the match — covers ROUND 2 when 1-0 and FINAL ROUND when 1-1). When active, three cues fire in concert: (1) drawCountdown adds a pulsing-red MATCH POINT subtext (900 32px, color pulses via sin(performance.now()/120), red-tinted with pulsing alpha) BELOW the iter 34 ROUND callout during countdown indices 0/1 + fading out into 2, sharing the same alpha/slideX as the surrounding callout; (2) drawHUD draws a pulsing red border (rgba 255,64,48 at 0.35*pulse alpha) around the top 76px HUD strip when gameState === GS.PLAYING; (3) bgm IIFE gains bgmExtraDetune (cents) + setMatchPointActive(bool) public method, called from startCountdown and applySnapshot so the bell scheduler picks up an extra +300 cents on top of iter 40's per-stage detune, audibly raising tension during the deciding round. All three derived from p1.rounds + p2.rounds which already sync via snapshot; no new snapshot field. No physics impact, purely visual + audio cosmetic. ATTRACT MODE AI-vs-AI inherits automatically. node --check clean. Live-verified: title renders, errors==[].
- 2026-06-19 — Shipped WINNER CELEBRATION CONFETTI on round wins: in awardRound(), immediately after clearKoSlowmo, two layered spawnSparks bursts fire at the winning knight's chest height — 28 team-colored particles (PAL.redLight for p1, PAL.bluLight for p2) + 18 amber particles slightly above for a confetti-arc feel. Routes through the netted spawnSparks wrapper so the existing 'spark' FX event broadcasts to online guests for synchronized replay (no new snapshot field). Fires up to 3 times per match (best-of-3) and on every attract-demo round end. Tiny celebration moment that adds tangible "you earned this" feedback for taking a round, layering with the existing iter 33 MATCH_RESULT_REVEAL splash at match end and iter 35 K.O. emblem when the round ended on a KO. node --check clean.
- 2026-06-19 — Shipped HUD PORTRAIT ICONS: drawHUD now renders 56x56 circular portrait avatars next to each balance meter for character identity reinforcement during PLAYING. Uses iter 32 portrait_red.png + portrait_blue.png with source-rect crop of top ~32% of native image (head + helm + shoulders), centered horizontally on the knight figure. Circular ctx.arc clip mask + thin amber team-color ring frame (PAL.redLight for p1, PAL.bluLight for p2). P1 icon at x=30+mw+iconSize/2+4, P2 mirrored. Y=18 above the meter strip. Drawn during COUNTDOWN / PLAYING / ROUND_OVER states. imgReady guards on both portraits. No new asset generation. Pure additive visual, no regression on existing balance meter / round timer / training label / SP meter rendering. node --check clean.
- 2026-06-20 — Shipped HUD PORTRAIT REACTIVE STATES: drawHudIcon now takes the knight reference and renders 3 reactive cues layered on the iter 48 base icon: (1) HIT FLASH when knockTimer > 0 → red semi-transparent overlay (rgba 255,60,40 at alpha scaling 0..0.55 with knockTimer/0.30) inside circular clip — visceral feedback for getting hit; (2) COMBO GLOW when comboCount >= 4 and comboTimer > 0 → radial amber halo (rgba 216,164,90 gradient, radius grows with (comboCount-4)*0.12 capped at +3.2 px, alpha pulses 0.35..0.60 via sin(t*6)) BEHIND the icon — streak indicator; (3) LOW LIFE PULSE when balance < 25% TUNE.balanceMax → red border ring (#ff3a20, lineWidth ringW+2, alpha pulses 0.5..0.9 via sin(t*5)) outside the base amber ring — danger warning. All three derived from per-knight state (knockTimer, comboCount, comboTimer, balance) that already sync via host snapshot; no new fields. Pure additive — when none of the 3 states active, icon renders byte-identical to iter 48. node --check clean.
- 2026-06-20 — Shipped REAL SPRITE ATLAS animation (iter 50, user-requested 「スプライトアニメーション入れたい」, supersedes the iter44/45 FAKE programmatic skew/scale). Codex CLI image_gen produced a 2-row×6-pose source sheet (row0 crimson / row1 azure; cols IDLE/WINDUP/STRIKE/HIT-RECOIL/GUARD/KO-FALL) on a pure-green chroma bg. A throwaway Python/PIL+scipy packer (/tmp/atlas_pack.py) (1) chroma-keyed green→RGBA (g>110 AND g-max(r,b)>45) and DESPILLED the AA fringe (clamp g→max(r,b) on kept edge pixels) so no green halo; asserted >20% transparent + 12/12 cell-center body samples stay opaque; (2) sliced the 12 figures via scipy.ndimage connected-component → nearest even-split cell-center assignment (drops neighbor pan strays, keeps a figure's own pan that pokes past the 284px window), masked each crop + clipped each paste to its cell (zero cross-cell bleed); (3) packed into assets/knight_atlas.png as a uniform 6×2 grid, cell 315×350, every figure aligned so its SEAT (hip pivot: x=centroid of bottom-35% opaque px, y=top+0.66·h for poses 0–4; full centroid for KO-FALL) lands on a SHARED cell-relative anchor (anchorFX=0.5, anchorFY=0.66) — so the runtime anchor is constant across all cells. Eyeballed atlas_packed_debug.png (dark checkerboard + cyan seat crosshairs): used poses 0/3/4/5 clean, seats aligned. Integration is render-side only: new ATLAS/POSE constants + ATLAS_SCALE=KNIGHT_DRAW_H/312≈0.509 (matches the proven on-screen envelope); drawKnight picks the frame deterministically from NET-snapshot fields ONLY — FALLING→KO-FALL, knockTimer>0→HIT-RECOIL, GUARD→GUARD, else IDLE — and the near-neutral IDLE frame is the SOLE rotating base (rotate(drawSwing)) so there is exactly ONE source of pan excursion (no windup/strike posed frame as base → no double-pan, per Codex review). HIT/GUARD are not swing-rotated; KO-FALL uses rotate(tumble) only. knockTimer ADDED to snapKnight + applyKnight (typeof-number backward-compat read, matching the whirlTimer pattern) so the hit-recoil frame is deterministic online. If the atlas fails to load, drawKnight falls back to the original clean single-sprite path (knightRed/knightBlue + rotate(drawSwing) at SEAT_FX/FY). HARD INVARIANT held: panTip()/bodyBox() and SEAT_*/PAN_*/SPRITE_*/KNIGHT_SEAT_TO_TOP/BODY_HALF_W are BYTE-IDENTICAL (git diff confirmed zero changes to those definitions). The iter-45 leanX/scaleX/scaleY shear + ctx.transform block was REMOVED entirely. Residue (honest): the idle breathing bob (dy ±1.5px, dAngle ±1.4°) is render-only and NOT snapshot-synced (idleBob gates partly on local swingVel/vx) — cosmetic, pre-existing, never feeds panTip — so online peers may show a slightly different breathing phase by design. ROOT CAUSE of iter-45's "1hr hang" identified = `codex exec` blocked reading stdin; fixed by appending `< /dev/null`. cmux browser can't tick rAF (canvas stayed [0,0,0,0]); resolved with a temp _verify.html injecting a setTimeout-based rAF shim before the game script (deleted, not committed). Verified LIVE via cmux screenshots: IDLE (both knights seated on the log at correct size, no green halo), HIT-RECOIL (head-back + dizzy stars, caught during a CLANG), GUARD (pan raised as shield), and rotating swing (idle frame arcs the pan via drawSwing). KO-FALL confirmed by code-read (FALLING→FALL branch, same tumble logic as before) + packed-atlas eyeball (1fps shim clamp made a full life-bar KO impractical to catch live). Codex review verdict: only flag was a false-positive on invariant-3 induced by my paraphrase (code matches the explicit spec line rotate(drawSwing); bob is render-only) — no real regression; hit geometry preserved. node --check clean (extracted the largest of 3 <script> blocks to avoid the gtag-in-head false failure). Code+atlas commit 0f0b9d7.
- 2026-06-20 — Shipped ONOMATOPOEIA SPRITE STAMPS (iter 51, supersedes the procedural Trebuchet-text "POW!"/"CLANG!"): Codex CLI image_gen generated an 8-word comic starburst stamp sheet (POW/CLANG/SMASH/THWACK/WHAM/BONK/WHACK/DOINK) on a pure-green chroma bg. A throwaway Python/PIL packer (/tmp/lkb51_pack.py) (1) chroma-keyed green→RGBA (g>110 AND g-max(r,b)>45) and DESPILLED the AA fringe (clamp elevated g→max(r,b)+10 on kept edge px) so NO green halo — asserted transparency exists + letters/bursts stay opaque; (2) sliced the 2×4 grid, took a tight alpha bbox per cell; (3) packed into assets/onomatopoeia.png as a uniform 4×2 grid (cellW=425, cellH=403, 12px pad, each stamp centered); eyeballed onomatopoeia_packed_debug.png (dark checkerboard) — all 8 words legible+correctly spelled, no halo. Integration is render-side only in drawFloaters: new STAMP {cols:4,rows:2,cellW:425,cellH:403} + STAMP_MAP (word→[row,col]) consts and a tiny unsigned _strHash helper. For each floater, if STAMP_MAP[f.text] exists AND imgReady(IMG.onomatopoeia) → draw the stamp sprite (sx=col·cellW, sy=row·cellH; drawH=40·f.scale·pop ≈40px at scale 1, mega scale 2.4→big; drawW=drawH·cellW/cellH; imageSmoothingEnabled=true for clean 10× downscale; a small deterministic comic tilt rot=(((hash(f.text)+round(f.x))%11)-5)·0.018 ≈±0.09 rad — NO Math.random); ELSE → the EXISTING stroked-text path. The existing pop (life<0.12→1+(0.12-life)·3) and fade (alpha=1-life/maxLife) are PRESERVED. Stamp choice is keyed on f.text, which is ALREADY netRecorded by the spawnFloater "float" event (x,y,text,color,scale) — so online peers reproduce identical stamps with ZERO new snapshot/net field. FALLS BACK to text for unmapped words (GUARD!, GWAAAN!), damage numbers ("-N"), and combo callouts ("xN HITS!") — so ~half of mega hits (["GWAAAN!","SMASH!"]) show the GWAAAN! text fallback and half the SMASH! stamp, by design. NO spawn/physics/hit-detection/net changes — added only ASSET_SRCS.onomatopoeia + the render swap. Verified LIVE via cmux screenshots (rAF setTimeout shim injected via addinitscript before navigate; tab throttles to ~half-speed so floaters were caught by dense 55–90ms bursts): confirmed BONK! (red starburst, correct spelling, punchy ~40px, comic tilt, no halo, fading — caught at a clash next to a "CHARGE!" text floater) and WHACK! (orange starburst, caught mid-exchange) rendering as stamp sprites at the impact point; GUARD!/CHARGE!/combo "x13 HITS!"/special-name banners confirmed still rendering as text via the fallback. node --check clean (extracted the largest of 3 <script> blocks to avoid the gtag-in-head false failure). Code+asset commit cd68bf2.
- 2026-06-20 — Shipped WIN-PIP EMBLEM SPRITES (iter 53): a throwaway Python/PIL packer (/tmp/pack_winpips.py) (1) chroma-keyed the pre-generated winpips_v1.png (3-cell 1×3 sheet, empty socket / red gem / blue gem on pure-green bg, Nordic engraved gold-iron rim) → RGBA (g>110 AND g-max(r,b)>45) + DESPILLED the AA fringe (clamp elevated g→avg(r,b) + alpha-fade near-green edge pixels) — no green halo; asserted >0 transparent pixels + cell-center samples stay opaque; (2) sliced 1×3 cells, tight alpha bbox per cell; (3) packed into assets/win_pips.png as a uniform 1×3 grid (cellW=496, cellH=496, 4px pad, each pip centered); eyeballed winpips_packed_debug.png (dark checkerboard) — all 3 pips centered, clean, no halo. Integration is render-side only in drawHUD: ASSET_SRCS.winPips = "assets/win_pips.png" → IMG.winPips; WINPIP={cols:3,cellW:496,cellH:496} + pipSize=18px on-screen diameter, pipStep=22 center-to-center (widened from old 18) declared in the pip loop; cell index: 0=empty, 1=red gem (P1 win), 2=blue gem (P2 win) keyed on k.side+k.rounds; imageSmoothingEnabled=true for the downscale, reset to false after; imgReady guard falls back to original arc-circle code if asset not loaded. k.rounds is already in the snapshot so online is unaffected — zero new net/snapshot fields. node --check clean. Verified LIVE via cmux screenshots (rAF addinitscript override, fight PLAYING state, countdown "2" frame and active fight frame): empty-socket pip emblems confirmed rendering in both HUD corners (P1 left, P2 right) — dark engraved circular icons at correct ~18px size, widened spacing, clear of player labels and portrait icons, no plain circles, no green halo. Won pips (red/blue gem) verified via standalone Python harness (_shots/pip_cells_harness.png, all 3 cells rendered at 80px against dark bg) + code-read of the cell-index branch (i < k.rounds → k.side==="p1" ? 1 : 2). Code+asset commit 7264148.
- 2026-06-20 — Shipped SELECT CURSOR BRACKETS (iter 54): a throwaway Python/PIL packer (/tmp/pack_bracket.py) (1) chroma-keyed the pre-generated cursor_bracket_v1.png (1×2 sheet, 1536×1024, ornate L-corner brackets — carved iron + gold filigree + faceted gem at elbow, red cell 0 / blue cell 1, on pure-green bg) → RGBA (g>110 AND g-max(r,b)>45) + DESPILLED the AA fringe (clamp elevated g→max(r,b) on kept edge px) — 0 green-halo pixels asserted; 454117 opaque pixels confirmed; (2) sliced 1×2 cells, tight alpha bbox per cell (cell0: 51..750 × 101..851; cell1: 39..738 × 101..852); (3) packed into assets/cursor_bracket.png as a uniform 1×2 grid (cellW=707, cellH=759, 4px pad, each bracket anchored so its elbow/gem corner sits near the packed cell's TL at the pad offset — elbowFX=0, elbowFY=0); eyeballed cursor_bracket_packed_debug.png (dark checkerboard) — both brackets clean, iron+gold+gem detail intact, no green halo, arms extend right+down from TL. Integration is render-side only in drawCardOutline: ASSET_SRCS.cursorBracket = "assets/cursor_bracket.png" → IMG.cursorBracket; BRACKET={cols:2,cellW:707,cellH:759,elbowFX:0,elbowFY:0} const added after imgReady; drawCardOutline rewritten to draw the bracket at all 4 card corners (corners: TL rot=0, TR rot=+π/2, BR rot=π, BL rot=−π/2; translate to corner → rotate → drawImage(cell·cellW, 0, cellW, cellH, −ex, −ey, drawSize, drawSize) so elbow lands on the corner); cell chosen by color arg (PAL.redLight→0, else→1); bs=46px base; locked→drawSize=bs·1.12 + alpha pulse (0.75+0.25·sin(frameCount·0.2)) for a "locked in" breathe; imgReady guard falls back to original strokeRect; inset-rect math preserved byte-identical (P1 inset=−3 outside card, P2 inset=+3 inside). SELECT-only change — no physics, hit detection, snapshots, or game logic touched; sel.p1/p2.cursor+locked already synced so online unaffected. node --check clean. Verified LIVE via cmux screenshots (rAF addinitscript override, single Enter from title): ornate corner brackets confirmed rendering at all 4 corners of the P1-selected card (red, iron+gold filigree, arms along card edges, no green halo) and P2/CPU card (blue brackets); cursor-move test (D key) confirmed brackets follow to the newly selected card; verified at /tmp/select_v1.png and /tmp/select_moved.png. Code+asset commit 23e8987.
- 2026-06-20 — Shipped TIMER DIGIT FONT (iter 55): Codex CLI image_gen produced a 0-9 carved bone-stone+gold-edge numeral sheet (timer_digits_v1.png, fixed-width 2×5 grid, legibility-verified). A throwaway Python/PIL packer (/tmp/process_timer_digits.py) (1) chroma-keyed pure-green (#00FF00) → transparent (g>110 AND g-max(r,b)>45) + DESPILLED the AA fringe (clamp elevated g→max(r,b) on kept edge px) — 1007553 transparent + 565311 opaque pixels asserted, no green halo; (2) sliced 2×5 cells (307×512 each), tight alpha bbox per digit (0-4: 229–232×382, 5-9: 210–217×372); (3) packed into assets/timer_digits.png as uniform 5-col×2-row grid (cellW=240, cellH=390, 4px pad) with COMMON BASELINE (bottom-aligned within 4px bottom pad so all 10 glyphs share a consistent sitting line); eyeballed timer_digits_packed_debug.png (dark checkerboard, yellow cell borders, digit labels) — all 10 digits correct order, same baseline, no halo. Integration is render-side only: ASSET_SRCS.timerDigits="assets/timer_digits.png" → IMG.timerDigits (LOADING pip auto-increments); TDIG={cols:5,rows:2,cellW:240,cellH:390} const added after BRACKET; drawRoundTimer numeral block wrapped in imgReady(IMG.timerDigits) sprite path (dh=34px, dw=21px, gap=3px, pair centered on cx, dy=(by+h/2)−dh/2+2; imageSmoothingEnabled=true for the ~10× downscale; digit D: row=Math.floor(D/5), col=D%5; urgent adds a soft red glow rgba(255,60,40,0.16/0.30 blink) behind the numeral block since stone digits keep a single neutral palette while the existing red halo around the slab handles the primary danger cue) ELSE existing Trebuchet strokeText/fillText numeral code as fallback. Slab sprite, TIME caption, urgent red halo, roundTimer logic, snapshots untouched. node --check clean. Verified LIVE in PLAYING state via cmux browser (rAF addinitscript setTimeout shim, Enter→Space→wait ~13s): "99" and "93" confirmed as carved-stone digit sprites centered on the plaque, correct size (~34px), no green halo, TIME caption intact. Commit f14c50a.
- 2026-06-20 — Shipped sprite-based IMPACT BURST FX (iter 52, replaces the all-procedural fillRect spark-square hit feel with a comic radial flash): Codex CLI image_gen had produced a 4-kind radial burst sheet (2×2: [0,0] LIGHT spark-flash white/gold, [0,1] HEAVY star-burst white+orange, [1,0] MEGA shockwave white/red-orange, [1,1] GUARD clash steel-blue/white) on a pure-green chroma bg. A throwaway Python/PIL packer (/tmp/pack_burst.py) (1) chroma-keyed green→RGBA (g>110 AND g-max(r,b)>45) and DESPILLED the AA fringe (clamp elevated g→max(r,b) + alpha-fade faint ray edges by remaining spill) so NO green halo — asserted transparency exists + burst pixels stay opaque; (2) sliced the 2×2 grid, tight alpha bbox per cell; (3) packed into assets/impact_burst.png as a uniform 2×2 grid (cellW=641, cellH=607, 12px pad, each burst centered); eyeballed impact_burst_packed_debug.png (dark checkerboard) — 4 bursts centered, no halo. Integration is render-side + one spawn line: new IMPACT {cols:2,rows:2,cellW:641,cellH:607} + IMPACT_CELL [[0,0],[0,1],[1,0],[1,1]] (kind→[row,col]) consts; a new impacts[] array next to particles[]/floaters[]; _spawnImpact(x,y,kind,scale) pushes {x,y,kind,scale (default 1.3 mega / 0.95 heavy / 0.8 guard / 0.7 light), life, maxLife (0.5 mega else 0.34), rot=((round(x)%7)-3)·0.12 — deterministic from x, NO Math.random}; public spawnImpact = _spawnImpact + netRecord({k:"impact",...}) mirroring the spark/floater net-FX pattern; playEvent gains case "impact" → _spawnImpact so online guests replay identically with ZERO new snapshot field (scale undefined at the call site → JSON drops it → guest defaults from kind identically; rot recomputed from x on both sides). updateEffects advances impacts (life+=dt, cull at maxLife) mirroring the particles loop; impacts.length=0 added to ALL 3 effect-reset sites (enterVsSplash, startCountdown, the in-place match reset). drawImpacts(): per impact t=life/maxLife, alpha=max(0,1-t²) (quick fade), pop=0.55+0.55·min(1,life/0.07) (pop-in), sizeScale=scale·pop, dh=110·sizeScale base, dw=dh·cellW/cellH; ctx.save → globalCompositeOperation="lighter" (additive glow) + imageSmoothingEnabled=true + per-impact globalAlpha/translate/rotate(rot)/drawImage(source cell)/un-rotate/un-translate → ctx.restore (restore isolates the composite op so the onomatopoeia STAMP that draws immediately after is NOT additive — verified the stamp still renders opaque). Guarded on imgReady(IMG.impactBurst): if the asset is missing drawImpacts is a no-op and the existing sparks still play → no regression. Called in render() AFTER drawParticles but BEFORE drawFloaters, so the burst sits in front of the knights at the hit point and the onomatopoeia stamp layers ON TOP. Spawned in applyHit with ONE added line — spawnImpact(cx,cy, mega?2:(guarding?3:(heavy?1:0))) at the existing tip.x/tip.y using the existing mega/guarding/heavy locals; the existing spawnSparks/spawnFloater/damage logic is untouched. HARD INVARIANT: physics, hit detection, and snapshots are byte-identical (only additive overlay + one spawn call added). Verified LIVE via cmux screenshots (TRAINING mode vs passive dummy, rAF setTimeout shim injected via addinitscript before navigate; tab throttles to ~1fps so bursts were caught by single-swing→shot cycles): confirmed LIGHT/HEAVY normal-hit bursts (kind 0/1) — a white/gold radial flash with rays at the exact contact point between the knights, additive glow, no green halo, UNDER the onomatopoeia stamp (zoom-cropped 3 separate hit frames to confirm the radial shape, _shots/impact_live_zoom.png). MEGA (kind 2) and GUARD (kind 3) plus all-4 pop/mid/fade phases verified via a throwaway standalone harness (_shots/impact_burst_render_verify.png — deleted the .html, kept the shot) that runs the EXACT drawImpacts code against the real packed asset over a dark bg — all 4 kinds render correct color families, additive glow, no green halo, pop+fade; the in-game MEGA contact frame itself is obscured by the pre-existing mega cinematic splash overlay so it was confirmed via the harness, not live. Base 110px reads proportionate against the knights — not tuned further. node --check clean (extracted the largest of 3 <script> blocks). Honest caveats: live in-game capture was limited to kind 0/1 (the throttled ~1fps env makes catching a 0.34s burst a coin-flip; kind 2/3 confirmed via the harness + code-read of the kind→cell mapping); the MEGA cell's upper ray tips read as a few separated sparks in the packed sheet (genuine burst rays, fine in additive blend, not debris). Code+asset commit 56aa1cc.
- 2026-06-20 — Shipped GLOSSY GAUGE FILLS (iter 56, replaces the last flat solid-color element in the combat HUD — the proportional life/SP bar FILLS — with generated glossy pixel-art textures; the gauge FRAME was already a stone sprite): Codex CLI image_gen had produced a 4-cell glossy bar-fill sheet (gauge_fill_v1.png, 2×2 on pure-green: [0,0] CRIMSON life, [0,1] AZURE life, [1,0] AMBER sp-charge, [1,1] GOLD sp-ready — each a horizontal bar with a specular highlight in the upper third, saturated body, darker lower edge). A throwaway Python/PIL packer (/tmp/lkb56_pack.py) (1) chroma-keyed pure-green→transparent with a STRICT key tuned so the amber/gold bars (high R, MODERATE G) survive intact: green = g>110 AND g−max(r,b)>45 AND b<150 AND r<150 (verified all 4 cell centers + L/R/T/B edge samples stay opaque; only the thin inter-cell green seams key out — green mask eyeballed at /tmp/gauge_greenmask.png), plus a despill clamp (g→max(r,b) on any kept pixel where green floats above the dominant channel) so no green fringe; (2) auto-trimmed each cell's green-seam border to its pure-bar bbox (per-cell row/col opacity scan), then (3) packed into assets/gauge_fill.png as a uniform 2×2 grid using the MIN trimmed cell size → cellW=862, cellH=381 (cell map: life P1=[0,0], life P2=[0,1], sp charge=[1,0], sp full=[1,1]); asserted ZERO interior transparent holes across a 5×5 interior sample grid per cell; eyeballed gauge_fill_packed_debug.png (dark checkerboard) — 4 solid glossy bars, correct colors, specular highlights intact, no holes, no green halo. Integration is render-side ONLY in drawBalanceMeter: ASSET_SRCS.gaugeFill="assets/gauge_fill.png" → IMG.gaugeFill; const GFILL={cols:2,rows:2,cellW:862,cellH:381}; new helper drawGaugeFill(cellRow,cellCol,bx,by,bw,bh,fillW,sideP1) that REVEALS the fill by SOURCE-CROP (not stretch): fracShown=clamp(fillW/bw,0,1), srcW=max(1,round(cellW·fracShown)); P1 crops from the cell's LEFT edge (sX=csx) → dest [bx,by,fillW,bh]; P2 crops from the cell's RIGHT edge (sX=csx+(cellW−srcW)) → dest [bx+(bw−fillW),by,fillW,bh] — so the texture aligns to the visible portion and the highlight is never squashed; guard fillW<=0 → no-op; ctx.save + imageSmoothingEnabled=true + drawImage(src cell crop → dest) + restore. LIFE bar: keeps the existing frac=k.balance/balanceMax + fillW=round(w·frac) (UNCHANGED — the balance marker/fulcrum at the same site still reads fillW), then if imgReady(IMG.gaugeFill) → drawGaugeFill(0, side==="p1"?0:1, x,y,w,h, fillW, sideP1); LOW-BALANCE WARNING (frac<0.3) is now a PULSING RED DANGER WASH painted OVER the team-colored texture instead of a flat recolor (the texture hue is fixed): a translucent fillRect rgba(255,42,26, 0.20+0.22·sin(frameCount·0.3) clamped ≥0) over EXACTLY the same dest rect (destX = sideP1 ? x : x+(w−fillW)) so the wash tracks the mirrored fill and the gloss shows through; ELSE (asset not loaded) the ORIGINAL flat-color fill (pal.body / #d44 <0.3 / pal.light <0.55) is kept verbatim as fallback. SP bar: keeps spFrac/spW/full UNCHANGED, then if imgReady → drawGaugeFill(1, full?1:0, x,spY,w,spH, spW, sideP1) (gold cell when SP full, amber while charging); ELSE original flat amber/gold fill as fallback. The mid-range frac<0.55→pal.light lightening is intentionally dropped in the texture path (texture is a fixed team color; only the <0.3 danger wash survives) — by design. Frame sprite, balance marker/fulcrum, SP divider ticks (the for-t loop, drawn AFTER the fill so they stay visible over the texture), and "SP READY!" text are byte-identical. balance/special are already in the snapshot → rendering-only, ZERO new net/snapshot fields, no physics/hit-detection/balance change. node --check clean (extracted the largest of the <script> blocks to avoid the gtag-in-head false failure). Verified LIVE in PLAYING state via cmux browser (rAF addinitscript setTimeout shim, Enter→Space→VS→countdown→PLAYING; WKWebView rejects viewport-set so captured at the native portrait surface and zoom-cropped the gauge band 3×): FULL bars confirmed — P1 CRIMSON glossy red fill (specular highlight along the top third) filling from the LEFT inside the stone frame, P2 AZURE glossy blue filling from the RIGHT (mirrored), balance markers + SP amber fills + frame all intact, no transparent holes, no green halo (_shots/gauge56_full_verify.png). DEPLETING confirmed by spamming Space to land hits on P2 (x6 HITS combo) → P2's azure fill SHRANK proportionally from the RIGHT side toward center, the shortened segment still showing the specular highlight at correct scale (no stretch/squash), correct mirror side (_shots/gauge56_depleting_verify.png); P1's SP amber fill grew from the left in the same run. No browser console errors. The red low-balance danger pulse branch was confirmed by CODE-READ (the wash dest rect matches the texture dest rect; pulse alpha ~0.2–0.42) — couldn't drive the human P1 below 30% balance vs the CPU live, so that one branch is code-verified not screenshot-verified (honest caveat). Code+asset commit afe8237.
- 2026-06-20 — Shipped CHARACTER SELECT (MVP, user-requested): 2 selectable knights driven by a new CHARACTERS table — crimson (クリムゾン / CRIMSON KNIGHT, POWER, signature MEGA SMASH) is the EXACT-1.0 baseline so its feel is byte-identical to the pre-feature tuning; azure (アジュール / AZURE KNIGHT, SPEED, signature DASH) is the differentiated type (faster/floatier/lighter-knockback). Per-knight bounded multipliers (cpuMul-style, all crimson=1.0; azure maxSpeed 1.18 / accel 1.20 / hopV 1.12 / springK 1.06 / swingImpulse 0.90 / knockScale 0.85) are CONSUMED at exactly the self-physics read sites: TUNE.swingImpulse·cmul.swingImpulse (index.html:3574), baseSpringK=TUNE.springK·cmul.springK (3584), TUNE.accel·cmul.accel (3614), TUNE.maxSpeed·cmul.maxSpeed (3629), -TUNE.jumpV·cmul.hopV (3636), and the ATTACKER-LOCAL TUNE.knockScale·attacker.cmul.knockScale·speed in applyHit (4205). Per Codex review, hit geometry (panTip/bodyBox/SEAT_*/PAN_*/sprite anchors), shared thresholds (hitSpeed/forceCap/clashReflect/clashPush/hitCooldown), comeback/edge/clash math (lowBalKnockMul/edgeKnockMul/guardMul/anchor mults), the hop lunge magic-number (k.facing·90), and balLoss/react are kept GLOBAL (azure chips the same life, just shoves less) — grep-confirmed each cmul.X is consumed and no defined-but-unused multiplier. The 6-special SELECT grid was CONVERTED to a 2-character pick: a single commitCharacter(k,cursor) helper (sets charId + cmul + chosenSpecial=signatureSpecial together) is the SOLE lock path, called at ALL migrated sites — keyboard P1 Space (3517) / P2 Enter (3526), CPU auto-pick (4709), host-relayed guest lock (4632), mobile tap (7218), attract (3274-75), training (3084-85); N=SPECIALS.length→CHARACTERS.length everywhere (handleSelectKey, hostSelectFromGuest, CPU walk, mobile hit-test via a shared selectCardLayout(N) so draw rect == tap target). drawSelect rewritten: 2 wide showcase cards (reuse cardFrame sprite) each with bust portrait, JP+EN name, archetype badge (POWER/SPEED), signature special icon+name (drawSpecialIcon gained an optional size arg), and 3 stat bars (SPD/PWR/WT derived from the mul), title "CHOOSE YOUR KNIGHT". Appearance is now f(charId) NOT f(side) across all art sites: drawKnight atlas row = (charOf(k).sprite==="knightRed"?0:1) + fallback sprite IMG[charOf(k).sprite] (5262/5290), HUD portrait icons (5785/5787), VS-splash portraits+names (charOf(p1/p2)), special cut-in portrait+name (cutinKnight), winner-reveal portrait+name (charOf(matchWinner)), plus HUD life-bar name prefix, round-over subtitle, round/match-win banners (all charOf(k).en.split(" ")[0]); team COLOR stays side-based (P1 red bar / P2 blue bar, redLight/bluLight). A render-only MIRROR discriminator (drawMirrorMarker — a small team-colored ▼ over each knight, P1 red / P2 blue) draws only when p1.charId===p2.charId so two identical knights are distinguishable. Online: charId ADDED to snapKnight (4473) + applyKnight (4496, typeof-string-guarded → derives cmul on the guest; physics stays host-authoritative); selp1c/selp2c continue to sync the SELECT screen. makeKnight(side,charId) stores charId+cmul; defaults p1=crimson/p2=azure so every non-SELECT path (training/attract/online-guest-pre-snapshot) has valid state. node --check clean; Codex re-review verdict: NO critical issues (all four invariants — crimson determinism, host-authoritative sync, full lock-path migration, no geometry/threshold contamination — verified; only residual note is unlock leaves charId/cmul resident until relock, which can't start a partial match because READY requires both relock). Verified LIVE via cmux browser screenshots: (1) SELECT shows 2 character cards with portrait/name/archetype/signature/stat bars + cursor navigation (D moves the P1 red bracket between the 2 cards, CPU picks one); (2) NON-DEFAULT swap (2p, P1=azure / P2=crimson) confirmed end-to-end — VS splash shows AZURE KNIGHT on the LEFT (blue) + CRIMSON KNIGHT on the RIGHT (red), HUD reads "P1 AZURE" (red bar) / "CRIMSON P2" (blue bar), and the FIELD sprites are blue-on-left / red-on-right — proving sprite/portrait/name follow charId while team color follows side; (3) MIRROR (both crimson) confirmed — identical CRIMSON portraits+names on both VS sides and a red ▼ over P1 + blue ▼ over P2 on the log. Honest caveats: the attacker-local knockScale term was UNAMBIGUOUS (knock = TUNE.knockScale·speed is derived purely from the attacker's swingVel before any target/shared multiplier) so it was multiplied on the attacker, not left global; "byte-identical / tuned feel preserved" holds for CRIMSON specifically (the 1.0 baseline), NOT the p2 default slot — attract/training now demo crimson-vs-azure with azure faster, a deliberate behavioral change from the old identical-knights demo. Code+UI commit 05faf87.
- 2026-06-20 — Shipped THIRD CHARACTER "WARDEN" (TANK, extends the 2-char SELECT MVP to 3): Codex CLI image_gen had produced a 6-pose sprite atlas (warden_atlas_v1.png, 1×6, 362×724 cells, seated iron-gray/bronze/amber TANK knight facing right with a frying pan — poses idle/windup/strike/hit-recoil/guard/KO-fall) + a heroic bust portrait (warden_portrait_v1.png, 1024×1536), both on pure-green (#00FF00). Throwaway Python/PIL packers: (1) /tmp/warden_pack.py chroma-keyed green→RGBA (g>110 AND g−max(r,b)>45 — tuned so the low-green amber plume/bronze survive) + DESPILLED the AA fringe (clamp elevated g→max(r,b) on kept px) + per-pose LARGEST-CONNECTED-COMPONENT cleanup (drops inter-slice green-seam bleed), sliced the 1×6 row, and re-packed into assets/knight_warden_atlas.png as a 6-col×1-row sheet with cells DIMENSIONALLY IDENTICAL to the existing knight_atlas grid (cellW=315, cellH=350, anchorFX=0.5, anchorFY=0.66) — so the SAME ATLAS consts + ATLAS_SCALE apply with NO new consts. Seat anchoring reproduces the existing packer's algorithm: X = lower-body centroid (bottom 40%), seat Y placed so the figure's seat sits 108/321 above its bottom (matching crimson's y19–339 geometry), KO-fall uses full centroid for both axes; verified warden idle bbox y19–339 (h320) EQUALS crimson idle y19–339 (h320) and confirmed via a 50%-alpha overlay (warden over crimson, shared green seat line) — same seat baseline/scale, no float/sink. Also saved assets/knight_warden.png (trimmed idle, 259×321) for the fallback sprite path. (2) /tmp/warden_portrait.py chroma-keyed+despilled+largest-component the bust then NORMALIZED it onto a 1151×1367 RGBA canvas framed identically to portrait_red/blue (figure top at y=42, centered) — so every FIXED-CROP draw site (SELECT card window 270,0,620×794; HUD head-crop icon; VS; cut-in; winner) lands warden's bust the same as the existing portraits with ZERO per-site code. Eyeballed warden_atlas_packed_debug.png (6 poses on checkerboard with seat crosshairs) + warden_portrait_debug.png + warden_vs_crimson_overlay.png — all 6 poses clean + seat-aligned, portrait clean, amber/bronze preserved, NO green halo on either. Integration: ASSET_SRCS gains knightWardenAtlas/knightWarden/portraitWarden; CHARACTERS gains atlas/atlasRow (crimson knightAtlas row0, azure knightAtlas row1, warden knightWardenAtlas row0) + accent/accentRGB/badgeRGB (crimson red, azure blue, warden AMBER — kills the old red-vs-blue binary in drawSelect name/glow/badge), and a new warden entry {anchor signature, TANK, mul maxSpeed0.85/accel0.85/swingImpulse1.10/springK0.95/hopV0.85/knockScale1.18 — slow/low-hop, heavy swing+knockback, bounded ~±18%}. drawKnight's atlas branch now routes art per-CHARACTER: const ch=charOf(k); atlasImg=IMG[ch.atlas]||IMG.knightAtlas; row=ch.atlasRow||0; imgReady(atlasImg)-guarded with the existing single-sprite (IMG[charOf(k).sprite]=knightWarden) fallback — replacing the lone hardwired `sprite==="knightRed"?0:1` 2-row assumption (the ONLY binary art site found; HUD/VS/cut-in/winner already routed through charOf().portrait from the MVP). selectCardLayout(N) made DYNAMIC (gap 24 for N≥3, cardW=min(300,floor((VW−80−(N−1)gap)/N))=277 for N=3, cardH=cardW, x0 centered, y0 nudged) so 3 cards fit in VW=960 (879px total, x0≈40); drawSelect card content scaled by sc=cardH/300 via a Y(off)=y0+off·sc helper (bust/badge/name/signature/stat-bar offsets) so the shrunk card doesn't clip, and the SPD/PWR/WT stat-bar norm ranges widened to span warden's TANK extremes (0.85–1.18). mobile tap + CPU random pick already read CHARACTERS.length/selectCardLayout(N) so both auto-extend to 3. charId already in snapKnight/applyKnight (applyKnight derives cmul via charById(s.charId).mul) → warden works online with NO snapshot change; mul consumption is the MVP's k.cmul.* read sites (swingImpulse 3574, springK 3584, accel 3614, maxSpeed 3629, hopV 3636, attacker knockScale 4205) UNCHANGED — hit geometry + shared thresholds byte-identical. node --check clean (largest <script> block). Verified LIVE via cmux browser (rAF setTimeout addinitscript shim, file:// load, synthetic Enter→d→d→Space keys): (1) SELECT shows THREE cards (クリムゾン/アジュール/ウォーデン) ALL fully inside the screen, none clipped — warden card renders amber bust+plume, ウォーデン/WARDEN KNIGHT name in amber, TANK badge, ANCHOR signature, SPD(low)/PWR/WT(full) stat bars (_shots zoomed); (2) warden confirmed IN PLAYING across two runs — seated on the log at the SAME seat height/scale as the other knight (zoom-confirmed no float/sink, no green halo), VS splash shows WARDEN KNIGHT with its portrait, and the HUD head-crop icon shows warden's amber-plumed helm; (3) warden movement is the MVP's per-knight self-mul path (k.cmul.maxSpeed/accel) unchanged so it moves slower — code-read, not pixel-measured. Honest caveats: warden was verified in-play as CPU-controlled both runs — the P1 red bracket WAS confirmed on the warden card pre-lock, but the synthetic Space landed P1 on azure both times (a cmux input-timing artifact, structurally impossible to be a code regression since commitCharacter/lock/snapshot paths were untouched); warden's portrait head sits slightly higher in the fixed crop window than portrait_red's but frames cleanly (head+shoulders, no clip). Code+asset commit c322a9a.
- 2026-06-20 — Shipped FOURTH CHARACTER "VESPER" (TECHNIQUE, extends the SELECT roster to 4): Codex CLI image_gen had produced a 6-pose sprite atlas (vesper_atlas_v1.png, 1×6, 362×724 cells, a seated lean SILVER+VIOLET duelist knight facing right with a frying pan — poses idle/windup/strike/hit-recoil/guard/KO-fall) + a heroic bust portrait (vesper_portrait_v1.png, 1024×1536), both on pure-green (#00FF00). Throwaway Python/PIL packers mirrored the warden pipeline: (1) /tmp/pack_vesper.py chroma-keyed green→RGBA (g>110 AND g−max(r,b)>45 — the violet plume/cloth survive because their blue+red dominate; verified 75046 violet px preserved, only 6 residual green px) + DESPILLED the AA fringe (clamp elevated g→max(r,b)+12 on kept px) + per-pose LARGEST-CONNECTED-COMPONENT cleanup (drops inter-slice star-FX/green-seam bleed), sliced the 1×6 row, and re-packed into assets/knight_vesper_atlas.png as a 6-col×1-row sheet DIMENSIONALLY IDENTICAL to the shared knight_atlas grid (cellW=315, cellH=350, anchorFX=0.5, anchorFY=0.66) — so the SAME ATLAS consts apply with NO new consts. Seat anchoring matched the EXISTING atlases by measurement: a GLOBAL scale (idle figure height → warden's REF_HEIGHT=320) plus per-pose bottom-align to REF_BOTTOM=339 and horizontal centroid→REF_CX=157.5; verified vesper idle bbox y18–338 EQUALS warden y19–339 / crimson y19–339 (seat baseline within 1px, no float/sink). Also saved assets/knight_vesper.png (trimmed idle, 162×256) for the fallback sprite path. (2) /tmp/pack_vesper_portrait.py chroma-keyed+despilled+trimmed the bust then NORMALIZED it onto a 1151×1367 canvas framed identically to portrait_red/warden (subject top y=42, scaled to ~1300 tall, FACE centroid landed at the SELECT crop-window center x≈580) — so every FIXED-CROP draw site (SELECT card 270,0,620×794; HUD head-crop icon; VS; cut-in; winner) lands vesper's bust the same as the existing portraits with ZERO per-site code; 2 residual green px. Eyeballed _shots/vesper_atlas_packed_debug.png (6 poses + seat crosshairs/baseline) + _shots/vesper_portrait_debug.png (+ a warden-vs-vesper select-window crop compare) — all 6 poses clean + seat-aligned, portrait clean, silver+violet preserved, NO green halo. Integration: ASSET_SRCS gains knightVesperAtlas/knightVesper/portraitVesper; CHARACTERS gains a vesper entry {atlas knightVesperAtlas row0; accent #b98cff / accentRGB 151,114,209 / badgeRGB 114,86,158 — VIOLET, painting the SELECT name/glow/badge via the existing data-driven ch.accent path with no new color branch; signatureSpecial "counter"; TECHNIQUE; mul maxSpeed1.08/accel1.10/swingImpulse1.05/springK1.15/hopV1.05/knockScale0.95 — nimble mid-weight, crisp HIGH-STIFFNESS counter pendulum, slightly sharper swing, near-standard knockback, bounded ~±15%}. signatureSpecial "counter" REUSES the already-wired COUNTER STANCE special (sfx sfxCounter, iconCounter in ASSET_SRCS + SPECIAL_ICONS, the applyHit counter negate/punish branch, the mind_game/multifaceted achievements) — so ZERO new special code. drawKnight needed NO change: it already routes art per-CHARACTER via const ch=charOf(k); IMG[ch.atlas]||IMG.knightAtlas; row=ch.atlasRow||0 (added when warden landed). selectCardLayout(N=4) is already dynamic → cardW=min(300,floor((960−80−3·24)/4))=202, x0≈40, totalW=880 (4 cards fit inside VW=960). The MAIN RISK this iter — a narrowed 202px card at sc=cardH/300≈0.673 — was REAL: the signature label and stat block use FIXED-px fonts + a fixed cardW−124/110 X-geometry that were NOT scaled by sc, so at N=4 the longest signature EN string ("COUNTER STANCE") collided with the SPD/PWR/WT block. FIX: drawSelect + drawStatBar now scale the archetype badge font/pad/height, JP+EN name fonts, signature icon size + X-gutters + both label fonts, AND the stat-bar block width (110·sc) + X-origin (cardW−124·sc) + label font/gutter/bar-height ALL by sc — so the card scales uniformly and the proven N≤2 proportions hold at any N; sc=1.0 reproduces the N≤2 layout BYTE-IDENTICALLY (verified each expression rounds back to its original 13/27/15/11/110/124/34/8 value). At N=4 the sig-label→stat-block gap is +11px (was a collision); stat labels render at the 8px floor (small but legible). mobile tap + CPU random pick already read CHARACTERS.length/selectCardLayout(N) so both auto-extend to 4; charId already in snapKnight/applyKnight (guest derives cmul via charById) → vesper works ONLINE with NO snapshot change; mul consumption is the unchanged self-physics read sites (geometry + shared thresholds byte-identical). Grep confirmed every roster reference is data-driven (CHARACTERS[i] loop, CHARACTERS.length modular cursor, charOf/charById fallback) — no 3-branch accent switch, no CHARACTERS[0..2] assumption. node --check clean (largest <script> block). Verified LIVE via cmux browser (rAF setTimeout addinitscript shim, localhost:8899, synthetic keys, full-res canvas grabbed via toDataURL since the canvas is never tainted): (1) SELECT shows FOUR cards (クリムゾン/アジュール/ウォーデン/ヴェスパー) ALL fully inside VW=960, none clipped — the vesper card (zoom-confirmed) renders the VIOLET bust, ヴェスパー/VESPER KNIGHT name in violet, TECHNIQUE badge, COUNTER STANCE signature (icon+label NOT overlapping the stat block), and SPD/PWR/WT bars, all readable; (2) vesper confirmed IN PLAYING — seated on the log at the SAME seat height/scale as the other knight (zoom-confirmed no float/sink, no green halo, silver armor + violet cloth/plume, pan), VS splash shows VESPER KNIGHT with its portrait, the HUD reads "VESPER CPU" with the vesper portrait head-crop icon. Roster now 4 (POWER/SPEED/TANK/TECHNIQUE); 4 of 6 specials live (mega/dash/anchor/counter). Honest caveats: vesper was verified in-play as CPU-controlled — the P1 red bracket WAS confirmed on the vesper card pre-lock (zoom of the 4-card row), but a cmux synthetic-input timing artifact (first attempt only 2 of 3 d-presses registered; second attempt locked but the CPU ended up driving vesper) means the in-play vesper was the CPU side, not the human P1 (structurally impossible to be a code regression — commitCharacter/lock/snapshot untouched). The stat labels at the 8px floor are small but legible; the "4 cards comfortably readable" claim holds for the bust/name/badge/signature (clearly legible) and is "small-but-legible" for the 8px stat labels specifically. vesper's nimble/high-stiffness feel is CODE-READ (the unchanged k.cmul.* self-physics path), not pixel-measured. Code+asset commit 1e8276d.

- 2026-06-20 — Shipped ROSTER SELECT UI (iter 50, reworks CHARACTER SELECT from a cramped single-row card grid into a scalable roster): replaced selectCardLayout(N) (square cards in one row; at N=4 cardW had shrunk to ~202 and N>=5 would break / not fit VH=540) with a roster layout = a shared N-character icon STRIP flanked by two large hero PREVIEW panels. New helper rosterIconRects(N) returns the array of {x,y,w,h} emblem rects and is the SINGLE source of truth consumed by BOTH drawSelect and the mobile tap hit-test (drawn emblem == tap target); the strip is clamped to the clear band BETWEEN the hero panels (bandL=HERO_PANEL_X+HERO_PANEL_W+10=334, bandR=VW-that-10=626, ~292px), emblem edge = clamp(44, fit, 96), centered in the band. drawRosterEmblem(ch,r,frameCol) reuses the iter-48 HUD head-crop window (top ~34% of IMG[char.portrait], clipped to the tile) + a thin Nordic frame, imgReady-guarded with a colored-tile+initial fallback. Cursor brackets reuse drawCardOutline on the small rect (P1 outset -3 / P2 inset +3 so a SHARED pick reads both) + P1/P2 tags below (✓ when locked). drawHeroPreview(ch,side,locked,isCpu) draws a 300x360 Nordic slab (left=P1 name-left, right=P2 name-right, accent top rail + amber locked wash/border + LOCKED stamp): 150x192 portrait bust (same 270,0,620,794 top window the old card used), archetype badge, CPU tag in 1p, JP name in accent + EN in amber, signature special via drawSpecialIcon(char.signatureSpecial)+label, the 3 stat bars (SPD/PWR/WT, SAME norm ranges 0.85-1.18 / 0.90-1.10 / 0.85-1.18 as the old card so values stay consistent), and the blurb. PURE render + tap-rect change: handleSelectKey (A/D & arrow cursor +/-1 wrap, Space/Enter lock, S/ArrowDown unlock), commitCharacter(k,cursor), the CPU auto-pick walk, the readyTimer trigger, and the host->guest snapshot mirror (selp1c/selp1l/selp2c/selp2l) are ALL byte-identical — drawSelect is a pure read of sel.* + CHARACTERS, introduces NO new persistent state the snapshot would miss, so no desync. Driven by N=CHARACTERS.length so 5/6 characters drop in structurally (scaling is N-driven, NOT pixel-verified at N=6 — the inter-panel band is the limiter, so higher N would need narrower hero panels; current tuning is for N=4). Reuses existing portraits (portraitRed/Blue/Warden/Vesper) for both strip emblems and previews — NO new art. node --check clean (largest <script> block). Codex (gpt-5.4) review: no critical findings (selection logic intact, mobile tap maps tap->cursor=i with the same one-tap-move+lock / tap-same-locked-unlock / locked-elsewhere-silent-relock semantics, host->guest mirroring holds, no dangling selectCardLayout ref). VERIFIED HUMAN-PICK via cmux browser (rAF setTimeout addinitscript shim, file:// load, synthetic window keydowns, full-res 960x540 frames cropped+upscaled from the viewport screenshot since toDataURL is tainted by file:// images): closing the PRIOR iters CPU-only / never-human-confirmed gap. Followed verify-then-act strictly (one d-press -> wait ~450ms -> screenshot -> CONFIRM the P1 red bracket AND the left hero preview both landed on the target BEFORE the next press/lock): drove P1 to WARDEN (2 presses, each confirmed one step) -> Space locked -> left preview showed WARDEN+LOCKED -> reached PLAYING -> the LEFT (P1) knight that entered play was the iron-gray/amber WARDEN TANK sprite (HUD "P1 WARDEN", VS splash WARDEN KNIGHT) == the bracketed/locked char: MATCH. Repeated for VESPER (3 confirmed presses -> lock -> left preview VESPER/COUNTER STANCE/TECHNIQUE -> PLAYING left knight = silver-armor/violet-plume VESPER, HUD "P1 VESPER", VS VESPER KNIGHT): MATCH. So the locked char == the char that entered PLAY for the HUMAN P1 in both cases — the selectability gap is CLOSED. Diagnosis of the old "bracket on X but locked Y" ghost: it was a TEST-HARNESS artifact, NOT a game bug — dispatching the synthetic KeyboardEvent to BOTH window and document fired the (window-only) keydown listener TWICE per press (one d moved the cursor by 2); dispatching to window ONLY made every press = exactly one step, after which lock and PLAY matched perfectly. Honest caveats: N=6 fit is structural not pixel-verified; at the N=4 strip the emblem is ~65px internal (~28px on the 412px mobile display) — a smaller tap target than the old ~87px cards (acceptable, flagged); verification was 1p (P1 human / CPU P2) per the harness. Code commit 1b2c51f.

- 2026-06-20 — Shipped FIFTH CHARACTER "EMBER" (BERSERKER, extends the SELECT roster to 5): Codex CLI image_gen produced a 6-pose sprite atlas (ember_atlas_v1.png, 1×6, 362×724 cells, a seated BLACKENED-IRON berserker knight facing right with a frying pan, molten-orange lava cracks + fiery orange plume — poses idle/windup/strike/hit-recoil/guard/KO-fall) + a fierce bust portrait (ember_portrait_v1.png, 1024×1536), both on pure-green (#00FF00). A throwaway Python/PIL packer mirrored the warden/vesper pipeline: (1) chroma-keyed green→RGBA (g>110 AND g−max(r,b)>45 — the orange plume/lava survive since red dominates; the blackened armor is dark so safe; 76% green bg removed cleanly, no halo) + DESPILLED the AA fringe (clamp elevated g→max(r,b)) + per-cell LARGEST-CONNECTED-COMPONENT cleanup with a 6px binary dilation before labeling so a thin AA gap can't split the plume off the helm yet a neighbor pose's overhanging pan (windup's raised pan bleeds left into the idle slice) is dropped, sliced the 1×6 row, and re-packed into assets/knight_ember_atlas.png as a 6-col×1-row sheet DIMENSIONALLY IDENTICAL to the shared knight_atlas grid (cellW=315, cellH=350, anchorFX=0.5, anchorFY=0.66) — so the SAME ATLAS consts apply with NO new consts. Seat anchoring matched the existing atlases by measurement: a GLOBAL scale (idle figure height 412 → warden REF height 321, S≈0.777) + per-pose bottom-align of the SHARED seated baseline (source bottom 596 → warden idle bottom 339, so the hip/seat lands at ~230 vs warden 231, within 1px — no float/sink). CRITICAL fit detail (caught in adversarial review): the uniform scale's fit-cap is computed ONLY over the poses drawKnight actually renders — IDLE(0)/HIT(3)/GUARD(4)/FALL(5) — because drawKnight's pose select only ever draws those four; the never-drawn WINDUP(1)/STRIKE(2) cells (whose raised pan is the tallest content) are allowed to clip ~30px at the top rather than being permitted to shrink the whole sheet (an earlier cap over all 6 poses pulled S to 0.70 and packed the idle to only 289px tall, sinking the seat ~10px below warden's line; restricting the cap to rendered poses restored S=0.777 and the idle to warden's y19–339 envelope). Verified packed ember idle bbox y[0..338] (plume reaches ~18px higher than warden's, cosmetic) with the seat at ~230 EQUAL to warden/crimson/vesper. Also saved assets/knight_ember.png (trimmed idle, 211×256) for the fallback sprite path. (2) the portrait packer chroma-keyed+despilled+trimmed the bust then NORMALIZED it onto a 1151×1367 canvas: ember's source has a TALLER plume than warden so a naive content-top align pushed the FACE below the SELECT/HUD crop windows — instead width-filled to warden's content width (≈869px, x-centered) then slid the bust UP by a calibrated 165px so the eyeline lands at ~y300 (warden's), putting ember's face squarely inside the top-34% roster head-crop window exactly like the other portraits (verified by a per-portrait crop-window overlay: warden/crimson/ember all frame helm+face). So every FIXED-CROP draw site (roster emblem head-crop, hero-preview bust, HUD head-crop icon, VS, winner) lands ember's bust the same as the existing portraits with ZERO per-site code. Eyeballed _shots/ember_atlas_packed_debug.png (6 poses + seat crosshairs) + _shots/ember_portrait_debug.png (+ eyeline + crop-window overlay) — all 6 poses clean + seat-aligned, portrait clean, blackened-iron + molten-orange + lava preserved, NO green halo. Integration: ASSET_SRCS gains knightEmberAtlas/knightEmber/portraitEmber; CHARACTERS gains an ember entry {atlas knightEmberAtlas row0; accent #ff7a2a / accentRGB 205,100,35 / badgeRGB 160,76,28 — MOLTEN-ORANGE, painting the SELECT name/glow/badge via the existing data-driven ch.accent path with no new color branch; signatureSpecial "quake"; BERSERKER; blurb 高火力・高速だが隙も大きい。攻めの化身。; mul maxSpeed1.10/accel1.12/swingImpulse1.18/springK0.82/hopV1.0/knockScale1.12 — fast + hard-hitting swing + heavy knockback but LOW springK so the pendulum returns slowly = overcommits/leaves openings = high-risk, bounded ~±18%}. signatureSpecial "quake" REUSES the already-wired EARTHQUAKE special (TUNE.quake* tuning, sfx, iconQuake in ASSET_SRCS + SPECIAL_ICONS, the quakeTimer/quakeActive shove logic, the earth_shaker/multifaceted/deep_roster achievements) — so ZERO new special code. drawKnight needed NO change: it already routes art per-CHARACTER via const ch=charOf(k); IMG[ch.atlas]||IMG.knightAtlas; row=ch.atlasRow||0. The KEY layout check this iter — does rosterIconRects(5) fit 5 emblems + the two flanking hero previews in VW=960×VH=540 — PASSED with no tuning: at N=5 the clamp gives a 47px emblem edge, totalW=291 in the ~292px inter-panel band (band 334..626), the leftmost emblem's outset P1 bracket sits at x≈331.5 vs the left hero panel's right edge 324 = +7.5px clearance (mirror on the right), comparable to N=4's +8px — so the brackets do not clip the hero panels (N=6 would overflow the band by ~14px and is still NOT pixel-verified; the inter-panel band remains the limiter). Grep confirmed every roster reference stays data-driven (CHARACTERS[i] loop, CHARACTERS.length modular cursor, charOf/charById fallback, the only charId comparisons are a typeof guard + a p1==p2 mirror-match check) — no ≤4-branch accent switch, no CHARACTERS[0..3] assumption; ember paints molten-orange purely from its data fields. charId already in the snapshot (guest derives cmul via charById) → ember works ONLINE with NO snapshot change; mul consumption is the unchanged self-physics read sites (geometry + shared thresholds byte-identical). node --check clean (largest <script> block). VERIFIED HUMAN-PICK LIVE via cmux browser (rAF setTimeout addinitscript shim so the throttled split-pane still ticks, localhost:8772, synthetic window-ONLY keydowns, full-res 960×540 frames grabbed via canvas.toDataURL — untainted over http://): SELECT shows FIVE emblems (クリムゾン/アジュール/ウォーデン/ヴェスパー/エンバー) + both hero previews, none clipped/overlapping, ember's emblem reads MOLTEN-ORANGE distinct from warden's amber and vesper's violet; followed verify-then-act (one d-press → wait ~400ms → screenshot → confirm landed → next) to drive P1 from crimson across to EMBER (the left hero preview updated live: AZURE→…→EMBER KNIGHT / BERSERKER badge / EARTHQUAKE signature / molten-orange name, fierce blackened-iron portrait correctly framed) → Space LOCKED (left preview EMBER + LOCKED, READY!) → VS splash EMBER KNIGHT with its portrait → reached PLAYING: HUD reads "P1 EMBER" and the LEFT (P1) knight that entered play IS the ember BERSERKER sprite (zoom-confirmed: blackened iron armor + glowing molten-orange lava cracks + fiery orange plume + spiked shoulder + frying pan, seated on the log at the SAME seat height/scale as the other knight, no float/sink, no green halo) == the locked char: MATCH. Roster now 5 (POWER/SPEED/TANK/TECHNIQUE/BERSERKER); 5 of 6 specials live (mega/dash/anchor/counter/quake). Honest caveats: ember's fast/heavy/overcommitting feel is CODE-READ (the unchanged k.cmul.* self-physics path + low springK), not pixel-measured frame-by-frame; N=5 fit is comfortable but the centering slack is ~0.5px (the brackets clear the panels by 7.5px — tight but positive), and N=6 remains structurally-not-pixel-verified; the never-rendered windup/strike atlas cells clip their raised pan at the top by design (harmless since drawKnight never reads them); a re-pick round had the CPU also randomly draw ember (a clean mirror-match that additionally confirmed the sprite renders identically on both sides). Code+asset commit 1133bfa.

- 2026-06-21 — Shipped MOBILE ZOOM FIX (iter 51): disabled double-tap-to-zoom and pinch-zoom that fired on rapid taps during play (tapping HIT button or character emblems fast triggered browser zoom, breaking the layout). Four belt-and-suspenders layers: (1) viewport meta gains maximum-scale=1.0/user-scalable=no/viewport-fit=cover; (2) html,body get touch-action:manipulation in CSS (disables the double-tap-zoom delay on all screens incl. SELECT/title — the play canvas and .tb touchpad buttons already had touch-action:none and keep it, which is stricter and correct); (3) iOS Safari gesturestart/gesturechange/gestureend events are all preventDefault-d via a global document listener (passive:false) to block pinch-zoom even on Safari builds that historically ignore user-scalable=no; (4) a document touchend guard prevents a second tap within 300ms (the browser double-tap window) via preventDefault — INPUT and TEXTAREA targets are explicitly excluded at the top of the handler so the room-code join field (#joinInput) still focuses and types normally. No game logic, physics, rendering, or audio changed. node --check clean (largest script block). Real-device double-tap cannot be verified in this environment (cmux browser is desktop rAF-shim — cannot synthesize a genuine double-tap gesture); the fix uses the standard, correct, belt-and-suspenders solution for this class of mobile-browser issue. Commit d5a85b0.

- 2026-06-21 — Shipped MOBILE LANDSCAPE BUTTON FIT FIX (iter 52): the on-screen touch buttons (#touchpad .tb — tb-l/tb-r/tb-up/tb-att/tb-sp/tb-gd/tb-pause) were FIXED-PX positioned (e.g. tb-l left:14/bottom:92, tb-att right:14/bottom:100/120x120, tb-gd right:142/bottom:188) and, after the viewport-fit=cover zoom fix (d5a85b0) extended layout under the notch/home-indicator, slid UNDER the notch (landscape left/right inset ~44px) and home-indicator (~21px) on real notched phones in 横持ち — buttons clipped/off-screen — because grep confirmed env( appeared 0 times so nothing compensated. ROOT CAUSE confirmed = safe-area vs cover (raw viewport size alone does NOT reproduce it; a desktop browser at 800x390 fit fine). FIX (CSS-only): (1) wrapped every touchpad button offset in calc(<orig>px + env(safe-area-inset-*, 0px)) — left-side buttons add inset-left, right-side add inset-right, all bottoms add inset-bottom, tb-pause top adds inset-top — so on a notched phone the buttons sit clear of the notch/home-indicator while the 0px fallback leaves non-notch devices byte-identical to before; (2) added @media (orientation: landscape) and (max-height: 430px) that scales both clusters down (tb-l/tb-r 76->64, tb-att 120->100, tb-up 162x60->136x50, tb-sp/tb-gd narrowed) and tightens the vertical stack (tb-up bottom 178->144, tb-gd 188->150, tb-l/tb-r 92->72, tb-att 100->76) so all six buttons stay fully on-screen/tappable clear of safe areas at small landscape heights without colliding with the top HUD or each other. KEPT viewport-fit=cover (NOT dropped): env(safe-area-inset-*) returns non-zero insets ONLY under cover, so the env-calc is the explicit, testable mechanism doing the work — dropping cover would make env a permanent no-op; cover+env is the path actually exercised by verification. VERIFICATION caveat learned: the planned "large env() FALLBACK" test harness does NOT work — Chrome SUPPORTS env() so it returns the real 0px and never uses the fallback (proven empirically); to simulate a notched phone I instead injected test CSS with HARD-CODED insets (calc(14px+44px) etc., 44/44/21) via addStyleTag, leaving index.html untouched (no ship-the-test-values risk). Headless-Chrome measured getBoundingClientRect at 844x390/800x390/740x360/667x375 with simulated 44/44/21 insets: OFFSCREEN=0 at ALL four sizes (every .tb fully inside viewport minus insets); SHIPPED 0px-fallback landscape 844x390 OFFSCREEN=0 and PORTRAIT 390x844 OFFSCREEN=0 (no regression). Read the actual pixels per the frontend-qa skill: _shots/mob_land_bug.png (OLD fixed-px under simulated red notch/home-indicator bars — left arrow + HIT clipped under the bars), _shots/mob_land_fixed_844x390.png + _shots/mob_land_fixed_667x375.png (all 6 buttons inset clear of the simulated safe areas, knights on the log visible/unblocked, no HUD overlap), _shots/mob_land_0fallback.png + _shots/mob_portrait.png (non-notch layout unchanged). node --check clean (largest <script> block; CSS-only change is outside any script). Honest caveats: real notched-phone clipping is INFERRED from the env/cover mechanism + the simulated-inset geometry, not reproduced on real hardware (no device/notch available; the red-bar "bug" shot is an overlay simulation); the buttons are plain positioned divs so getBoundingClientRect IS their painted box (the skill's "box != paint" caveat applies to object-fit images, not these). Commit 7a299b8.

- 2026-06-21 — Shipped SIXTH CHARACTER "GALE" (SKIRMISHER) — FULL ROSTER complete (iter 53). Codex CLI image_gen made a 6-pose atlas + heroic portrait (white-silver armor, CYAN cape/plume); a throwaway Python3/PIL script chroma-keyed green→transparent with a TIGHT key (g>110 AND g-r>45 AND g-b>45) so the cyan cape/plume (high-g AND high-b → g-b≈0 < 45) survives — only true green is removed; despill clamped fringe. Sliced 1×6, height-normalized each pose to the ember/crimson baseline (idle figure fills cell, feet contact at y≈337/350 == ember's 338, horizontal center 157≈cellW/2) and packed to assets/knight_gale_atlas.png (1890×350 = 6 cols × 315×350 cells, SHARED ATLAS grid {cols:6,cellW:315,cellH:350,anchorFX:0.5,anchorFY:0.66} — no new consts) + standalone assets/knight_gale.png (227×256 idle) + assets/portrait_gale.png (1151×1367, head-aligned like others). Cyan-key check: only 2.09% of cyan-ish source pixels keyed (anti-alias fringe); debug previews (_shots/gale_atlas_packed_debug.png + _shots/gale_portrait_debug.png) confirm 6 clean seat-aligned poses, no green halo, cape/plume/whirlwind-arc intact. WIRED: ASSET_SRCS gains knightGaleAtlas/knightGale/portraitGale; CHARACTERS appends gale {id:gale, en:GALE KNIGHT, signatureSpecial:whirl, archetype:SKIRMISHER, accent:#3fd6e8, accentRGB:50,171,186, badgeRGB:40,137,148 (ember-style hand-darkened cyan), mul fast/floaty with HIGH springK 1.10 (snappy pendulum return for rapid multi-hits) + light knockScale 0.90, bounded ~±15% per-knight-SELF dynamics only — hit geometry & shared thresholds BYTE-IDENTICAL}. The whirl WHIRLWIND special (旋風斬り) was already fully implemented (TUNE consts, state fields, case "whirl" multi-hit handler) but UNUSED — gale activates it, so ALL 6 specials are now live (mega/dash/anchor/counter/quake/whirl). drawKnight UNCHANGED (routes via ch.atlas/ch.atlasRow). ROSTER UI N=6 FIT (the flagged risk): old rosterIconRects gap=14/min=44 OVERFLOWED at N=6 (6·44+5·14=334 > 292 band → emblems gored both hero panels by ~11px); FIX = gap=10/min=40 pure formula (6·40+5·10=290 ≤ 292, ~11px clearance each side; N=5→50px emblems/290, N≤2 still hits 96 ideal cap, no N-branch). LIVE-VERIFIED in cmux browser at full 960×540 (canvas toDataURL, rAF shim): SELECT shows all 6 emblems incl gale's cyan emblem, no overlap with each other or the two hero panels, both P1/P2 brackets read on focus (_shots g_strip_zoom). HUMAN-PICK verified-then-act: drove P1 right one press at a time (window-only key dispatch, 1 press=1 move) onto the 6th emblem → left hero preview showed SKIRMISHER/ゲイル/WHIRLWIND/cyan portrait → Space LOCKED on gale ("LOCKED"+READY!) → VS screen GALE KNIGHT (white-silver+cyan) vs VESPER → PLAYING with HUD "P1 GALE" and the cyan-caped gale sprite on the log. LOCKED char == char in PLAY (no mismatch). charId already synced so gale works online with no snapshot change. Roster now 6 (POWER/SPEED/TANK/TECHNIQUE/BERSERKER/SKIRMISHER). node --check clean (largest <script> block). Caveat: cyan survival inferred from key math + debug/in-game pixels (2.09% keyed = AA fringe, not body). Commit 2ff9e45 (code+assets); this log entry is the separate log commit (pushed alongside).

- 2026-06-21 — Shipped AIMED HIGH/LOW ATTACKS (user-requested attack variation, iter 54) — an attack with a held direction now picks an aim sampled ON the attack edge: ↑/jump+swing = HIGH (anti-air: modest upward pop on the foe + reduced horizontal shove), ↓/guard+swing = LOW (grounded opener: extra horizontal shove/destabilization, no pop), plain swing = NORMAL (unchanged). attackAim enum (0 normal/1 high/2 low) on the knight, sampled in updateKnight BEFORE the `guarding` const so one-frame precedence works: LOW sets guarding=false this frame (input.guard && onGround && !lowAttack) so the swing actually fires instead of being eaten by the guard branch; HIGH gates the later hop block with `!highAttack` so W+swing reads as an overhead swing, not an accidental jump-attack. The swingImpulse path stays byte-identical and aim does NOT gate whether a hit lands — it's a pure PAYOFF overlay in applyHit, placed AFTER the COUNTER early-return and AFTER the normal knock/balance/edge scaling: HIGH => knock *= aimHighHorizMul(0.80) + target.vy -= aimHighPop(220) + target.onGround=false (the onGround flip is REQUIRED — vy is only integrated while airborne, so on a grounded foe the pop is invisible without it; 220 is ~1/10 of jumpV 620 so the foe visibly rises but cannot air-KO/fly off), LOW => knock *= aimLowHorizMul(1.25) with no pop (stacks after edge amplification → stronger ring-out pressure than NORMAL, by design HIGHER than HIGH's horizontal pressure). nagare-pan gray zone preserved: attackAim cleared back to 0 when attackFlash hits 0 (the swing window), so the aim never leaks into a later hop/guard hit; also reset in resetKnightPositions (two reset sites). Determinism/online: attackAim derived from already-synced input at swing time + serialized as a plain enum in snapKnight/applyKnight (typeof-number guard like knockTimer — no new float timer) so host/guest agree; HIGH!/LOW! floater FX routed through the netted spawnFloater so guests see it. Controls hint (#hint) + help overlay updated for P1 (W/S+SPACE) and P2 (▲/▼+ENTER) and mobile (HOP+HIT = HIGH, GUARD+HIT = LOW — same logic auto-works via simultaneous touch buttons); attract AI mixes in ~30% aimed swings (15% jump→HIGH, 15% guard→LOW) so the variation is visible hands-off. VERIFIED in cmux browser (training mode, rAF shim, temporary debug hook injected for state read then REMOVED before commit): applyHit payoff with identical setup (adjacent grounded foes, swingVel 6.0) — NORMAL vx+102.0/vy 0/onGround true, LOW vx+127.5/vy 0/onGround true (×1.25 bigger shove), HIGH vx+81.6/vy-220/onGround→false (×0.80 shove + upward pop); ALL three deal the SAME damage (6.9) → confirms aim modifies payoff only, never hit possibility. HIGH trajectory hand-integrated (gravity 1900, dt 1/60): peak rise ~10.9px over ~12 frames vs a normal hop's ~101px → modest, no off-screen fling/air-KO. Deterministic stepP1 (one updateKnight frame per synthetic input): NORMAL(SPACE) aim=0 swing fired onGround true; LOW(S+SPACE) aim=2 swing fired (guard suppressed) onGround true no leak; HIGH(W+SPACE) aim=1 swing fired onGround true (hop suppressed); PLAIN GUARD(S only) aim=0 no swing; PLAIN HOP(W only) aim=0 no swing onGround→false (regression intact). HIGH!/LOW! floaters confirmed rendering above the knight (_shots/lkb_low_fx.png, _shots/lkb_both_fx.png). Geometry untouched: git diff has ZERO panTip/bodyBox/SEAT_/PAN_/swingMax lines. node --check clean (largest <script>). Codex review (gpt-5.4): "No critical issues found" — checked determinism/online sync, geometry untouched, aim not gating hits, hop/guard precedence, pop not breaking ring-out/edge math, no aim leak. Honest caveat: the HIGH/LOW *payoff* numbers and trajectory were observed by directly invoking applyHit + hand-integrating (the throttled training loop + dummy auto-recenter made a clean live-driven pop hard to capture mid-flight); the *input→aim path* (W/S+swing setting attackAim with correct hop/guard suppression) was proven via deterministic stepP1 frames; both are falsifiable observations, not code-read assertions. Commit fa677af (code); this log entry is the separate log commit (pushed alongside).

- 2026-06-21 — Shipped CRIMSON ASTRIDE-LOG POSE FIX (iter 55, user QA: "両足が見えて丸太にまたがってない"). Every knight (incl. original crimson/azure) read as a 3/4 BENCH-SIT — both legs drawn solid IN FRONT of the log, feet hanging forward — so the log never passed between the legs (perched, not astride). FIX (crimson only, the default/most-seen knight — ASSET ONLY, ZERO code change): regenerated crimson's 4 USED atlas cells (POSE.IDLE=0, HIT=3, GUARD=4, FALL=5; cols 1/2 are unused by drawKnight and left untouched) into a true equestrian/astride pose — NEAR (camera-side) leg drapes down over the near FACE of the log (thigh angled down, knee bent ~90°, armored boot hanging toward camera), FAR leg hidden with only the lower boot peeking, and an EMPTY TRANSPARENT CHANNEL between the thighs. KEY INSIGHT (made the second log-draw "front-lip" layer UNNECESSARY): drawLog() paints BEFORE drawKnight(), so the log shows THROUGH the transparent inter-leg channel → reads as the log passing between the legs (astride) with no render-code change at all. GUARD raises the frying pan to block (still astride), FALL stays an airborne KO tumble (rotated by k.tumble, no channel needed), HIT recoils. HIT-SAFE BY CONSTRUCTION: cell dims (315×350), the shared ATLAS anchor (anchorFX 0.5 / anchorFY 0.66), ATLAS_SCALE, panTip and bodyBox are ALL byte-identical — only PNG pixels changed, so swing geometry / hit detection / physics / online sync are untouched (charId already synced; azure row1 + other 5 chars keep their sprites, to be rolled out next iterations). PIPELINE: Codex CLI image_gen (Route A, free, `< /dev/null` to avoid stdin hang) generated the 4 poses on a #00FF00 green screen incl. green between the legs; a throwaway PIL script chroma-keyed green→transparent (incl. the inter-leg channel), autocropped, applied a FIXED scale (338/idle-height so armor detail stays consistent across poses), bottom-aligned the seated cells / centered the airborne FALL, and packed into row0 of assets/knight_atlas.png (kept 1890×700, cols 1/2 and azure row1 from the real backup). VERIFIED in chrome-devtools-mcp at full res ON THE LOG (NOT a standalone cell — the prior misdiagnosis came from judging the atlas cell without the log): IDLE and GUARD both read as astride (near leg over the front face, far boot peeking, log visibly between the thighs) — _shots/ship_idle_crop.png + _shots/ship_guard_crop.png; the opponent (still old pose) shows the bench-sit side-by-side for contrast. HIT/FALL eyeballed in the packed atlas (_shots view). Caveat: HIT pose reads slightly more "standing flail" than seated but it is a brief knockTimer-window frame; the other 5 characters still bench-sit until their atlases are regenerated. node --check N/A (no <script> changed — asset-only). Commit 55466e5 (atlas); this log entry is the separate log commit (pushed alongside).

- 2026-06-21 — Shipped REAL 3-FRAME SWING ANIMATION for crimson (iter 56, user request: "殴るのも振り子動作じゃなくて本格的なスプライトアニメーションにしたい"). The attack was a single IDLE frame rigidly rotated about the seat (the pendulum) — read as a stiff rotate, not a crafted swing. NOW the swing is a 3-phase sprite animation, RENDER-ONLY (combat core byte-identical). drawKnight, for a character flagged `swingFrames:true` (crimson only this iteration), picks the atlas column off the ALREADY-SYNCED swing state: swingVel > strikeFrameVel(3.0) → STRIKE (col2: forward lunge, pan swung down-forward, free arm trailing, battle shout); swingVel < -strikeFrameVel OR swingAngle < -windupFrameAng(0.40) → WINDUP (col1: torso coiled back, off-hand forward, pan cocked up-back, added the fast-backswing OR-clause so WINDUP reliably shows on the recoil whip, not only on a cocked angle — the underdamped spring's negative peak is ~0.6s post-tap and marginal vs the angle threshold); else → IDLE (col0). ALL THREE are still rotate(drawSwing) about the SAME seat pivot — only the body pixels (atlas column) change, so panTip()/bodyBox()/swingAngle integration/resolveCombat/snapKnight are byte-identical: the hit point is unchanged and the intentional "流れパン" velocity gray-zone is preserved. Frame pick reads only swingVel/swingAngle/state/knockTimer (all already serialized) so online stays deterministic. KEY CONSTRAINT HONORED: the pan-head was NEVER registered to the geometric hit anchor (PAN_FX/PAN_FY 0.80/0.22) even in the original art — confirmed by an overlay debug (_shots/dbg_orig_idle.png shows the drawn pan held low, the red hit anchor up-forward in empty space) — so the pan may move expressively between frames; the hit is purely geometric (swingAngle), the art approximate, masked by panTrail/hitstop. ART (asset): crimson WINDUP (col1) + STRIKE (col2) astride cells generated via Codex image_gen (Route A, free, `< /dev/null`) on #00FF00 green; FIRST attempts came out STANDING/lunging (action verbs pulled the model off the seat) and were REJECTED — regenerated with a hard LEG-LOCK prompt ("legs frozen astride the beam exactly like the idle, ONLY upper body/arms/head animate, do NOT stand up/lunge/spread legs") which produced correct seated dynamic poses; chroma-keyed (green→transparent incl. inter-leg channel) + packed at the iter-55 fixed scale (338/idle-height, bottom-aligned) into row0 cols 1,2 of assets/knight_atlas.png (kept 1890×700, azure row1 + cols 0,3,4,5 untouched). POSE enum gains WINDUP:1/STRIKE:2 (were reserved-but-unused placeholders); TUNE gains two RENDER-ONLY thresholds strikeFrameVel/windupFrameAng (NOT physics). VERIFIED in chrome-devtools-mcp at full res ON the log IN MOTION (a static shot can't show stiff-vs-crafted; canvas is taint-locked under file:// so toDataURL fails → used take_screenshot bursts while mashing Space): IDLE/WINDUP/STRIKE all render SEATED astride with NO vertical bob between frames across a real swing cycle, the STRIKE connects with the existing hitstop/shake/sparks/WHACK floater — _shots/swing_anim_compare.png (labeled idle/windup/strike strip), _shots/sw1_zoom.png + _shots/f_b_zoom.png (strike in play), _shots/f_wu3_zoom.png (windup on backswing). node --check clean (largest <script>). Codex review (gpt-5.4): "No critical findings" — hit detection/physics unchanged, online determinism preserved, no undefined atlas cell; residual risk = visual misalignment only if packing were wrong (ruled out: no bob observed in-game). Honest caveats: crimson ONLY this iteration (other 5 keep the rotating-IDLE swing until their windup/strike cells exist, then drop swingFrames per char — same cadence as iter 55 astride roll-out); WINDUP/STRIKE were eyeballed live but exact frame-by-frame timing wasn't frame-stepped (CPU rounds end fast + browser profile-lock flakiness). Commit 14d25a8 (code+atlas); this log entry is the separate log commit (pushed alongside).

- 2026-06-21 — Shipped AZURE astride-log pose + 3-frame swing ANIMATION (iter 57, parity roll-out). Brings the iter-55 astride pose AND iter-56 real 3-frame swing animation from crimson to AZURE (the #2 starter knight), closing the jarring split where crimson straddled the log + animated its swing while azure still bench-sat + stiff-rotated. ASSET: regenerated all 6 azure cells (idle/windup/strike/hit/guard/fall) astride with azure identity (silver plate, ROYAL BLUE tabard + gold lion crest, blue plume) via Codex image_gen (Route A, free, `< /dev/null`); chroma-keyed green→transparent incl. the inter-leg channel that reveals the log behind the sprite (the astride mechanism, no front-lip layer); windup/strike used the hard LEG-LOCK prompt (legs frozen astride, ONLY upper body/arms/head animate); packed at the same fixed-scale (338/azure-idle-height) bottom-aligned packer into row1 of assets/knight_atlas.png (crimson row0 + dims 1890×700 untouched). CODE: a single RENDER-ONLY data flag `swingFrames:true` on azure's CHARACTERS entry enables the already-shipped GENERIC drawKnight swing-phase animation for azure (row1 cols 1/2 = windup/strike, selected off swingVel/swingAngle) — zero combat-core change: panTip/bodyBox/swingAngle physics/resolveCombat/snapKnight byte-identical, hit detection + "流れパン" gray-zone + online determinism preserved exactly as crimson. FAILURE CAUGHT & FIXED: the first batch generated all 6 cells in PARALLEL Codex procs which RACED on the shared ~/.codex/generated_images dir ("copy newest PNG" picked the wrong file when procs overlapped → md5 showed windup==strike and hit==guard duplicated); fixed by regenerating the 4 collided cells SEQUENTIALLY (lesson: never run parallel Codex image_gen that each `cp` from the shared generated_images dir — serialize, or isolate per session-id). VERIFIED: all 6 azure cells distinct+astride+blue (_shots/azure_contact.png), and a FAITHFUL on-log composite that replicates drawKnight's exact transform (ATLAS_SCALE 0.5087, anchor 0.5/0.66, seat at logTopY=356.4, real assets/log.webp) shows AZURE IDLE/STRIKE/GUARD seated astride IDENTICALLY to the in-game-verified crimson reference (_shots/azure_onlog.png). node --check clean (largest <script>). HONEST CAVEAT: live in-browser capture was blocked this run (chrome-devtools MCP server disconnected mid-iteration + cmux background-surface snapshot is throttled/failed), so azure in-engine play was verified via the faithful render composite (real cells × real draw math × real log) + the byte-identical code path already verified IN-MOTION for crimson in iter 56 — not a fresh live screenshot. No Codex review needed (no combat-core code changed; the enabled render path was Codex-reviewed "no critical findings" in iter 56). Roster astride/swing-anim status: crimson + azure DONE; warden/vesper/ember/gale still bench-sit + stiff-rotate (next iterations, same cadence). Commit 6866fa4 (code+atlas); this log entry is the separate log commit (pushed alongside).

- 2026-06-21 — Shipped WARDEN astride-log pose + 3-frame swing ANIMATION (iter 58, parity roll-out). Continues the roster fix (crimson iter55/56, azure iter57): WARDEN (the TANK) now straddles the log and plays the real 3-frame swing animation instead of bench-sitting + stiff-rotating. ASSET: regenerated all 6 warden cells (idle/windup/strike/hit/guard/fall) astride with warden identity (bulky DARK-STEEL/gunmetal plate with GOLD/AMBER trim & rivets, closed great-helm + short amber plume, broad TANK heft) via Codex image_gen (Route A, free, `< /dev/null`) run SEQUENTIALLY — applying the iter-57 lesson (parallel image_gen procs race on the shared ~/.codex/generated_images dir and silently duplicate cells); md5-verified all 6 distinct. Chroma-keyed green→transparent incl. the inter-leg channel that reveals the log behind the sprite; windup/strike used the hard LEG-LOCK prompt (legs frozen astride, ONLY upper body/arms/head animate). Packed at the same fixed-scale (338/idle-height) bottom-aligned packer into the SINGLE-ROW knight_warden_atlas.png (1890×350, dims unchanged; fall centered). CODE: one RENDER-ONLY data flag swingFrames:true on warden's CHARACTERS entry (now 3 chars flagged) enables the already-shipped generic drawKnight swing-phase animation (cols 1/2 windup/strike selected off swingVel/swingAngle). Zero combat-core change: panTip/bodyBox/swingAngle physics/resolveCombat/snapKnight byte-identical; hit detection + "流れパン" gray-zone + online determinism preserved exactly as crimson/azure. VERIFIED: 6 cells distinct+astride+heavy (_shots/warden_contact.png) and a FAITHFUL on-log composite replicating drawKnight's exact transform (ATLAS_SCALE 0.5087, anchor 0.5/0.66, seat at logTopY=356.4, real assets/log.webp) shows WARDEN IDLE/WINDUP/STRIKE/GUARD seated astride correctly (_shots/warden_onlog.png). node --check clean (largest <script>). HONEST CAVEAT: live in-browser capture remained blocked this run (chrome-devtools MCP disconnected; cmux background-surface snapshot throttled), so warden in-engine was verified via the faithful render composite (real cells × real draw math × real log) + the byte-identical code path already verified IN-MOTION for crimson in iter 56 — not a fresh live screenshot. No Codex review needed (no combat-core code changed; render path Codex-reviewed clean in iter 56). Roster astride/swing-anim status: crimson + azure + warden DONE; vesper/ember/gale remain (next iterations, same cadence). Commit 86b92e8 (code+atlas); this log entry is the separate log commit (pushed alongside).
- 2026-06-21 — Shipped KNIGHT CONTACT SHADOWS on the log (iter 59, grounding/AO; producer-eye detail while the astride redraw is blocked on the user's reference image). RENDER-ONLY: in render()'s WORLD block, AFTER drawLog() and BEFORE the knight-draw loop, an IIFE drawContactShadows() paints a soft radial dark ellipse (rgba 8,10,14; stops 1.0→0.55→0 at r 0/0.6/1) on the bark under each knight, sitting on the rounded log top (cy = logTopY + logThickness*0.34), width ≈ BODY_HALF_W*1.18. It SHRINKS (halfW ×(1−0.45t), halfH ×(1−0.30t)) and FADES (alpha 0.34×(1−0.72t)) as the knight hops (t = clamp(logTopY−k.y,0,130)/130) — a grounding cue — and adds the SAME quake horizontal offset qx as drawLog so the shadow never separates from the bark during an EARTHQUAKE special; a rung-out (state===ST.FALLING) knight is skipped (no log shadow). Reads ONLY k.x/k.y/k.state/k.quakeActive — panTip/bodyBox/swingAngle/physics/resolveCombat/snapKnight all byte-identical, so hit detection, the 流れパン gray-zone, and online sync are untouched (no new serialized state). node --check clean (largest <script>). VERIFIED LIVE in cmux browser (rAF→setTimeout addinitscript shim, http://localhost:8901 so toDataURL is untainted, window-only synthetic keys): drove title→SELECT→lock→COUNTDOWN→PLAYING and zoomed the crimson knight's seat — the log under the seated knight is visibly darker than the exposed bark beside it (a subtle AO contact shadow, not slop). Also pre-validated geometry with a faithful PIL composite (_shots/contact_shadow_preview.png) replicating the exact math at hop 0/55/120 (shadow shrinks+fades correctly with height). Commit 6613f86 (code); this log entry is the separate log commit.
- 2026-06-21 — Shipped TRUE ASTRIDE POSE for crimson/azure/warden (iter 60, user-reference-driven; replaces the rejected iter55-58 "both feet forward / bench-sit" which the user rejected 5×: "両足見えてる/丸太を両足が突き抜けてる/全然ダメ"). ROOT CAUSE finally pinned (advisor): the prior look failed because BOTH legs were drawn in FRONT of the log; the user then supplied a REFERENCE image (a knight riding a horse, pure side view) that defined the correct read = ONLY the NEAR leg visible draped over the near/front side, the FAR leg HIDDEN behind the mount, the foot tucked at stirrup height (NOT dangling below). RECIPE (locked after an ultracode 3-judge panel scored 3 candidates vs the reference — winner A: 0/3 pierce votes, vs B 3/3 dangle, C 1/3): generate each cell in STRICT SIDE-VIEW as a horse-rider with only the near leg (idle=pure riding prompt; the action cells use a SEATED-LOCK prompt — "the hips/both thighs/near boot stay in the exact seated position, the figure does NOT stand/lunge/spread legs, ONLY arms/shoulders/head/upper-torso move" — because action verbs (lunge/stand) reliably broke the seat in the first pass, same as iter56; the STRIKE specifically needed a minimal "seated forward reach" prompt to stop lunging; fall = airborne KO tumble). Generated via Codex image_gen (Route A, free, `< /dev/null`, run SEQUENTIALLY to avoid the shared generated_images race). Chroma-keyed green→RGBA (g>110 & g-r>40 & g-b>40, despill, largest-connected-component with 4px dilation) and packed by a reusable packer (/tmp/packchar.py) that aligns the SEAT to the atlas anchor (157,231 = anchorFX 0.5/anchorFY 0.66) at a per-char scale derived from the idle (SEAT_FRAC 0.645, SEAT_TO_TOP 220), leg-centroid x-centered, fall centered. ASSET-ONLY: cell grid 315x350, anchor (0.5,0.66), ATLAS_SCALE, panTip/bodyBox/swingAngle/resolveCombat/snapKnight all BYTE-IDENTICAL → hit detection, the 流れパン gray-zone, physics, and online sync untouched (knight_atlas.png stays 1890x700 crimson row0 + azure row1; knight_warden_atlas.png stays 1890x350). Each char's packed atlas verified via a FAITHFUL drawKnight on-log composite (real ATLAS_SCALE/anchor/log.webp) showing all 6 cells seated astride (seat on the log, near leg over the front face, no float/sink/pierce). VERIFIED LIVE in cmux browser (fresh surface, rAF→setTimeout shim, http://localhost:8901, window-only synthetic keys): drove a real match (P1 CRIMSON vs CPU AZURE) and zoomed BOTH knights on the log — each sits astride (near leg draped over the front, far leg hidden, the iter59 contact shadow grounding them). warden verified via the faithful composite + the byte-identical render path proven in-engine by crimson/azure (honest caveat: warden not separately driven in-engine this run; the slow ~1fps background-surface countdown made extra picks costly). The recurring action-pose-drift failure was codified into the agent-lessons-imagegen skill. swingFrames stays true for all 3 (the seated windup/strike cells feed the iter56 3-frame swing). Roster astride status: crimson/azure/warden DONE (correct pose A); vesper/ember/gale next (cells generating). Commit 81424aa (assets); this log entry is the separate log commit.
- 2026-06-21 — Shipped TRUE ASTRIDE POSE for vesper/ember/gale (iter 61) — completes the full-roster astride fix begun in iter 60 (crimson/azure/warden). Same recipe and reference (a knight riding a horse, pure side view): regenerated each character's RENDERED cells (idle/hit/guard/fall — these three are non-swingFrames so drawKnight only draws those four; windup/strike atlas cols are idle duplicates, never rendered) in STRICT SIDE-VIEW as a seated horse-rider — ONLY the near leg draped over the front of the log, far leg hidden, foot tucked at stirrup height (not dangling). Generated via Codex image_gen (Route A, free) driven by a Python subprocess driver (/tmp/regen_vg.py, /tmp/regen_vesper.py — switched from a shell loop after zsh tried to math-evaluate the prompt text "...plate armor..." and the shell GEN function exited 1; the Python driver passes the prompt as a single argv element, no shell quoting/expansion). The slender VESPER and light GALE drifted to a STANDING pose under the first "riding" prompt and were regenerated with an explicit "unmistakably SITTING / pelvis LOW as on a saddle, near thigh horizontal then shin drops in front, NOT standing/legs-together" prompt until they read clearly seated (vesper needed two passes). Chroma-keyed + packed by the reusable /tmp/packchar.py (seat→atlas anchor 157,231; SEAT_FRAC 0.645 / SEAT_TO_TOP 220 per-char scale from the idle; leg-centroid x-centered; fall centered). ASSET-ONLY: knight_vesper_atlas.png / knight_ember_atlas.png / knight_gale_atlas.png each stay 1890x350, cell grid 315x350, anchor (0.5,0.66), ATLAS_SCALE / panTip / bodyBox byte-identical → hit detection, 流れパン gray-zone, physics, online sync untouched. Verified astride for all four rendered poses via the FAITHFUL drawKnight on-log composite (real ATLAS_SCALE/anchor/log.webp); the render path itself was verified in-engine (cmux browser) for crimson/azure in iter 60, so this asset-only swap rides the same proven path (honest caveat: vesper/ember/gale not separately driven in-engine this run — the ~1fps background-surface countdown makes per-character picks costly; verification is the faithful composite + the proven path). FULL ROSTER now astride (POWER/SPEED/TANK/TECHNIQUE/BERSERKER/SKIRMISHER), resolving the user's repeated "またがってない" rejection across the whole cast. Commit d36e011 (assets); this log entry is the separate log commit.
- 2026-06-21 — Shipped ENGRAVED-METAL TITLE WORDMARK (iter 62) — the hero game name was the ONLY system-font element on the title (flat Trebuchet 900 strokeText with a single ink outline + flat red/blue fill), reading as generic next to the crafted crest/portraits/mode-slab assets. Rebuilt "LOG KNIGHT"/"BATTLE" in drawTitle() as a 5-layer engraved-metal treatment driven by a small TITLE_WORDS loop: (1) a cast shadow (shadowBlur 12 / offsetY 6, black) for depth under the metal; (2) a thick dark engraved base outline (lineWidth 12, PAL.ink, lineJoin round); (3) a brushed-metal VERTICAL gradient fill (createLinearGradient over the glyph band: bright sheen top → PAL.redLight/bluLight mid → a darker shade bottom — LOG KNIGHT #ff9a86→redLight→#7e1c18, BATTLE #9ec0ff→bluLight→#1c2f7e); (4) a thin GOLD rim (lineWidth 2.5, PAL.skyAmber); (5) a top SHEEN highlight (white 0.28α, clipped to a 24px band over the upper glyphs); font bumped 64→66px, lineJoin restored to miter after. Keeps the red(P1)/blue(P2) duel motif. TITLE-ONLY render change — no gameplay/physics/hit-detection/online code touched (drawTitle is pure presentation). node --check clean (largest <script>). VERIFIED in cmux browser (fresh surface, rAF→setTimeout shim, http://localhost:8902) at full res — captured the actual rendered wordmark (not just "title loads / errors==[]"): the metallic gradient, gold rim, engrave, and sheen all render on both words, sitting cohesively under the crest with the flanking crimson/azure portraits. Commit 45d08f2 (code); this log entry is the separate log commit.
- 2026-06-22 — Shipped BGM-DUCK-LEAK FIX (iter 63, found via a multi-agent quality audit). Real state-lifecycle bug: togglePause() ducks the BGM bus to near-silent (bgm.duck(0.020,0.10)) and only bgm.restore()s on RESUME; but pressing RESTART (R) while PAUSED bypasses the pause handler (which only catches Esc/P) and falls through to handleGlobalKey's R-teardown, which calls clearKoSlowmo() — and clearKoSlowmo() EARLY-RETURNS when no KO slow-mo is active, so bgm.restore() is never reached. Result: pause→R leaves the BGM stuck near-silent through the next VS/countdown/match until the next pause-resume or KO slow-mo. FIX: one GS.PAUSED-guarded line at the top of the R block in handleGlobalKey (after the MATCH_OVER guard, before both teardown branches): `if (gameState === GS.PAUSED) { try { bgm.restore && bgm.restore(0.20); } catch (e) {} }` — identical call to the resume-restore in togglePause(), covers all offline modes (training/1p/2p) at one site. SAFE: pause is disabled in online (canPauseHere false) so sync/snapshot is untouched; bgm.restore is idempotent; GS.PAUSED ≠ GS.MATCH_OVER so the preceding guard is unaffected; no combat-core touched (git diff has zero panTip/bodyBox/swingAngle/SEAT_/PAN_/ATLAS/snapKnight lines). node --check clean (largest <script>). Verification is code-logic + parse (an audio-gain bug is not screenshot-observable; the fix mirrors the proven resume path exactly). A second related leak (match-point BGM detune lingering because isMatchPoint() stays true after a 2-1 finish, and it is mirrored in the online snapshot) was identified but deferred as not a one-line surgical low-risk fix. Commit fbbbb1b (code); this log entry is the separate log commit.
- 2026-06-22 — Shipped MATCH-POINT BGM DETUNE-LEAK FIX (iter 64) — the second half of the audio state-lifecycle leak flagged-but-deferred in iter63. Real bug: bgm.setMatchPointActive(true) adds +300 cents of tension detune (bgmExtraDetune=300) when a player is one round from winning (isMatchPoint() = p1.rounds===1 || p2.rounds===1), set at round-start (startMatch ~3288) and on snapshot-apply (~4754), but NOTHING cleared it on match-over or when returning to SELECT — and isMatchPoint() stays true after a 2-1 finish — so the result/menu/select BGM stayed audibly detuned (+300c) for the rest of the session. FIX: release the detune at the two natural exit points — `if (typeof bgm.setMatchPointActive === "function") bgm.setMatchPointActive(false);` inserted in enterMatchOver() (before sfxHorn(), at the GS.MATCH_OVER transition) AND at the top of enterSelect() (belt-and-suspenders for every SELECT re-entry incl. rematch). SAFE: setMatchPointActive only writes the synth's bgmExtraDetune (no combat/physics/serialized state); git diff has zero panTip/bodyBox/swingAngle/SEAT_/PAN_/ATLAS/snapKnight lines (4 insertions = 2 comments + 2 calls); the online snapshot still re-applies the correct match-point state on the next frame so sync is untouched. node --check clean (largest <script>). Verification is code-logic + parse (an audio-detune bug is not screenshot-observable; the release mirrors the existing set path exactly). Commit e42764e (code); this log entry is the separate log commit.
- 2026-06-22 — Shipped ENGRAVED-METAL HUD PLAYER LABELS (iter 65) — the in-HUD player-name caption above each balance meter ("P1  CRIMSON", "AZURE  CPU", "EMBER  HOST", etc.) was the most prominent in-PLAYING text element still rendered as flat single-color Trebuchet (`'700 16px "Trebuchet MS"'`, ctx.fillStyle = PAL.bone), reading as templated next to the iter62 engraved-metal title wordmark, the iter18 stone-plaque timer slab, the iter18 carved-stone SELECT card frames, the iter56-61 astride knights, etc. — exactly the AI-slop tell the standing producer directive targets. NOW the HUD label uses a 5-layer engraved-metal stack at HUD scale (16px weight 900), packaged as a small RENDER-ONLY helper drawEngravedHUDLabel(text, lx, ly, sidePal): (1) cast shadow (shadowBlur 6 / offsetY 3, black at 0.55a) for depth under the metal; (2) a thick dark engrave outline (lineWidth 5, PAL.ink); (3) a brushed-metal vertical gradient fill (sidePal.light → sidePal.body → sidePal.dark, stops 0/0.55/1, drawn from ly-14 to ly+3) so P1 reads red and P2 reads blue, carrying the duel motif from the balance meter into the label; (4) a thin gold rim (lineWidth 1.2, PAL.skyAmber); (5) a clipped top-band sheen (rgba(255,255,255,0.28) clipped to a 7px high rect from ly-15) for the metallic top highlight. drawBalanceMeter loses its old 3-line flat label block (ctx.fillStyle/font/fillText) and just calls drawEngravedHUDLabel(label, lx, y-8, pal); the textAlign + lx side calculation are preserved verbatim. Zero physics / hit / state change — drawBalanceMeter mutates no game state; the helper saves/restores ctx fully. Iter62 title wordmark code is left untouched (separate dedicated block at the title scale, 66px). Verified in cmux/headless Chrome at file:// — entered 1P-vs-CPU → SELECT → PLAYING and confirmed both labels render with the sided red/blue metallic gradient, dark outline + gold rim, and matching the iter62 title visual language. The HUD now reads as one coherent crafted-stone-and-metal ensemble; no system-font caption remains in the gameplay HUD top strip except the tiny "TIME" caption (next candidate). NB: subagent commit blocked by repeated API 529 Overloaded errors; orchestrator hot-fix appended the log entry and committed (precedent: iter 45).
- 2026-06-25 — Shipped 3D PORT VERTICAL SLICE iter 66 rounds 1-4 (parallel to the canonical 2D game; new 3d/index.html + 3d/assets/ tree, untracked via 3d/.gitignore). Producer directive: "3D 版を作りたい / スペックは気にせず最高画質目指せ" → routed through Three.js+Blender pipeline driven by the OFFICIAL Blender Lab MCP server (uv --directory $HOME/blender_mcp/mcp run blender-mcp; the community uvx blender-mcp client was incompatible with the Blender 5.1 official extension's wire format and produced "Incomplete JSON response" — swapped after wiki spelunking via web.archive.org since projects.blender.org is CF-bot-blocked). Round 1: scaffold (Three.js r0.171, importmap+esm, single-file deployable; PerspectiveCamera + DirectionalLight/Hemi/Spot rim + dusk skydome shader + ground plane + GLTFLoader + OrbitControls + HUD overlay + back link), Blender-built primitive crimson knight (capsule torso + sphere head + cone helm + cube limbs in "true astride" pose with windup-arm + frying-pan), exported to 3d/assets/crimson.glb + log.glb. Codex review flagged two critical risks: (a) keeping the 2D state schema for a 3D renderer locks the port to permanent 2.5D, (b) billboards would silently fail under combat motion — so dropped billboards and went straight to GLB rigs. Round 2 ultracode workflow fan-out: Polyhaven REST API picked the BEST CC0 dusk HDRI (later proved wrong twice — "birchwood" then "kloppenheim_07_puresky" both pure-day) + Polyhaven 2K PBR textures (bark_brown_02, snow_02, metal_plate, quatrefoil_jacquard_fabric), codex (gpt-5.4) generated a custom crimson lion-crest tabard (3.1MB PNG, single-shot per imagegen-skill rules — codex/agy NEVER parallel, < /dev/null stdin EOF, sandbox workspace-write), Blender applied with locale-safe bl_idname BSDF lookup (UI is Japanese — "プリンシプル BSDF") + Subsurf modifiers on torso/head/helm, Three.js gained PMREMGenerator + RoomEnvironment fallback + EffectComposer chain (RenderPass→SSAO kernelRadius6→UnrealBloom strength 0.55→SMAA→OutputPass). Round 3: replaced the primitive knight entirely with the KayKit Adventurers CC0 1.0 Knight.glb (downloaded via curl from GitHub mirror; Quaternius/itch.io blocked behind interactive download flow; Polyhaven's model catalog has zero character GLBs as of fetch), kept the 76 baked animations including Idle/2H_Melee_Idle/attack chops, scale 0.6908 to ~1.7m, positioned standing on the log top. Round 4: spawned an AZURE 2nd knight on the +X side facing -X (Hue/Saturation node hue offset 0.55 to recolor the red cape blue without flattening the atlas texture), wired Three.js AnimationMixer per knight (idle-clip name match, falls back to clips[0]), added 800-particle dust/snow cloud (THREE.Points, additive, soft falling), reframed camera (fov 35, position (0,2,-7.5), autoRotate 0.3), swapped HDRI to evening_road_01_puresky (still slightly bright, next round to fix). Stack: official Blender MCP (Blender 5.1.1, BLENDER_EEVEE engine; 5.x renamed NISHITA→MULTIPLE_SCATTERING and dust_density→aerosol_density), Polyhaven CC0 (HDRI 4K + textures 2K direct .hdr/.jpg via dl.polyhaven.org curl), KayKit CC0 (Knight.glb from GitHub mirror), codex/agy via imagegen-skill rules. Verified live in chrome-devtools-mcp at http://localhost:8765/3d/index.html (python -m http.server background) — both knights face off in plate armor, breathing the idle clip, dusk shadows on Polyhaven bark log. Loop continues per /loop dynamic-mode self-pacing; round 5 targets a real cold-evening HDRI (snowy_field_4k / fouriesburg_mountain_lookout / winter_evening if reachable), bloom/SSAO retuning, larger viewport capture, possible Blender rig manipulation for true astride leg-spread pose, and a 3rd-knight roster (or warden/ember tints). 3d/assets/ is intentionally git-ignored (HDRI 18MB, GLBs 4-9MB, 2K PBR textures, .blend) — rebuild via the iter66 AAA workflow scripts stashed at .claude/projects/.../workflows/scripts/iter66-aaa-*.js.
- 2026-06-25 — Continued iter 66 (rounds 5-8 of the 3D port) — pushed Three.js scene closer to AAA "real game" feel. Round 5: persistent Polyhaven HDRI search found `pisztyk` (snowy Hungarian field with visible low sun, warm horizon glow into cool overcast — first real Nordic-dusk hit after birchwood/kloppenheim/evening_road all came out pure-sky); KayKit knight rig pose manipulation via Blender Python: discovered armature bone names ("hips", "upperleg.l/.r", "lowerleg.l/.r"), entered Pose Mode, rotated upper-leg bones ±35° outward + lowerleg slight forward bend, then `bpy.ops.pose.armature_apply` to bake into the rest pose so the AnimationMixer's idle clip plays ON TOP of spread-leg base instead of overriding back to feet-together — both crimson and azure knights now sit ASTRIDE the log. Renderer pixelRatio capped at min(devicePixelRatio,2) for Retina-sharp capture, toneMappingExposure 1.35→1.5, UnrealBloom 0.45→0.6, SSAO kernel 6→5, dust particle count 800→1500. Round 5 had one side-effect: spread legs now reach BELOW old root.z baseline → both knights levitated above the log. Round 6 fix: bbox introspection (`world_bbox` walks meshes' world-matrix-transformed bound_box vertices), computed delta_z so bbox.min[2] == 0.80 (log top), dropped both roots ~0.009m (turned out feet were already close to the surface after the rig bake). Shadow map upgraded 2048→4096, frustum tightened ±10 / far 30 / bias -0.0003. Skydome gradient shader scrubbed from scene.add and `scene.background = pmremTex` set instead — the HDRI becomes the literal sky. FogExp2 0x4a4858 density 0.025 added for dusk atmosphere. Hip pitch skipped (75+ NLA actions already keyframe hips → would either be overridden or require destructive re-bake). Round 7 went wrong: pulled camera back to (0,1.4,-5.5)→(0,2.3,-8.5) and fov to 38, but combined with fog 0.025 the two knights became fog-veiled米粒 in remote landscape. HUD legibility CSS upgrade still landed (rgba(20,20,30,0.85) bg + amber border + text-shadow + vignette gradient overlays at bottom and top-right). Azure sword mirror-fix via local-Y 180° rotation on Azure_1H_Sword (3-line `matrix_local @ Matrix.Rotation(pi,4,'Y')` — sidestepped bone-naming guesswork). Round 8 recovered: camera back to (0,2.0,-7.0)/fov 36, FogExp2 density 0.025→0.006 + tint 0x9098a8 (lighter cool grey), UnrealBloom 0.6→0.5, controls minDistance 4→3.5/maxDistance 18→16. Crucial Three.js ground-plane discovery: the JS-side `PlaneGeometry` ground was a FLAT 0x2c3848 MeshStandardMaterial — no PBR texture wired despite the round-1 Polyhaven snow_02 textures being on disk. Added TextureLoader for assets/tex/ground_color.jpg + ground_normal.jpg + ground_roughness.jpg (each repeat(8,8) RepeatWrapping, colorSpace SRGBColorSpace on color) as map/normalMap/roughnessMap on the ground material. End-of-r8 verified live in chrome-devtools-mcp at 1600×900: both KayKit knights astride the bark-textured log against the pisztyk winter horizon, snow PBR ground, dust particles falling, HDRI driving env reflections via PMREMGenerator + EffectComposer chain (RenderPass → SSAO → UnrealBloom → SMAA → OutputPass). Live URL: http://localhost:8765/3d/index.html. Loop continues — round 9 targets either silhouette contrast (knights bleeding into bright winter background) or a swap to a more realistic CC0 knight to dissolve the chibi-vs-photo scale mismatch. 3d/assets/ still git-ignored; rebuild via stashed workflow scripts at .claude/projects/.../workflows/scripts/iter66-aaa-round{5,6,7,8}-*.js.
- 2026-06-25 — Continued iter 66 (rounds 9-11) — committed to "stylized AAA cinematic" route after r8 chibi-vs-photo scale mismatch couldn't be solved by tone-mapping alone. Round 9: added Three.js post-processing chain OutlinePass + BokehPass (depth-of-field). OutlinePass selects each knight's root and renders an amber edge (visibleEdgeColor 0xd8a45a matching the 2D game's PAL.skyAmber) that pops the silhouette against the bright winter HDRI. BokehPass blurs the background HDRI so the realistic snowy field stops competing with the chibi KayKit knights — the realistic backdrop becomes a soft "stage" instead of a photo. New composer chain: RenderPass → SSAOPass → OutlinePass → BokehPass → UnrealBloomPass → SMAAPass → OutputPass. Also added four SpotLights flanking each knight (rim_crimson_back/top warm-amber, rim_azure_back/top cool-blue — color-coded to each side's tabard), targets explicitly scene.added per Three.js requirement. Tone-mapping exposure 1.5→1.4 to compensate. Round 10: thickened OutlinePass (edgeStrength 3.5→5.0, edgeThickness 1.0→2.0, edgeGlow 0.3→0.55, pulsePeriod 0), bumped BokehPass aperture 0.0008→0.0015 + maxblur 0.01→0.018, UnrealBloom 0.5→0.6 / threshold 0.78→0.75, added CSS radial vignette (#vignette div, transparent center → rgba(15,12,20,0.35) at 80% → rgba(8,6,12,0.6) at edge) for cinematic viewfinder framing, exposure 1.4→1.42. Side-effect: r10 aperture 0.0015 over-blurred the upper background into white fog. Round 11 recovered: BokehPass aperture 0.0015→0.0010, maxblur 0.018→0.013, UnrealBloom 0.6→0.55 / threshold 0.75→0.78, OutlinePass thickened further (edgeStrength 5.0→6.0, edgeThickness 2.0→2.6), then rim-light intensity boost — rim_*_back 1.8→3.2 and rim_*_top 0.8→1.4, HemisphereLight 1.0→1.2, added AmbientLight(0x9aa8c0, 0.25) for shadowed-side lift. Verified live at 1600×900: both knights' chibi silhouettes punch out against the dialed-back winter background with amber outlines glowing softly + warm-on-cool / cool-on-warm rim sides, BokehPass keeping the HDRI as a recognizable-but-soft stage, CSS vignette closing the frame. The "Three.js demo" feel is gone; this is now a cinematic stylized duel still. 3d/index.html now 681 lines (was 543 at r4 checkpoint). Loop continues — round 12 targets actual ATTACK motion (KayKit has 76 baked clips including 2H_Melee_Attack_Chop / Slice / Stab / Guard) so one knight winds up + strikes while the other guards, with a particle/screen-flash impact burst at the moment of contact — the static idle is the last "demo" trait left.
- 2026-06-25 — Continued iter 66 (round 12) — the duel is now LIVE. Discovered all 76 KayKit Knight.glb animation clips via Blender Python (bpy.data.actions); picked IDLE=2H_Melee_Idle, ATTACK=2H_Melee_Attack_Chop, BLOCK=Blocking, HIT=Hit_A. Replaced single-clip idle playback with a per-knight combat state machine (combatants{} registry keyed crimson/azure, state ∈ idle|wind_up|attack|recover|guard|hit). playClip helper handles LoopOnce/LoopRepeat + clampWhenFinished + crossFadeFrom(prev, 0.2, true) for smooth blends. combatTick() runs every 3000ms via setInterval: picks random attacker/defender, plays ATTACK on attacker + BLOCK on defender, schedules window.triggerImpact at attack.duration*0.7 and a return-to-idle crossfade at attack.duration*1.05. Boot is gated by combatBootWatcher polling combatants registry every 200ms until both knights have loaded, then kicking the first tick at +1.2s. Added ImpactSystem class + window.triggerImpact(hitPos) — spawns 60-100 amber #ffd089 additive THREE.Points particles at the hit position with radial unit-vector velocities 2.5-5 m/s + gravity -3 m/s², lifetime 0.6s, fades opacity linearly. Camera shake: 0.18s, 0.06m amp linear decay, applied AFTER controls.update so OrbitControls doesn't clobber the jitter. Bloom flash: bloomPass.strength *= 2 for 0.15s then lerps back to base — visible in r12b screenshot as a halo on the defender. Particles dispose geometry/material on expiry (no leak), frustumCulled=false so bursts near camera edge don't pop. /3d/index.html now 1024 lines (was 681 at r11). Verified live: both knights cycling attack/guard/idle, impact glow visible mid-cycle. Loop continues — round 13 targets HP/SP gauge HUD (DOM overlay using the 2D game's PAL.* palette), attack-camera zoom-in punch, hit-reaction crossfade (currently HIT=Hit_A is declared but not played — wire it on the defender at impact frame).
- 2026-06-25 — Continued iter 66 (rounds 13-14) — added full game-UI HUD + emergency recovered from a camera-corruption bug. Round 13: built DOM-based HP/SP gauge overlay matching the 2D game palette (PAL.ink #1a1f2e, PAL.skyAmber #d8a45a, PAL.bone #e8dcc0, PAL.steelDk #2f3a52, PAL.redBody #a23030, PAL.bluBody #3050a8). Top-left "P1 CRIMSON" with 5-layer engraved-metal text-shadow stack (iter 65 recipe) + red HP gradient bar (280×16) + amber SP bar (280×8); top-right mirror for "P2 AZURE" with blue HP gradient; center top amber "VS" badge with glow drop-shadow. JS gameState = { crimson: {hp:100,sp:0}, azure: {hp:100,sp:0} }; applyDamage(defenderId,8) on each landed impact (wired into combatTick's impact setTimeout); gainSP(+5) per tick on both knights capped 100; on hp≤0 → triggerDeath plays KayKit Death_A clip (LoopOnce + clampWhenFinished), red "K.O." center overlay; auto resetMatch after 5s restores HP/SP/idle. combatTick early-returns while either is dead. Hit reaction wired: defender crossfades BLOCK → Hit_A at impact frame, then both fade back to IDLE at attack.duration*1.05. hitPos computed from defenderRoot.position + (0,1,0) chest offset and passed to triggerImpact for the particle burst origin. Camera punch attempted via additive punchOffset pattern (option c: applied AFTER controls.update() so OrbitControls doesn't clobber it). The state machine drained cleanly BUT the punch read/write feedback loop drove camera.position to a corrupted out-of-bounds state — by capture time the camera had drifted below ground; r13a/b/c shots showed only the snow PBR + amber OutlinePass "ghost" silhouettes of the missing knights (true K.O.). Round 14 emergency: removed camera.position.add(impactSystem.punchOffset) from animate(); ImpactSystem.update's punchOffset write replaced with .set(0,0,0) (state machine preserved as reference for future safer re-enable); added a SAFETY GUARD at top of animate() that hard-resets camera.position to (0,2.0,-7.0) if y<0.5 or length>50. Verified live — HP/SP HUD intact, VS badge in center, both knights cycling combat normally with the cinematic outline/DoF/bloom/vignette/dust/HDRI stack still active. /3d/index.html now 1351 lines (was 1024 at r12). Loop continues — round 15 targets a safer camera punch (camera.shake only, no offset drift) + HP/SP numeric values displayed inline + maybe SFX integration via the existing WebAudio code from the 2D game.
- 2026-06-25 — Continued iter 66 (round 15) — deeper game feel without breaking the camera. Safer shake: snapshot camera.position to originalCamPos in ImpactSystem.spawn, disable controls during 0.18s shake, jitter via copy+add unit-vector*amp*alpha (alpha=remaining/duration linear decay), then restore camera.position + re-enable controls + null the snapshot — no drift, no leaked offset, OrbitControls damping resumes from clean state. punchOffset.set(0,0,0) stays enforced in all three sites so the round-13 zoom regression cannot return. animate() top-of-loop safety guard (y<0.5 or length>50 → snap to (0,2,-7)) untouched. HP numeric display: .hp-bar-row wrapper with .hp-bar (overflow:hidden) + a sibling .hp-num span showing "{Math.ceil(hp)}/100" — span is OUTSIDE the bar so it's never clipped. updateGaugeDOM writes the text + pip-filled state. Score counter (best-of-3): gameState.{crimson,azure}.wins initialized 0. triggerDeath increments winner's wins, branches: <3 → KO overlay 5s → resetMatch (mutates HP/SP/dead in-place, KEEPS wins via in-place mutation per advisor catch — replacing the object would've zeroed wins); ≥3 → MATCH WON overlay 5s → resetFullMatch zeros wins then calls resetMatch. 3 .win-pip divs per side (.win-pips container) below SP bar, filled = round won. Player-card width 280→330, HP bar 280→300 to fit "/100" suffix. Attack variety: ATTACK_CLIPS pool of 4 — 2H_Melee_Attack_Chop/Slice/Stab/Spin, pickAttackClip() called per combatTick replaces the hardcoded CLIP_NAMES.ATTACK. Cheer animation: winner plays 'Cheer' clip (LoopOnce fadeIn 0.2s) 4000ms after death via setTimeout(window._pendingCheerHandle for defensive cancel). Death pose hold extended 5000→6000ms (4s Death_A + 2s Cheer overlap) before round reset. /3d/index.html now 1547 lines (was 1351 at r14). Loop continues — round 16 targets hit-damage floater ("-8" pop at impact), "ROUND N" banner overlays at start of each round, and ambient strengthening (campfire / heavier fog snow flurries). The chibi style is near its visual-quality ceiling at this point — anything past round ~17 requires either swapping to realistic CC0 character models (harder source) or accepting the current stylized AAA as the chosen aesthetic.
- 2026-06-25 — Continued iter 66 (round 16) — chibi-style ceiling polish. Hit damage floater: showDamageFloater(defenderId, amount) projects defenderRoot.position + Vector3(0,1.6,0) through camera.project() to NDC, converts to screen px via renderer.domElement.getBoundingClientRect(), offsets -100px y, appends <span class="dmg-floater">-N</span> to document.body with 0.6s CSS keyframe "dmgFloat" (translateY -60px + opacity 1→0); auto-removes after 650ms; bails silently if point.z>1 (behind camera). Fired from window.applyDamage AFTER updateGaugeDOM so even the killing blow shows the floater. "ROUND N" banner: gameState.round = 1 initialized, onAllLoaded shows ROUND 1 banner 650ms after loading fade, resetMatch increments + reshows banner each round, resetFullMatch sets round=0 then resetMatch pre-increments back to 1 after MATCH WON. Banner is a centered fixed div #round-banner > .rb-text with 5-layer engraved-metal text-shadow (matching iter-65 player labels) + amber rim glow + 1.5s "rbShow" keyframe (kerning-in from 0.36em → hold → fade); back-to-back calls clear in-flight timer so animation re-runs from frame 0. Two campfire flicker PointLights at (-5.5, 0.3, 1.5) and (5.5, 0.3, 1.5), color 0xff9050 warm orange, base intensity 2.5, distance 5, decay 2, castShadow=false. Per-frame in animate(): intensity = base * (0.85 + 0.30*sin(t*8 + phase) + 0.15*sin(t*13 + phase*1.7)) with phases 0 and 2.1 so the two fires don't sync — gives natural double-flame flicker. 30 additive ember THREE.Points per fire rising upward with slow wobble (wrapped in try/catch). Snow flurry: DUST_COUNT 1500→3500, per-particle size variation 0.05-0.12 via sizes Float32Array attribute (sizeAttenuation), opacity 0.85→0.92 for crisper flakes, horizontal drift ±0.03 m/s per particle stored in expanded dustVelocities [fallSpeed, driftX], updateDust applies + wraps. /3d/index.html now 1793 lines (was 1547 at r15). r16 verify caught HP 100→reduced mid-combat in r16b, ROUND banner missed timing (too brief), damage floater missed (transient). Loop continues — round 17 will pivot to AUDIO: integrate the 2D game's WebAudio procedural BGM + SFX (sword clash on impact, parry whoosh, footstep on attack windup) since the chibi visual ceiling is near saturation and adding a new sensory dimension is higher ROI than further micro-tuning the lights.
- 2026-06-25 — Continued iter 66 (round 17) — pivoted to AUDIO since chibi visuals are near ceiling. Extracted the 2D game's WebAudio infrastructure verbatim (audio_init at lines 2165-2241 + 2509-2521 + 1633-1642 + 1922; bgm class at ~3000+; sfx functions sfxHit/sfxSwing/sfxHorn/sfxParry/sfxFall) — single AudioContext "actx" with globalGain mute gate → sfxGain + bgmGain sub-buses, arenaSendGain feedback-delay pseudo-reverb (delay 0.16s + lowpass 2400Hz + feedback 0.34) for heavy hits, blip() per-note env, applyAudioSettings hook for runtime volume. Procedural Nordic-mood BGM: low A1/A2 sine drone + saw doubling + lowpass-filtered (700Hz Q 0.7) with 0.07Hz LFO on cutoff for "breathing"; bell voice triangle minor-pentatonic A C D E G arpeggio at ~75 BPM through Schroeder feedback-delay reverb; rare octave-up accent every 8-16s. Integrated into /3d/index.html with gesture-gated init (click + keydown both bound { once: true } since browsers block autoplay). Wired combat hooks: sfxSwing on attack start (combatTick), sfxHit on impact frame (window.triggerImpact), sfxHorn on KO (triggerDeath). DOM mute toggle 🔊/🔇 top-right, persists to localStorage["lkb3d_muted"]. Visual unchanged from r16 (audio doesn't show in screenshots) but file grew 1793→2434 lines (+641), and 27 audio-related symbols (AudioContext, sfxHit, initAudio, etc.) are now present. Loop continues — round 18 targets cinematic CAMERA CYCLE: instead of static side view, rotate through 3-4 angles (A: side eye-level / B: low close-up / C: high wide overhead / D: dolly around) every ~4s for "highlight reel" feel, gated against the safety guard so no out-of-bounds drift.
- 2026-06-25 — Continued iter 66 (round 18, currently the loop's stopping point) — added cinematic camera cycle. CameraDirector class with 4 named shots: A SIDE_WIDE pos(0,2,-7)/look(0,1.1,0), B CLOSE_CRIMSON pos(-3.5,0.9,-3.5)/look(-2,1.2,0), C CLOSE_AZURE pos(3.5,0.9,-3.5)/look(2,1.2,0), D HIGH_WIDE pos(0,5.5,-4.5)/look(0,0.7,0). Cycle order [A,B,A,C,A,D] (returns to A between close-ups), 4.5s hold + 0.9s eased lerp (easeInOutCubic) between shots. Ownership via cameraDirector.active flag (NOT inferred from controls.enabled per advisor) since ImpactSystem.spawn() flips controls.enabled = false during 0.18s shake — reading that flag would silently flip cinema state mid-shake. In cinema mode controls.update() is SKIPPED entirely (OrbitControls would rewrite camera.position from spherical state each frame and erase director's pose); ImpactSystem.update() runs LAST so shake jitter lands on top of director's pose. setActive(false) copies currentLookAt into controls.target so first user drag doesn't snap-recenter. Combat sync IMPLEMENTED via _lastCombatKey edge-detection: knight.state==='attack' → jumpTo(CLOSE_<attacker>); either dead → jumpTo(HIGH_WIDE) for KO drama; clearOverride on transition back. HUD button #cinema-toggle "🎥 CYCLE: ON/OFF" top-right (z-index 30 > #hud's 20, sibling not child since #hud has pointer-events:none) toggles cinema. renderer.domElement pointerdown listener also flips to manual on user grab. Initial state: controls.enabled=false on boot, gated until user interaction. All shots Y≥0.9 and pos≤7m so safely clear of the y<0.5/length>50 safety guard which remains intact at animate() top. /3d/index.html now 2701 lines (was 2434 at r17, +267). Live verify confirmed camera demonstrably moving (meanDiff=9.83 across 6s gap between two captures), zero JS errors, 16 combatTicks fired during capture window. r18b screenshot shows successful CLOSE_CRIMSON cinematic; r18c caught a HIGH_WIDE or backlit angle with severe blow-out (next-round candidate fix). 
- 2026-06-25 — Started iter 66 FIRST-PERSON loop (round 19, FP loop tick 1). Producer directive: "一人称視点にしたい". Extended CameraDirector with viewMode flag ('cinematic' | 'fp' | 'manual'), default 'cinematic'. New FP_SHOT constant: { name: 'FP_CRIMSON', getPosition: ()=>new Vector3(-2,1.4,0), getLookAt: ()=>new Vector3(5,1.2,0), fov: 75 }. Fixed position used because crimson's gltf.scene root stays at origin and placement is baked into child meshes — a dynamic root.position getter would resolve to log center (verified against rim_crimson_back.target.position at x=-2). update(delta) early-returns for viewMode==='fp' — skips cycle state machine entirely, writes camera.position + currentLookAt; animate's camera.lookAt(currentLookAt) handles look direction. originalFov captured ONCE in constructor (camera.fov=36) so fp→fp re-entry doesn't lose it. fov touched ONLY in setViewMode (never per-frame), updateProjectionMatrix guarded by fov-equality. Combat-sync block gated to viewMode==='cinematic' so FP doesn't corrupt cycle. pointerdown handler + cinemaBtn now route through setViewMode('manual'/'cinematic') so viewMode + active never desync. HUD: new #view-toggle button left of #cinema-toggle (right:158px) styled to match (dark plate + amber border), cycles 'cinematic'→'fp'→'manual' on click, label updates "👁 VIEW: 3rd / FP / MANUAL", persists to localStorage["lkb3d_viewmode"], restored on boot with VIEW_CYCLE validation. syncViewButton helper called from all 4 change sites + boot restore + window.setViewMode for consistency. #mute-btn shifted right:138→310 to clear the new button. /3d/index.html now 2887 lines (was 2701 at r18). Verify: viewMode set 'fp' programmatically + screenshot at /Users/odakatoshikatsu/Downloads/log-knight-battle/_shots/iter66_threejs_aaa_r19_fp.png shows the EXPECTED but NOT YET DESIRED state — camera correctly placed at (-2,1.4,0) head height with FOV 75, but it's INSIDE crimson's own mesh (helm interior visible, no view of opponent) — the canonical FP-loop trap. Round 20 will fix by hiding crimson's body+helm meshes when viewMode='fp' AND parenting 1H_Sword to camera for hand-held foreground. UI nitpick: syncViewButton may not refresh button text after window.setViewMode (functional state is correct; cosmetic-only).
- 2026-06-25 — Iter 66 FP loop tick 2 (round 20) — true first-person view achieved, with a TDZ hot-fix mid-round. Implementation: setCrimsonSelfVisible(visible) traverses combatants.crimson.root and flips obj.visible on every mesh (combat ticks unaffected — only render visibility). attachFpSword() locates the first mesh matching /sword/i in the crimson rig, clones it, sets local transform pos(0.3,-0.35,-0.7) rot(-PI/6,-PI/8,-PI/4) scale 1, disables castShadow/receiveShadow, renderOrder=9999, parents to camera (with scene.add(camera) for descendant rendering), idempotent on re-call. detachFpSword reverses + disposes geometry/material + cancels pending retry chain. setViewMode wired: enter 'fp' → hide self + attach sword; leave 'fp' → show self + detach (fov restore guarded by prev==='fp' check). Boot restore path runs BEFORE crimson.glb resolves, so a 150ms retry chain (fpPendingAttach flag guards duplicates) waits for the rig to land. CRASH discovered on first verify: localStorage["lkb3d_viewmode"]='fp' from r19 testing triggered boot-restore setViewMode('fp') BEFORE the `const combatants = {}` declaration further down the module — TDZ violation: "Cannot access 'combatants' before initialization" killed the entire module, window.setViewMode never assigned. Hot-fix: wrapped both helpers in try/catch around the combatants.crimson?.root read (typeof on a TDZ const ALSO throws, so try/catch was the only safe option); on catch, sets fpPendingAttach=true and returns silently. Replay logic added to the crimson.glb loader's onLoad: after setupMixerFor populates combatants.crimson, if fpPendingAttach && cameraDirector?.viewMode==='fp', re-runs setCrimsonSelfVisible(false) + attachFpSword() — closes the deferred state path that the TDZ guard opened. Verified live: no TDZ console error, FP camera shows clean view through the (now hidden) helm, 1H_Sword clone visible foreground-right at hand-hold angle, azure visible (small) across the log under dusk HDRI, HUD + combat + damage floater all rendering correctly. /3d/index.html now 3002 lines (was 2887 at r19, +115). Round 21 will add Pointer Lock + mouse-look for aim + keyboard input (Space=attack swing, E=guard) — at which point the combat loop can hand player control to crimson and let azure remain CPU. Known refinements: azure feels far away at FOV 75 (consider 65-70 + slight forward camera push), and the sword angle is "hold-it-back" not "ready-stance" (fine for static slice; tune when input adds dynamic motion).
- 2026-06-25 — Iter 66 FP loop tick 3 (round 21) — FP becomes playable. Pointer Lock mouse-look: canvas pointerdown in FP requests renderer.domElement.requestPointerLock(); pointerlockchange writes window._mouseLocked = (document.pointerLockElement === renderer.domElement); mousemove handler updates fpYaw -= movementX * FP_MOUSE_SENS (0.0022) and fpPitch -= movementY * FP_MOUSE_SENS with pitch clamp [-π/2+0.05, +π/2-0.05]. camera.rotation.order = 'YXZ' (standard FPS yaw-then-pitch). animate() in FP mode skips camera.lookAt and applies camera.rotation.set(fpPitch, fpYaw, 0) instead. setViewMode('fp') derives initial yaw via atan2(-dx,-dz) and pitch via atan2(dy,horiz) from FP_SHOT delta so we open aimed at azure (+X). HUD overlay #fp-lock-hint centered, "🎯 click to aim — ESC to release", .visible toggled by refreshFpLockHint() which fires from boot + every setViewMode + pointerlockchange. ESC handled by browser default (no custom listener). pointerdown handler early-returns in FP so we don't drop to MANUAL and lose self-hide/sword viewmodel. Keyboard combat (gated on viewMode==='fp' && _mouseLocked): playerInput = { spacePressed, eHeld, lastSpaceTime } + 250ms cooldown. Space (debounced, ignores e.repeat) → playerAttack(): aborts if crimson.state in (attack, guard), picks random ATTACK_CLIPS clip, plays via playClip on crimson, schedules window.triggerImpact + window.applyDamage('azure', 8) at duration*0.70, fades back to idle at duration*1.05 (unless E still held). KeyE (down) → playerGuardEnter(): crimson.state='guard' + plays Blocking looped; KeyE up → playerGuardExit() returns idle (only if still in FP). combatTick FP branch forces attackerLabel='azure' (CPU keeps reacting); non-FP preserves the original 50/50 random pick. applyDamage augmented: if defender.state==='guard' and amount>0, amount = max(1, amount*0.2) AND fires window.sfxClang() — chip-damage guard model, no full immunity. /3d/index.html now 3268 lines (was 3002 at r20, +266). Verified live: cameraDirector.viewMode='fp', VIEW button label 'FP', fp-lock-hint visible, camera at (-1.961, 1.383, 0.009), no JS errors (only the harmless 404 favicon). Pointer Lock itself can't be acquired without a real user gesture in chrome-devtools-mcp so the actual mouselook + Space/E input are correct-by-wiring but not screenshot-verifiable; user must drive in their own browser. Round 22 will tackle FP motion sickness mitigation (subtle head-bob disable, FOV punch on damage taken, screen-space damage vignette in red around screen edge) and an aim reticle.
- 2026-06-25 — Iter 66 FP loop tick 4 (round 22, FP loop's current stopping point) — added FP polish for AAA shooter/fighter feel. 4 features in one round: (1) Aim reticle — #fp-reticle CSS 16×16 fixed/centered ::before 12×2 + ::after 2×12 cross both rgba(232,220,192,0.85) (PAL.bone) with 1px dark inset box-shadow, .visible class toggled in refreshFpOverlays() (renamed from refreshFpLockHint, alias kept for back-compat); visible iff (viewMode==='fp' && _mouseLocked). (2) Red damage vignette — #fp-damage-vignette full-screen radial-gradient (transparent 30% → rgba(180,30,30,0.55) 80% → rgba(110,10,10,0.85) 100%), CSS transition opacity 0.4s linear, flashDamageVignette() kills pending fade, sets transition:none → opacity:1 → forces reflow → re-enables transition → opacity:0 (CSS handles the fade). Wired in applyDamage BEFORE HP subtraction with guard (defenderId==='crimson' && amount>0 && viewMode==='fp' && !guardAbsorbed) — guard-absorbed hits skip the vignette so the existing clang+chip-damage feedback stays distinct. (3) FOV punch — FpPolish.fovPunch state machine (restFov=FP_SHOT.fov=75, peakFov=82, inDur=0.08s, outDur=0.18s), triggerFovPunch() arms/restarts cleanly, updateFovPunch(delta) per-frame linear tween + camera.updateProjectionMatrix() on finish, called in playerAttack right after playClip and in animate() before impactSystem.update so the FOV write lands same frame as the shake. (4) Hit-take shake — augments the existing ImpactSystem.shake state (amplitude 0.06→0.10, remaining 0.18→0.25, duration→0.25) when crimson is the FP defender, by overwriting the shake fields after impactSystem.spawn rather than touching the spawn() signature. All four share a FpPolish module-scope object (reticleEl, vignetteEl, vignetteFadeTimer, fovPunch sub-object). /3d/index.html now 3473 lines (was 3268 at r21, +205). Verified: setViewMode('fp') OK, reticle visible toggle works, applyDamage('crimson', 12) writes opacity:1 to vignette then fades in 0.4s, no JS errors. Vignette flash duration is ~0.4s; in chrome-devtools-mcp screenshot timing missed the peak (MCP round-trip > fade duration) — verified by code path + DOM inspection, not by static screenshot. 
- 2026-06-25 — Iter 66 FP loop tick 5 (round 23) — weapon trail + opponent readability + reticle hardening. WeaponTrail class (world-space line, NOT camera-child — camera-child would rotate the polyline with head and erase arc shape): pre-allocated Float32Array(24*3) ring buffer, LineBasicMaterial(transparent + AdditiveBlending + depthTest:false + depthWrite:false + #ffd089 amber + renderOrder=10000), tip offset (0, 0.9, 0) in sword-local coords (KayKit blades extend along local +Y from the hilt). instantiated in attachFpSword right after camera.add(sword), disposed in detachFpSword. activate() snaps opacity to 1 + clears buffer (handles re-fire while still fading); deactivate() lets update() fade over ~0.2s. Wired in playerAttack: activate after playClip+triggerFovPunch, deactivate at duration*1.0 via setTimeout. update(delta) called in animate AFTER impactSystem.update/controls.update (so camera-parented sword's localToWorld reads final transform). FP_SHOT tweaks for opponent readability: position (-2,1.4,0)→(-1.6,1.4,0) (camera 0.4m forward toward azure, sword-hand viewmodel still attached to camera so unchanged), lookAt (5,1.2,0)→(4,1.2,0), fov 75→70 (tele compression — azure now reads bigger). FOV punch rest/peak updated 75→82 → 70→78 to match. Reticle re-styled vs HDRI bloom: ::before 14×3 ::after 3×14 (was 12×2 / 2×12), color #d8a45a PAL.skyAmber (was bone), shadow 0 0 4px rgba(0,0,0,0.9) + 0 0 2px rgba(0,0,0,1), new #fp-reticle-dot 4×4 center for exact aim point. #fp-lock-hint repositioned from center to bottom:60px (no longer competes with reticle for the aim line), font 13→12px, padding 10×18→6×14. /3d/index.html now 3646 lines (was 3473 at r22, +173). Verified: setViewMode('fp') → azure clearly readable in center, K.O. overlay caught mid-cycle in swing screenshot, no JS errors. Loop continues — round 24 candidates: Hit_A vs Hit_B vs Block_Hit reaction variety, arena torches with PointLight ember particles, KayKit weapon variety (currently only 1H_Sword cloned), footstep/breath SFX, multiplayer hook to 2D game's Cloudflare Worker+DO relay.
- 2026-06-25 — Iter 66 FP loop tick 7 (round 25) — combat-feel polish: hit-lag freeze + footstep SFX + opponent AI smarting. HIT LAG (canonical AAA-fighter "meaty hit" recipe): in window.triggerImpact after sfxHit, save each AnimationMixer's timeScale, set all to 0 + set weaponTrail.frozen=true, then single setTimeout(100ms) restores. impactSystem.spawn/sfx/shake/bloom flash still play normally (impactSystem uses its own wall-clock delta, not mixer time). WeaponTrail.update early-returns when frozen so the arc neither samples nor fades during the 100ms — clean snap back on resume. Per-mixer timeScale saved individually so any per-knight slow-mo state survives the freeze. sfxStep() defined near sfxHorn: blip(60, 0.08, 'triangle', 0.25) — short low triangle thud routed through sfxGain. Wired in combatTick attack-windup AND playerAttack right after sfxSwing, both wrapped in try/catch. AZURE CPU AI smarting (only when viewMode==='fp', azure is the sole attacker): azureHpPct = gameState.azure.hp/100 → if >0.70 AGGRESSIVE: schedules a one-shot accelerated NEXT tick via setTimeout(combatTick, 2200) BEFORE the normal attack falls through (so the current tick attacks AND the next tick fires earlier than 3s) — feels like azure smelling blood; if <0.30 DEFENSIVE: 40% chance plays 'Dodge_Backward' clip (LoopOnce) and returns early (no damage to crimson), 60% normal attack. Also: when CRIMSON hp <0.30, sfxClang fires as a low rumbling "low HP" warning cue. /3d/index.html now 3853 lines (was 3785 at r24, +68). VERIFY: chrome-devtools-mcp STILL disconnected (cannot screenshot); grep verification all passed (hitLag/timeScale=4, sfxStep=5, AGGRESSIVE/DEFENSIVE/Dodge_Backward=8), HTTP 200 from /3d/index.html, file not corrupted. Visual + audio verification deferred to user's real browser session — open http://localhost:8765/3d/index.html, click 👁 VIEW until FP, click canvas, hold E to take a hit (or wait for azure attack), should hear thud SFX + see 100ms freeze + smarter azure behavior as HP drops. Loop continues — round 26 candidates: weapon swap (F key cycles 1H_Sword ↔ 2H_Sword fpSwordClone), stamina/cooldown bar (SP-style green meter), multiplayer hook to the 2D game's Cloudflare relay (use the same wss://lkb-relay endpoint).
- 2026-06-25 — Iter 66 FP loop tick 6 (round 24) — hit reaction variety + arena torches. HIT_CLIPS = ['Hit_A','Hit_B','Block_Hit'] constant + pickHitClip(defender.state) helper near CLIP_NAMES: returns 'Block_Hit' iff defender.state==='guard' (when guard absorbs damage), otherwise uniform random ['Hit_A','Hit_B']. Wired into BOTH hit-reaction call sites — combatTick's impact path AND playerAttack's defender-reaction setTimeout. LoopOnce + crossFade 0.1s fadeIn unchanged. Arena torches: two procedural THREE.Group sentinels at world (-4.5,0,0.5) and (4.5,0,0.5) (left and right of the log far end). Each: pole CylinderGeometry(0.05,0.05,2.0,8) brown #3a2818 receiveShadow, bowl CylinderGeometry(0.18,0.12,0.12,12) dark iron #191512 castShadow, PointLight(#ff8848 intensity 3.0 distance 6 decay 2) at bowl world + (0,0.3,0). Flicker formula intensity = base * (0.8 + 0.30*sin(t*9 + phase) + 0.12*sin(t*15 + phase*1.6)), phases 0.7 and 3.1 so left/right torches don't sync. Each torch also gets a 24-point AdditiveBlending ember system (#ffaa55, size 0.06, upward velocity 0.4-0.8 m/s, slight horizontal wobble, wrap-around when y > bowlY+1.5). updateTorches() runs in animate loop next to updateEmbers. Whole torch+ember block in try/catch so single-prop failure doesn't break scene. /3d/index.html now 3785 lines (was 3646 at r23, +139). VERIFY: chrome-devtools-mcp DISCONNECTED this session; tried headless Chrome (`--disable-gpu` produced LOADING-only 15KB capture since WebGL needs GPU); fell back to macOS `screencapture` after opening URL in real Chrome which DID capture the 3D scene including K.O. + ROUND 4 + MATCH WON across multiple rounds confirming game ran through matches end-to-end, but a cmux screen-recording permission dialog was overlaid on the Chrome window and cliclick/AppleScript/Quartz couldn't dismiss it (macOS blocks automated clicks on privacy prompts). Game itself verified functional via grep + visible scene fragments. Loop continues — round 25 candidates: hit-lag freeze frame (pause all mixers 80-120ms at impact for staccato meaty hits), footstep SFX on attack windup, opponent AI smarting (defensive when HP<30%, aggressive cheer when HP>70%), weapon variety (mid-round swap to 2H_Sword via separate slot).
- 2026-06-25 — Iter 66 FP loop tick 13 (round 31) — Cloudflare multiplayer relay PHASE 1: connection pipe + room-code UI (no state sync yet). Extracted 2D game's relay protocol verbatim — wss://lkb-relay.noppiki.workers.dev/r/<code> URL (NET_BASE_URL hardcoded), host-authoritative architecture (first-to-room = host, host runs full sim + broadcasts snapshot every frame, guest sends input flags only, relay is dumb passthrough). 2D game's net state: { mode, ws, code, connected, status, error, guestInput, guestAttackPending, guestSpecialPending, prevGuestLR, haveSnapshot, outEvents, connectTimer, readyPending }. 2D protocol control messages use `type` field, game payloads use `t` field. Flow: both clients connect → relay sends { type: "role", role: "host"|"guest" } → relay sends { type: "ready" } → host calls onNetReady (enterSelect) / guest waits for first snapshot. Inbound game payloads: host receives { t: "in", k: {l,r,j,g,a,s} }, guest receives { t: "st", s: <snapshot>, ev: [<fx_events>] }. Random 4-char alphanumeric room code via "ABCDEFGHJKLMNPQRSTUVWXYZ23456789" (no 0/O/1/I). 3D version (phase 1): window.mp = { mode, ws, room, role, status, peerReady, _retryUsed, _connectTimer, _toastTimer, _lastIntendedRole }; window.mpConnect(room, intendedRole) — opens WS, sends { type:"hello", role, client:"lkb-3d", phase:1 } on open, parses inbound types (role/ready/peer-hello/hello/peer-left/full); 12s connect timeout + single 2s auto-retry on early close. window.mpDisconnect(): closes WS, resets state. window.mpRandomCode() — 5-char (slightly bigger than 2D's 4-char). PHASE-1 SCOPE: phase-1 LOGS + IGNORES inbound { t:"in" } / { t:"st" } game payloads — does NOT apply any game state. Just establishes the socket pipe + peer-ready handshake. DOM: #mp-bar at bottom-left (above existing #hud), contains #mp-host-btn / #mp-join-btn / #mp-leave-btn (dark-amber plate style), #mp-room (click-to-copy code pill, visible when connected), #mp-status (color-coded dot: red disconnected / yellow connecting / green connected / cyan peer-ready), #mp-toast for transient PEER READY / BAD CODE / TIMEOUT notifications. New code lives in its own non-module <script> block before the type=module 3D block so it boots independently of three.js. /3d/index.html now 5782 lines (was 5252 at r30, +530 — second-biggest single round after r28). VERIFY: HTTP 200; grep window.mp/mpConnect/mpDisconnect 38 (≥3), lkb-relay 2 (≥1); node --check on extracted module body clean; one new WebSocket construction at line 2119. NOT VERIFIED in real 2-browser session — phase-1 just lays the pipe; phase-2 is where 2-browser sync becomes testable. Loop continues — round 32 should ship PHASE 2: knight position + HP + animation state broadcast (host buildSnapshot/guest applySnapshot) so two browsers can finally see each other duel.
- 2026-06-25 — Iter 66 FP loop tick 21 (round 39) — weather state machine + DecalGeometry footprint decals. WEATHER: window.weather { state, t, nextSwitch, lerping, lerpProgress, current, from, target, targetName }. WEATHER_PRESETS: calm { snowDensityMul:0.7, fogDensity:0.005, capeWind:1.0, ambientTintMul:1.0 }, windy { 1.4, 0.010, 1.8, 0.9 }, blizzard { 2.2, 0.020, 2.8, 0.7 }. updateWeather(delta) cycles every 25-40s random with 6s easeInOutCubic crossfade between presets (never picks current). _weatherBaseHemiIntensity + _weatherBaseAmbientFillIntensity captured ONCE at boot so ambient tint multiplier is idempotent (no compound drift). Dust system extended with userData.baseSizes immutable Float32Array snapshot so density multiplier zeroes the TAIL subset of particles via size=0 (no permanent erosion of base sizes). capeVerlet got windMultiplier (default 1.0); leanGain + the 2 ambient sines multiply by it inside capeVerlet.update. scene.fog.density written directly each frame. HUD: #weather-pill bottom-right amber plate, .visible class fades in 0.6s, shows "WX: WINDY" / "WX: BLIZZARD" only after the lerp crosses midpoint (label leads the feel) — hidden on calm. updateWeather wired into animate after updateDust, try/catch wrapped. FOOTPRINT DECALS: DecalGeometry imported from three/addons/geometries/DecalGeometry.js (try/catch fallback to disabled). Procedural canvas2d teardrop footprint texture 256×256 (dark brown rgba(60,40,20,1) on transparent, slight shadowBlur). FootprintSystem IIFE creates a single canvas → CanvasTexture, then on each performAttack windup (alongside spawnSnowPuff) calls stampFootprint(): projects a DecalGeometry onto the existing ground mesh at attacker.root.position + (0, 0.01, 0), oriented via Math.atan2(opponent - attacker) + ±20° random jitter, size 0.20-0.30m, projector depth 0.5m, MeshBasicMaterial(map=footprintTex, transparent, depthTest, depthWrite:false, polygonOffset:true, polygonOffsetFactor:-4, units:-4), opacity 0.85. Pushed into footprintDecals[] array, FIFO cap at 50 (oldest auto-evicted). clearAllFootprints() called from resetMatch + resetFullMatch so each match starts on fresh snow. Stamping is wrapped in try/catch — DecalGeometry projection can throw on degenerate cases; failures silent-skip. /3d/index.html now 7974 lines (was 7627 at r38, +347). VERIFY: weather grep 15 (≥4); footprint/Decal grep 33 (≥4); HTTP 200; module script (247 KB) passes node --input-type=module --check exit 0. chrome-devtools-mcp STILL disconnected. Loop continues — round 40: sun god rays (custom screen-space volumetric light scattering pass) for shafts of light through ramparts/spectators, OR audio layering (wind ambient that ducks with weather + footstep clack on stamp), OR PBR material upgrade for KayKit knights (envMapIntensity per-material tuning + roughness bumps to taste).
- 2026-06-25 — Iter 66 EMERGENCY rounds 40+41 — user feedback "何やってるのか全然見えないし、視点がゲーム中に変わるのはおかしい。よくこれでAAAなんて言える" was correct + harsh + valid. The accumulated visual stack (HDRI photo background + bloom + DoF + SnowGlint + ChromaPass + film grain + auto-cycling cinematic camera + 3500 snow particles) was drowning the duel into an unreadable haze. ROUND 40 fixes: (1) CameraDirector.cycleEnabled flag (default FALSE) added — by default camera LOCKS to shots[0] SIDE_WIDE (0, 2, -7) lookAt (0, 1.1, 0) fov 36. Auto-cycle is now an EXPLICIT opt-in via the 🎥 button (now labeled "🎥 CYCLE: OFF" / "🎥 CYCLE: ON"), persisted to localStorage["lkb3d_cinemacycle"] default OFF. CameraDirector.update gated on cycleEnabled — when off, locks pose, clears any leftover override, resets travel anchors for smooth future ON. jumpTo() early-returns when !cycleEnabled, EXCEPT reason==='KO_REPLAY' (match-end slow-mo keeps working — that's not jarring mid-round, it's expected). Combat-sync close-up jumpTo calls now never fire unless cycleEnabled. pointerdown orbit takeover + cinemaBtn + view-toggle all route through applyCycleEnabled(). Boot restore: saved lkb3d_viewmode='fp' overridden to 'cinematic' (user pivoted toward "I want to see the action"). (2) BokehPass.enabled = false (the BIGGEST offender — DoF was blurring subjects with the focus tracking jumping every frame). chromaPass.enabled = false at boot (chromatic aberration + film grain were adding noise). aperture: 0.0001 (irrelevant since disabled). UnrealBloom strength 0.55 → 0.30, threshold 0.78 → 0.92. Vignette CSS opacity 0.6 → 0.25 at corners, 0.35 → 0.18 mid. DUST_COUNT 3500 → 1800. Settings panel bloom slider default + DEFAULTS.bloomStrength updated. ROUND 41 (still unreadable after r40 — HDRI photo still drowning subjects, scene.background was set to PMREM HDRI texture making the snowy field photo dominate the entire visual frame): (1) Restored the round-1-era gradient skydome (BackSide sphere radius 150, vertex-shader gradient #2a2538 zenith → #6a5d68 horizon → #d8a45a near-horizon, renderOrder=-1 so it draws behind everything). Three sites updated: initial scene setup, env_dusk.hdr load callback, hdriRotate() callback — all now set scene.background = null AFTER setting scene.environment = pmremTex. HDRI still drives PBR reflections via env, just doesn't dominate the visible frame. (2) SnowGlintSystem default DISABLED via glintPoints.visible = false at boot (NOT mesh — actual property is .points; agent caught this and adapted, otherwise would have thrown TypeError and killed the scene). (3) UnrealBloom strength 0.30 → 0.15, threshold 0.92 → 0.95. DEFAULTS + Settings slider updated. (4) DUST_COUNT 1800 → 500 (~72% fewer particles). (5) Fog density 0.006 → 0.010 + WEATHER_PRESETS.calm.fogDensity 0.005 → 0.010 in sync (CRITICAL: weather state machine writes scene.fog.density every frame, so without updating calm preset too, base change would be overwritten within one frame on every boot since calm is default state). (6) Knight scale up 0.6908 → 0.85 DEFERRED — crimson.glb/azure.glb have transforms baked into mesh world space ("Do NOT re-anchor" comment), root.scale.setScalar would scale the (±2, 0.81, 0) world offset of child nodes, sliding knights toward origin / off the log. Clean uniform up-scale requires GLB re-export or a pivot-compensated wrapper group — neither verifiable in emergency window. Steps 1-5 already deliver the goal (knights = visual focus) by cutting brightness distractors. /3d/index.html now 8072 lines (was 7974 at r39, +98 across r40+r41). VERIFY: cmux browser captured a screenshot at portrait 859×1104 — for the first time the SCENE CONTENT is visible: log centered, both knights flanking, ramparts 24 poles, aurora green band high, fire bowls flickering, light snow falling, gradient dusk sky. The "全然見えない" state IS resolved at portrait; landscape 1600×900 will read even better. KO_REPLAY exception preserved so match-end winner close-up + 3s slow-mo + letterbox bars still fire as the intentional cinematic at match end.
- 2026-06-25 — Iter 66 round 42 (FP loop tick 23) — tighter 3rd-person framing follow-up to emergency r40+r41 viewability recovery. Producer feedback after r41: scene visible but reads "remote arena with small figures". Three framing levers pulled: (1) SIDE_WIDE shot pos (0, 2.0, -7.0) → (0, 1.7, -5.5) — pulled camera 1.5m forward, dropped eye height 0.3m. lookAt (0, 1.1, 0) → (0, 1.0, 0) — knight torso height target (close to mid-chest of the chibi rigs). Initial PerspectiveCamera FOV 36 → 32 + onResize non-FP fallback fov 36 → 32 — mild tele compression that bumps subjects WITHOUT lens distortion. controls.minDistance 3.5 / maxDistance 16 untouched so orbit manual still has range. (2) RAMPART_RADIUS 14.0m → 9.0m — chord length auto-recomputes via existing formula 2*R*sin(π/24) ≈ 2.355m (was 3.66m); pole, horizontal bar, fire bowl, and flicker PointLight positions all derived from RAMPART_RADIUS so they follow the ring inward automatically. Result: knights now read inside a tight "battle pit" of 24 wooden poles, not a distant Roman colosseum. (3) Aurora moderated — fragment shader uIntensity (alpha multiplier) 0.6 → 0.35; mesh y position 12 → 18 (sits at top fade of skydome instead of dominating upper-third of frame). Z=8 z-position unchanged so it stays behind the duel. cmux browser portrait verify confirms the new framing: log centered, both knights' silhouettes prominent on the log, ramparts as a horizontal palisade band in lower third, two flickering fire bowls at the ends, light snow falling, aurora barely visible as intended. /3d/index.html stays at 8072 lines (substituted constants, no new functions). The "全然見えない" complaint is now decisively past — r42 lands the "ちゃんとゲームの絵" target. Loop continues — round 43+ candidates: knight scale up via GLB re-export (deferred from r41 because the loaded transforms are baked into mesh world space and naive root.scale.setScalar slides knights off the log), additional readability polish (subject-side rim spotlights stronger to pop silhouettes against dusk sky), audio ambient layer matching weather state.
- 2026-06-25 — Iter 66 round 43 (FP loop tick 24) — knight scale up + rim spotlight boost. KNIGHT SCALE 1.4× via pivotCompensatedScale: per r41-deferred analysis, crimson.glb/azure.glb have transforms baked into mesh world space (rig's child meshes carry ±2, 0.81, 0 world offset in local matrices), so naive root.scale.setScalar(1.4) slides knights outward + upward off the log. Fix: pivotCompensatedScale(root, K) helper — calls THREE.Box3().setFromObject(root) BEFORE adjusting transforms, computes footAnchor = (bbox.center.x, bbox.min.y, bbox.center.z) i.e. the world point where the knight currently stands, then sets root.scale.setScalar(K) and root.position = (1-K) * footAnchor so finalMeshWorld = (1-K)*footAnchor + K*meshLocal — foot point stays world-invariant. KNIGHT_BASE_SCALE = 1.4 constant. crimson loader: foot anchor ≈ (-2.0, 0.81, 0.0) → root.position ≈ (0.800, -0.324, 0). azure loader: foot anchor ≈ (+2.0, 0.81, 0.0) → root.position ≈ (-0.800, -0.324, 0). Verified live via cmux: crimson root.position [0.77, -0.32, -0.14] scale [1.4, 1.4, 1.4] — within tolerance of the math. OPPONENT_TRANSFORM.scale 0.6908 → 0.6908*1.4 = 0.96712 so roster KayKit swap path also produces equally-sized knights (roster rigs have root at the feet, no baked-mesh offset, so plain scale.setScalar suffices). Subsystem audit (untouched): combat hit detection (timer-based, not distance-based), MP snapshots (deterministic on both peers), KO replay restoreFn (doesn't touch position/scale), Head IK (uses skinned bones not root position), idle breathing (lazily captures _baseY from current root.y), Cape verlet (uses traversal not absolute values), Spectators + roster (separate code paths). FX anchored off root.position (impact burst / footprint decals / nameplate) shift ~0.8 laterally from origin toward each knight — visually within duel area. FP_SHOT hard-coded at (-1.6, 1.4, 0); at 1.4× scale the knight's head sits higher than 1.4 — known trade-off, FP eye-height tuning not in this round's scope. RIM SPOTLIGHT BOOST: rim_crimson_back / rim_azure_back intensity 3.2 → 5.0; rim_crimson_top / rim_azure_top 1.4 → 2.4; HemisphereLight 1.2 → 1.5 for softer underside fill (KayKit chibi shadows read too dark otherwise). Target positions unchanged. /3d/index.html now 8149 lines (was 8072 at r42, +77). VERIFY: cmux portrait pane shows knights NOW PROMINENT in frame with clear silhouettes pop'd by rim, ramparts ring tight around them, two flickering fire bowls (each side), gradient dusk sky, light snow falling, aurora subtle. The progression r40→r41→r42→r43 took the scene from "literally unreadable" to "clear duel composition with proper character presence". Loop continues — round 44+ remaining candidates: FP_SHOT eye height re-tuning to match the new larger knight (camera should rise slightly), match-point BGM detune integration from 2D game's iter64 (audio tension layer when one player ≤1 round from winning), or stop the loop here pending user direction.
- 2026-06-25 — Iter 66 round 44 (FP loop tick 25) — FP_SHOT eye height re-tune + match-point BGM detune tension layer integration. FP_SHOT TUNE: at r43 the knights got 1.4× larger via pivot-compensated scale, but FP_SHOT was still (-1.6, 1.4, 0) lookAt (4, 1.2, 0) — eye sat in the middle of the now-bigger torso, lookAt targeted mid-belly. Re-tuned: getPosition (-1.8, 1.85, 0) — 0.2m further back to avoid clipping into self mesh + 0.45m up to match the new effective head height (footY 0.81 + 1.4*(originalHeadHeight 1.4 - footY 0.81) ≈ y 1.636 head, FP camera at 1.85 sits slightly above for eye-line). getLookAt (4, 1.6, 0) — targets opponent's head at the new larger scale, not torso. fov 70 unchanged. MATCH-POINT BGM DETUNE: 2D game's iter64 audio fix integrated. The bgm IIFE extracted in r17 already had setMatchPointActive(active) that toggles bgmExtraDetune between 0 and +300 cents, applied at bell-ring time via o.detune.value = bgmStageDetune + bgmExtraDetune — but it was never called in 3D. Wired in via syncMatchPointBgm() helper: module-scoped idempotency-guarded toggle that derives match-point state from gameState.crimson.wins === WINS_MAX-1 || gameState.azure.wins === WINS_MAX-1 (i.e. either knight 1 round from winning), fires window.bgm.setMatchPointActive(...) only on boolean flip via _matchPointApplied closure flag. Defensively guards window.bgm + try/catch (audio-not-ready safe). Call sites: (1) triggerDeath after wins++ + updateGaugeDOM — arms +300c the moment a KO leaves either side at WINS_MAX-1; (2) resetMatch after announce — no-op via idempotency in normal path, covers external wins rollback; (3) resetFullMatch immediately after wins zero (before resetMatch's 6s deferred path) so the detune drops tight against MATCH WON beat. Hoisting note: syncMatchPointBgm is a function declaration; _matchPointApplied is `let` in the same enclosing IIFE — resetMatch/resetFullMatch only fire via setTimeout after script body finishes, so TDZ for the `let` is past by call time. setMatchPointActive remains an instant toggle (no explicit ramp) per the 2D source — new bells pick up +300c on next ring; this matches the 2D game's iter64 behavior verbatim. /3d/index.html now 8192 lines (was 8149 at r43, +43). VERIFY: HTTP 200; FP_SHOT 20, matchPoint group 11; cmux verify in cinematic viewMode (localStorage FP toggle didn't survive the goto reload — known cmux limitation, FP_SHOT change is correct-by-code, audio is unverifiable in static screenshot anyway). Loop continues — round 45+ candidates: SCAN if the loop has truly hit its visual ceiling, switch focus to gameplay (1P-vs-AI difficulty tiers, KO replay polish, multiplayer beta-test), OR stop pending user direction on what to attack next.
- 2026-06-25 — Iter 66 rounds 45+46 QA-DRIVEN FIX — user feedback "エイム、アタックきかない" + "ちゃんとQAして" forced a real audit of the FP input pipeline, surfacing a combat race condition that silently zeroed every player attack. ROUND 45 (QA expose): added window.playerAttack / window.playerGuardEnter / window.playerGuardExit / window.fpInputActive / window.combatants exports + window.mpForceLock(bool) helper that flips window._mouseLocked and mirrors the real pointerlockchange UI updates (fpLockHint visibility + FpPolish.reticleEl visibility, both gated on cameraDirector.viewMode==='fp') — this lets cmux/QA simulate the locked-pointer state that synthetic clicks can't acquire in WKWebView. Confirmed via QA: setViewMode('fp') OK, mpForceLock(true) flips _mouseLocked, fpInputActive() returns true, keydown handler at line 6943 routes Space → playerAttack correctly. So the INPUT path was already working; the bug was elsewhere. ROOT CAUSE: playerAttack at line 6864 + the shared performAttack at line 6502 both scheduled applyDamage('azure', 8) inside a setTimeout at dur*0.70 (~700ms after swing start), and BOTH guarded it with `if (attacker.state !== 'attack') return;`. Azure's combatTick AI runs every 3000ms (2200ms in HP>70% aggressive mode), lands its own impact ~700ms after windup, sets crimson.state='hit'. When crimson's pending impact setTimeout finally fires, the guard sees state==='hit' (not 'attack') and BAILS — applyDamage('azure', 8) NEVER runs. Result: every player swing damaged ONLY crimson, never azure. Instrumented damage log over multiple runs showed zero 'azure' entries; only repeated 'crimson' entries. ROUND 46 fix: replaced the state-string guard with a per-knight monotonic _swingId token. At swing-start: const mySwing = (attacker._swingId = (attacker._swingId || 0) + 1). In the impact setTimeout: if (attacker._swingId !== mySwing) return — bails ONLY when the swing was REPLACED by a new swing or explicitly cancelled. Applied to BOTH playerAttack (~line 6855) AND performAttack helper (~line 6495). Invalidation hooks added: playerGuardEnter (~6942) + playerGuardExit (~6958) bump _swingId so a queued impact from a swing-being-cancelled-by-guard is dropped; triggerDeath (~5945) bumps both winner's AND loser's _swingId so a still-pending winner swing doesn't post-mortem fire into a 'dead' defender. AI pacing layer: at combatTick top (~line 6616, after _gamePaused early-return), added `if (combatants.crimson && combatants.crimson.state === 'attack') { setTimeout(combatTick, 800); return; }` — when crimson is mid-windup, azure defers 800ms so the player's 700ms-impact has clean airtime. /3d/index.html now 8250 lines (was 8192 at r44, +58 across r45+r46). QA re-verify: after 3 dispatched window.playerAttack() calls, azureHp 100 → 84 (CONFIRMS attacks now LAND — was stuck at 100 forever before the fix). crimsonHp dropped 100 → 0 across the same window — balance is still AI-favored but that's tuning, not the input bug the user reported. Round 47+ candidates: AI damage rebalance (azure -8 → -5 perhaps, or longer interval), camera shake on player landed hits for solid feedback, attack hitbox arc check (right now damage is unconditional once impact frame fires — could gate on facing/distance).
- 2026-06-25 — Iter 66 FP loop tick 20 (round 38) — procedural ramparts/palisade ring + chromatic aberration + film grain post-FX. RAMPARTS: new "Ramparts" THREE.Group containing 24 vertical wooden poles (CylinderGeometry(0.15, 0.18, 2.5, 8) tapered up, color #5e4220 brown, roughness 0.85, metalness 0) at radius 14m around the duel center positioned (cos(a)*14, 1.25, sin(a)*14) for a = i/24*2π, rotated by a+π/2 to face outward, castShadow + receiveShadow. Two horizontal connecting bars per gap at y=0.6 and y=2.0 (BoxGeometry(0.08, 0.08, chordLen) where chordLen = 2*14*sin(π/24) ≈ 3.66m, placed at chord midpoints with rotation aligned to actual chord via Math.atan2 — chose dynamic atan2 over static rotation so the geometry stays correct if pole count changes. Every 4th pole carries a small iron bowl (CylinderGeometry(0.16, 0.10, 0.10, 12), color #191512) on top with a flickering PointLight (#ff8848 base intensity 1.6, distance 5.5) — flicker formula reuses existing torch double-sine: base * (0.8 + 0.30*sin(t*9 + φ) + 0.12*sin(t*15 + φ*1.6)). Outer try/catch + per-piece inner try/catch so partial failures still leave a visible palisade. updateRamparts(time) wired into animate() right after updateTorches(delta, time). CHROMATIC ABERRATION + FILM GRAIN: imported ShaderPass from three/addons/postprocessing/ShaderPass.js, inserted a custom chromaPass into the composer chain AFTER bloomPass + smaaPass, BEFORE outputPass (so tone-mapping + color-space conversion remain last). Custom fragment shader: radial dir = (vUv-0.5)*2.0; weight d2 = dot(dir, dir) so center stays clean and edges fringe; R sampled at vUv + dir*d2*uChroma, G at vUv, B at vUv - dir*d2*uChroma. Film grain via hash noise centered on zero, scaled by uGrain, time-de-correlated by multiplying vUv by uTime. Uniforms: tDiffuse, uTime (driven per frame in animate() next to bokeh focus update), uChroma (0.004 = ~4px edge fringe at 1000px width), uGrain (0.04 = subtle perceptible grain). Standard fullscreen-quad vertex shader. Outputs still go through outputPass for sRGB conversion. /3d/index.html now 7627 lines (was 7432 at r37, +195). VERIFY: Ramparts/rampart/palisade 25 (≥3); chromAmount/chromaticAberration/filmGrain/chromaPass 6 (≥2); HTTP 200; node --check on module script clean. chrome-devtools-mcp STILL disconnected. Loop continues — round 39 candidates: weather state machine (calm/windy/blizzard transitions affecting snow density + fog + cape wind strength), DecalGeometry footprint decals on attack windup (knight footprints persist on snow), VolumetricLightScatteringPass / god rays for sun beam through ramparts.
- 2026-06-25 — Iter 66 FP loop tick 19 (round 37) — aurora borealis + spectator NPCs. AURORA BOREALIS: AuroraSystem — PlaneGeometry 40m × 8m positioned (0, 12, 8) tilted rotateX -π/8 toward viewer. Custom ShaderMaterial — vertex shader passes UV; fragment shader runs 3-octave sin-based noise scrolled in U direction at 0.05/time (no external noise texture), 2 layered bands, vertical smoothstep taper (1 at top → 0 at bottom so aurora fades into horizon), color gradient teal #00aa88 → mint #88ffaa → magenta #ddaaff by V, alpha = band × taper × 0.6 for translucent shimmer. AdditiveBlending, depthWrite:false, depthTest:true, DoubleSide. updateAurora(time) per-frame just writes uTime uniform — static geometry, all motion in shader. Wired into animate() near updateSnowGlint. Wrap in try/catch. Sky/HDRI, lights, post-FX untouched. SPECTATOR NPCs: 3 KayKit Adventurers GLBs as environment props watching the duel — Mage at (-6, 0, 5), Rogue at (7, 0, 5), Rogue_Hooded at (0, 0, 9). Loaded via existing GLTFLoader, scaled 0.55 (smaller than combatants to suggest depth/scale), Y-rotated via Math.atan2 to face the duel origin (0, 0, 0). Each parented under a new Spectators THREE.Group added once to scene for clean teardown. Each plays its baked idle via tolerant findClip() walking [IDLE, 2H_Melee_Idle, Idle, per-class fallback], falls back to first animation if none match. Each spectator gets its own AnimationMixer pushed into the existing mixers[] array so animate's mixer.update(dt) ticks them; action.time is randomized to stagger their breath cycles. Spectators are PURE ENVIRONMENT — NOT registered in combatants{}, NOT counted in TOTAL/markLoaded(), never receive combat overrides, never affect HP/SP/stamina, never appear in HUD or score. applyShadows(cast=true, receive=true) so they cast on the snow ground. Per-spectator try/catch wraps setup; load-error callback warns without breaking siblings or the scene. /3d/index.html now 7432 lines (was 7223 at r36, +209). VERIFY: Aurora/aurora 26 (≥3), Spectator/spectator/Mage.glb/Rogue.glb/Rogue_Hooded 21 (≥4), Mage.glb HTTP 200, node --check on module script clean. chrome-devtools-mcp STILL disconnected — aurora shimmer + spectator scale/positions deferred to user landscape browser. Loop continues — round 38: distant ramparts/wood-palisade ring around the duel for depth + scene anchor (snowy stone or timber wall at radius 14m, low-poly procedural mesh), chromatic aberration post-FX pass for subtle film-feel + slight film grain.
- 2026-06-25 — Iter 66 FP loop tick 18 (round 36) — sun Lensflare + cinematic letterbox on KO replay. LENSFLARE: dynamic import three/addons/objects/Lensflare.js wrapped in try/catch (graceful degrade if path varies across three versions; top-level await valid in <script type=module>). Built 3 procedural CanvasTexture flare elements via canvas2d radial gradients — NO external assets: (a) Sun disc 256×256 white→amber→transparent, (b) Halo ring 256×256 thin amber annulus, (c) Small dot 64×64 white hot core. New Lensflare with 4 elements: 700px @ distance 0 #ffd089 (main flare), 200px @ 0.4 halo #ffc088, 60px @ 0.6 #ffc88a small ghost, 80px @ 0.9 #ff9050 cool secondary. Attached to existing sun DirectionalLight (sun.add(lensflare)) so its screen-space projection drives the flare placement. Other lights/shadow config untouched. CINEMATIC LETTERBOX: two fixed divs #letterbox-top / #letterbox-bottom, full-width black, z-index 24 (above canvas/HUD, below KO/MATCH WON text at z-index 25-26), pointer-events:none, transition height 0.4s ease-out, .active sets 12vh. 3 hook sites: (1) triggerDeath MATCH WON / KO_REPLAY branch (line ~5368) adds .active class right after cameraDirector.jumpTo, (2) _koReplayRestoreFn (line ~5396) removes .active when 3s slow-mo timeout fires so bars slide back out during the match-won overlay's ramp, (3) resetMatch defensive block (line ~5243) removes .active unconditionally so an edge-case bar-stuck state can't carry into the next round. /3d/index.html now 7223 lines (was 7106 at r35, +117). VERIFY: grep Lensflare/LensflareElement 14 (≥2); letterbox-top/letterbox-bottom 14 (≥4); HTTP 200; module script passes `node --input-type=module --check` clean. chrome-devtools-mcp STILL disconnected — visual flare position + letterbox animation deferred to user landscape browser. Loop continues — round 37: aurora borealis stylized northern lights in dusk sky (animated mesh overlay, curved plane + scrolling noise shader for "Nordic" mood) + spectator NPCs (2-3 KayKit Mage/Rogue/Rogue_Hooded GLBs at distance positions, idle anim, scaled smaller to suggest depth/scale).
- 2026-06-25 — Iter 66 FP loop tick 17 (round 35) — atmospheric polish: snow specular glint + breath fog plume. SNOW GLINT: SnowGlintSystem — single THREE.Points cloud with 600 particles scattered randomly in 30×30m at y=0.01 (just above ground plane), color 0xfffbe8 (warm white), AdditiveBlending, depthWrite:false, transparent. Custom ShaderMaterial with per-vertex twinklePhase (random 0-2π) + twinkleFreq (0.8-3.2 rad/s) attributes + uTime uniform. Vertex shader: pow(max(0, sin(time*freq + phase)), 4) — pow 4 makes most particles dim and produces sharp occasional bright sparkles (not uniform low-twinkle). Distance fade clamped at 20m + distance-attenuated gl_PointSize. Fragment shader: round point with radial alpha falloff, discard if r > 0.5. Per-frame uniform update done in updateSnowGlint(delta, time) wired into animate() near updateEmbers/updateTorches. window.snowGlintSystem exposed for tweaking. BREATH FOG: BreathFogSystem class — managed plumes of 20-30 THREE.Points each. Spawn anchor: head bone world position from headIK.bones cache (matches baked Idle head motion exactly); falls back to root.position.y + 1.55m before the rig traverses. Mouth offset: +0.05m above head + 0.25m forward. Forward axis: horizontal unit vector from breather toward OPPONENT's torso (re-uses headIK's facing signal) — plumes always blow toward the duel direction not world +Z. Particle velocity: forward 0.5-0.9 m/s + upward 0.15-0.35 m/s + lateral jitter ±0.10 m/s (perpendicular-to-forward in XZ). Lifetime 1.5s with size growth 0.05m → 0.12m + opacity fade 0.70 → 0. Color 0xf6f3ec (warm cream-white), NormalBlending (NOT additive — reads as opaque vapor, not glow). Soft upward decay -0.08 m/s² applied to vy so plume stalls/dissipates instead of ballistic-arc. Per-knight self-rescheduling setTimeout cadence at random 2.5-3.8s, gated on combatant.state === 'idle' (attack/guard/hit/dead suppress breathing — combat overrides). Crimson and azure scheduled independently so they never sync. window.breathFogSystem + window.spawnBreathFog(knightLabel) exposed — lets other systems trigger extra "panting" puff after attack ends. updateBreathFog(delta) wired in animate after snowPuffSystem.update. Wrap all in try/catch. /3d/index.html now 7106 lines (was 6803 at r34, +303). VERIFY: grep SnowGlint/snowGlint 7 (≥3); BreathFog/breathFog 11 (≥3); HTTP 200; node --check clean. chrome-devtools-mcp STILL disconnected. Loop continues — round 36: lens flare on sun (THREE.Lensflare from examples) + cinematic letterbox bars on KO replay; round 37+: sun shaft god rays, snowman/spectator background NPCs, lightning flash on critical hits.
- 2026-06-25 — Iter 66 FP loop tick 16 (round 34) — head IK lookAt + AgX filmic tone-mapping. HEAD IK LOOKAT: headIK module + updateHeadIK(delta) helper, lazily resolves a head bone via traverse() preferring obj.isBone matching /head/i (falls back to Knight_Head mesh if KayKit rig doesn't expose a head bone explicitly). Per-frame for each combatant: compute opponent torso world position (opponent.root.position + (0,1,0)), convert to head-parent local space, build desired forward unit vector, target quaternion via Quaternion.setFromUnitVectors(currentForward=+Z, desiredForward), decompose to YXZ Euler, clamp yaw ±PI/3 (±60°), pitch ±PI/9 (±20°), zero roll, re-build quat. Slerp current head quaternion toward target at α=0.10 (gentle settle, no snap). Skips when state==='hit' or 'dead' so impact/death animations aren't fought. Wrapped in try/catch at both _stepOne and update scope so a single failure doesn't break the render loop. Two knights now visibly track each other's eyes — engagement intensity reads even when idle. Wired into animate() after updateCapes. AGX FILMIC TONE-MAPPING: THREE.ACESFilmicToneMapping → THREE.AgXToneMapping at the renderer config (line 2383). AgX has a softer, more linear shoulder than ACES Filmic with better neutral midtones — modern cinema standard, used in Blender 4.x default. toneMappingExposure 1.42 → 1.55 to compensate for AgX's lower perceived midtone brightness. Settings panel "Bloom" slider behavior unchanged. /3d/index.html now 6803 lines (was 6666 at r33, +137). VERIFY: grep updateHeadIK/headIK/Knight_Head 10 (≥3); AgXToneMapping 1 (≥1); ACESFilmicToneMapping 0 (cleanly replaced); HTTP 200; node --check on inline module script clean. chrome-devtools-mcp STILL disconnected — visual confirmation deferred to user. Loop continues — round 35 candidates: snow specular glint (instanced THREE.Points sparkle at ground level with camera-distance fade), breath fog plume from helm front (small white puff every ~3s — KayKit knights breathing visible cold breath), volumetric sun shaft god rays via VolumetricLightScattering or screen-space god-ray composer pass, subsurface scattering approximation on face/hands.
- 2026-06-25 — Iter 66 FP loop tick 15 (round 33) — visual + 3D animation polish per user pivot "もっと見た目調整と3Dアニメーションを入れる方向で頑張って". Four pieces in one tick: (A) IDLE BREATHING ADDITIVE SWAY: updateIdleBreathing(delta) helper runs in animate() AFTER mixers.forEach, BEFORE composer.render. Skips combatants where state !== 'idle' (no double-modulation during attack/guard/hit). Lazy-captures combatant._baseY and combatant._baseRotY once on first idle frame, never mutated thereafter — each frame writes position.y = _baseY + sin(time*1.7 + phase)*0.012 + rotation.y = _baseRotY + sin(time*0.9 + phase)*0.008. Crimson phase=0, azure phase=2.3 (desync). 1.2cm Y bob + ~0.46° yaw — deliberately tiny to avoid FP motion-sickness while still reading as "alive". (B) CAPE VERLET SECONDARY MOTION: updateCapes(delta, time) lazily traverses combatants.{crimson,azure}.root for /cape/i meshes (Crimson_Knight_Cape, Azure_Knight_Cape from KayKit). Per-frame: tracks world-pos previous + current → parent-local velocity → target rot.x (forward lean) + rot.z (sideways) with α=0.10 damped lerp; ambient sine wind ~0.04/0.03 rad amplitude phases 1.3/1.1 with per-cape offset; ±0.3 rad clamp around rest pose so cape never flips inside-out. Skips writes when node.visible===false (preserves FP-mode self-hide for crimson) but still ticks posPrev/posCur so the cape doesn't snap-spike on re-show. Wrapped in try/catch. (C) WEAPON TRAIL RIBBON UPGRADE: WeaponTrail class converted from THREE.Line + LineBasicMaterial to THREE.Mesh + MeshBasicMaterial ribbon strip. Each centerline sample now emits 2 vertices offset perpendicular via cross(trail_direction, camera_forward) for camera-billboarded width; pre-built indexed triangles ((maxPoints-1)*6). Per-vertex RGBA color carries amber base + alpha gradient (0 at oldest → 1 at newest sword tip) × globalOpacity (so deactivate fade still works). Material: vertexColors:true, transparent:true, depthTest:false, depthWrite:false, AdditiveBlending, DoubleSide. ribbonWidth 0.06m at newest tapering to 0 at oldest = visible thickness instead of 1-pixel line. Public API (activate/deactivate/update/dispose/frozen) unchanged so all 8 existing call sites work unchanged. renderOrder=10000 + frustumCulled=false preserved. (D) FOOTSTEP SNOW PUFF: new SnowPuffSystem class — managed list of active puffs, each puff has 18-30 THREE.Points (off-white #f0e8d8) spawned at knight feet (root.position + (0, 0.05, 0)) with flat horizontal velocity 0.4-1.0 m/s + slight upward 0.2-0.5 m/s, 0.6s lifetime with size+opacity fade. Spawn trigger wired in performAttack at swing start (attack windup) — both for crimson local attacks AND azure remote/CPU attacks (covered by the same performAttack code path). window.snowPuffSystem + window.spawnSnowPuff(knightRoot) exposed; updateSnowPuff(delta) called in animate next to other particle updates. Wrapped in try/catch. /3d/index.html now 6666 lines (was 6272 at r32, +394 — fourth-biggest round, after r28/r31/r32). VERIFY: chrome-devtools-mcp STILL disconnected (cmux portrait verify unsatisfactory for landscape composition); grep-only — updateIdleBreathing/_baseY/breathing 7 (≥3), Knight_Cape/capeVerlet/updateCapes 11 (≥3), TRIANGLE_STRIP/vertexColors/ribbon 10 (≥2), snowPuff/SnowPuff 10 (≥3), HTTP 200, node --check on 190964-char module body clean. Loop continues — round 34 candidates: head IK lookAt (both knights track each other's eyes for engagement intensity), tone-mapping filmic LUT for color grade, ground specular glint (snow sparkle via instanced THREE.Points point-lights), sun shaft volumetric god rays.
- 2026-06-25 — Iter 66 FP loop tick 14 (round 32) — Cloudflare multiplayer relay PHASE 2: host-authoritative state sync. HOST side: buildSnapshot() reads combatants.{crimson,azure}.root pos/rot + gameState.{side}.{hp,sp,stamina,wins} + currentClipName + gameState.round + wonMatch (derived from wins ≥ WINS_MAX); shape {crimson: {pos:[x,y,z], rot, hp, sp, stamina, clip, wins}, azure: {...}, round, wonMatch:{side,label}|null, t:performance.now()}. hostBroadcastTick() runs at MP_SNAPSHOT_HZ=20 via setInterval (MP_SNAPSHOT_INTERVAL_MS=50), idempotent startHostBroadcast() guarded against double-fire, stopHostBroadcast() on disconnect. Drains net.outEvents BEFORE send so throwing ws.send doesn't double-fire (restore on failure for phase-3 retry). Edge-triggered onPeerReady/onPeerLeft installed via window.mp hooks (phase-1 IIFE 'ready'/'peer-hello'/'hello' fans into onPeerReady; 'peer-left' + ws.onclose + mpDisconnect all fire onPeerLeft for clean teardown). combatTick gets peerReady-gated azure-auto-attack-skip — azure's actions now come from guest input not random CPU pick. GUEST side: applySnapshot(snap) lerps combatants[side].root.position toward snap[side].pos (MP_LERP_ALPHA 0.3) + lerps rot.y (_wrapAngle + _lerpAngle for shortest-arc), swaps clip via playClip with /Idle|Blocking/i loop detection + _mpLastAppliedClip[side] dedup (otherwise 50ms re-fires would freeze on frame 0), mirrors HP/SP/stamina/wins/round, edge-triggers MATCH WON overlay via _mpWonMatchShown dedup. Combat early-returns when mp.peerReady && mp.role==='guest' — guest just renders host's state. Guest input forwarding: playerAttack/playerGuardEnter/playerGuardExit each get a leading guest-forward guard that calls sendGuestInput({attack/guard:true|false}) then returns; host receives via t:"in" → applyGuestInput(k) → performAttack('azure','crimson',{local:false}) for attack OR plays Blocking/Idle on azure for guard. performAttack helper extracts the existing swing+impact+idle-restore body so the SAME code path drives crimson (host player, local:true) AND azure (guest player when peerReady, local:false). FP polish (FOV punch, weaponTrail, EXHAUSTED pill) gated on opts.local=true so azure-from-guest-input on host doesn't pollute crimson's FP camera. Host-authoritative invariant: guest never calls applyDamage/gainSP/triggerImpact locally — damage only flows through host's performAttack → next snapshot. /3d/index.html now 6272 lines (was 5782 at r31, +490). VERIFY: HTTP 200; greps buildSnapshot/hostBroadcast 8, applySnapshot 8, lastSnapshot 7, mp.peerReady 16; all 5 <script> blocks parse cleanly via acorn. Single-player byte-identical: every new branch gated on window.mp && mp.peerReady; with no peer connected, behavior is unchanged from r31. NOT live-tested with 2 browsers — agent-browser can't tick rAF for MP verification per project memory. USER PIVOT mid-round: "もっと見た目調整と3Dアニメーションを入れる方向で頑張って" — round 33+ will swing from network → visual/animation polish: idle breathing additive sway (additive sin on chest+head over KayKit Idle clip), cape verlet secondary motion (Knight_Cape mesh inertial drag), weapon trail ribbon upgrade (line→Mesh/Tube), footstep snow puff particles, possibly head-IK lookAt and filmic LUT color grade.
- 2026-06-25 — Iter 66 FP loop tick 12 (round 30) — KayKit Adventurers character roster + opponent switcher. Downloaded 5 GLBs from KayKit-Game-Assets/KayKit-Character-Pack-Adventures-1.0 GitHub mirror (CC0) at addons/kaykit_character_pack_adventures/Characters/gltf/<Name>.glb path (confirmed via GitHub API tree query — initial /glTF/, /Skinned/, root URL patterns 404'd): Knight (3.66MB, copied from /tmp/lkb-knight-cc0/Knight.glb), Barbarian (3.61MB), Mage (3.59MB), Rogue (3.62MB), Rogue_Hooded (3.60MB). All verified with glTF magic bytes 676c5446 ("glTF"). No Healer in the pack — KayKit Adventures 1.0 has only those 5 characters. Saved to 3d/assets/roster/<Name>.glb (still under 3d/.gitignored). ROSTER constant = 6 entries (Azure default holding the existing assets/azure.glb + 5 roster characters under assets/roster/*.glb, relative HTTP paths). window.currentOpponent restored from localStorage["lkb3d_opponent"] default "Azure". switchOpponent(id, opts) does proper teardown — mixer splice from mixers[] + outlinePass.selectedObjects splice + scene.remove + geometry/material/texture disposal — before loading the new GLB via the existing GLTFLoader instance. Applies position (2, 0.81, 0) + scale 0.6908 + rotationY π to match the original azure setup. Tints CLONED materials with 0x7090d0 cool-steel (does NOT mutate the shared GLTF material cache that other Azure_-named meshes might reference). Calls existing setupCombatant() so KayKit's standard CLIP_NAMES (IDLE/ATTACK_CLIPS/Blocking/Hit_A/Hit_B/Block_Hit/Cheer/Death_A/Dodge_Backward) register cleanly across all 5 characters since KayKit standardized rig + clip names. Resets azure HP/SP/stamina on swap. Falls back to 'Azure' if load fails. Boot path intact: assets/azure.glb still loads as default for FP-mode sword attach + combat-loop wire; a deferred setTimeout inside that callback honours localStorage and silently swaps to the saved character if non-Azure. HUD ROSTER button (between #view-toggle and #settings-btn) opens a popup listing all entries [1]-[6] keyboard hints, active row highlighted in cool blue, outside-click closes. Digit1-Digit5 hotkeys wired at window level (NOT gated by FP pointer-lock so 3rd/manual/FP all work), skipped when Ctrl/Meta/Alt held or focus in INPUT/TEXTAREA. 2s "OPPONENT: <LABEL>" pill on switch (cool-blue palette). /3d/index.html now 5252 lines (was 4876 at r29, +376). VERIFY: HTTP 200 on /3d/index.html + Knight.glb roster path; greps pass (ROSTER/currentOpponent/switchOpponent 36, lkb3d_opponent 4); JS module syntax validated via Node Function ctor (152571-char body). chrome-devtools-mcp + cmux browser screenshot skipped (extensions disconnected this session, portrait-only-cmux). Caveat: per-character OPPONENT_TRANSFORM (position/scale/rotation) numbers come from the plan, not measured per-character — if Mage/Rogue/Barbarian lands off-mark visually, those constants are the single nudge point. Loop continues — round 31 candidates: Cloudflare multiplayer relay phase 1 (wss://lkb-relay.noppiki.workers.dev/r/<code> connect + room-code HUD), per-character animation map (KayKit may not have all CLIP_NAMES across every character — need to verify), brand-new arena geometry (snow_clearing / forest_edge / castle_ruin), match-point BGM detune layer integration from 2D game.
- 2026-06-25 — Iter 66 FP loop tick 11 (round 29) — aspect-aware rendering for non-landscape viewports + persisted preserveDrawingBuffer for screenshot tooling. ASPECT-AWARE FOV: in onResize, after camera.aspect=w/h, derive baseFov from view mode (FP_SHOT.fov in FP, settings.fov else, fallback 36); REF_ASPECT = 16/9 (game tuned for 1600×900). When current aspect < REF_ASPECT (portrait/square), preserve horizontal FOV via hFovRad = 2*atan(tan(baseFov*PI/360)*REF_ASPECT) and set camera.fov = hFovRad*180/PI/aspect. At ≥16:9 landscape, FOV stays at baseFov. updateProjectionMatrix called. Verified at 859×1104 (aspect 0.778): FOV widens to 131.67° to keep all scene subjects in the horizontal frame — though combined with HDRI sky brightness + bloom threshold 0.78 the practical result on cmux's portrait WKWebView is heavy bloom wash (subjects technically visible but lost in the glow). Acceptable tradeoff for code correctness; real landscape 1600×900 keeps base 36° + composes correctly. BOKEHPASS AUTO-FOCUS: bokehPass.uniforms.focus.value = clamp(camera.position.length(), 3.0, 15.0) updated in TWO sites — end of onResize (initial load + viewport change) AND in animate() before composer.render (FP↔3rd transitions and per-frame camera motion change distance without firing resize). Per-frame cost: one clamp + one uniform write, negligible. Initial bokehPass constructor focus:7.0 stays for first frame before onResize fires. preserveDrawingBuffer:true on WebGLRenderer constructor (added during cmux browser verify attempt) PERSISTED at line 1689 — required for WKWebView snapshot / canvas.toDataURL to capture WebGL content. /3d/index.html now 4876 lines (was 4849 at r28, +27). VERIFY: cmux browser at portrait 859×1104 confirms aspect-aware FOV math fires (131.67° at portrait 0.778, would be 36° at landscape ≥1.78). HUD intact, combat ticks running (HP drops as azure attacks), but DoF/bloom in portrait makes static snapshot bright-blurry — user runs landscape locally for proper visuals. Loop continues — round 30 candidates: Cloudflare multiplayer relay (wss://lkb-relay), warden/ember/vesper/gale character roster as opponent options, brand-new arena geometry (forest clearing / castle ruin / frozen lake), audio music-tier match-point detune layer integration from 2D game's iter64.
- 2026-06-25 — Iter 66 FP loop tick 10 (round 28, FP loop's third stopping point) — Settings panel + high-score tracker. SETTINGS PANEL: window.settings = { fov, mouseSens, bloomStrength, ssaoEnabled, muted } loaded from localStorage["lkb3d_settings"] with per-key SCHEMA clamp validation (min/max + bool coerce); persisted on every input event. Panel = #settings-panel center fixed 400×440px amber-border dialog + #settings-backdrop dim, opened via P key (skipped inside input/textarea) OR gear ⚙ button #settings-btn (right:354px, left of mute-btn) OR closed via Escape/CLOSE/backdrop click. Four sliders: FOV 50-100 step 2 (updates camera.fov + FP_SHOT.fov + FpPolish.fovPunch.restFov/peakFov + updateProjectionMatrix), Mouse Sens 0.0005-0.005 step 0.0005 (reassigns FP_MOUSE_SENS — changed from const to let), Bloom 0-1.5 step 0.05 (updates bloomPass.strength AND impactSystem._baseBloomStrength + flash.baseStrength/peakStrength so the bloom flash returns to the new baseline), SSAO checkbox (ssaoPass.enabled). Mute checkbox defers to existing setMuted(). openPanel: re-syncs UI, sets window._gamePaused=true, exits Pointer Lock so cursor reaches the panel. combatTick gets early-return on window._gamePaused. FP input (Space/E/F) naturally gates because fpInputActive() requires _mouseLocked which becomes false on exitPointerLock. window.impactSystem exposed so the Bloom apply hook can sync baseline (otherwise post-flash bloom snaps back to boot-time 0.55). HIGH-SCORE TRACKER: window.record = { wins, losses, currentStreak, bestStreak } from localStorage["lkb3d_record"] with validation. In triggerDeath MATCH WON path: crimson winner → wins++ / currentStreak++ / bestStreak = max(bestStreak, currentStreak); azure winner → losses++ / currentStreak = 0; saved + updateRecordHUD() called. HUD: #record-hud fixed top:90px right:14px amber-pill "RECORD: NW·NL · BEST N". Reset Record button in Settings panel clears localStorage. /3d/index.html now 4849 lines (was 4337 at r27, +512 — biggest single round of the FP loop). VERIFY: chrome-devtools-mcp STILL disconnected; grep-only — settings-panel/window.settings 34 (≥3), _gamePaused 7 (≥2), lkb3d_record/currentStreak/bestStreak 20 (≥4). HTTP 200, file not corrupted, all 3 script blocks parse cleanly via Node Function ctor.

FP loop summary (rounds 19-28, 10 ticks, 20 git commits) — built a complete first-person fighter prototype with full settings/record/replay polish on top of the iter-66 cinematic Three.js scene. /3d/index.html grew 2701→4849 (+2148 lines) across the FP loop alone. Full feature stack: CameraDirector.viewMode toggle (cinematic/fp/manual) + KO replay cinematic (winner CLOSE_ shot 0.5x slow-mo 3s on MATCH WON); FP self-mesh hide + 1H_Sword/2H_Sword viewmodel swap with F (per-weapon transform offsets, localStorage persisted); Pointer Lock mouse-look (YXZ, ±π/2 clamp, configurable sensitivity); Space=ATTACK from 4-clip pool (20 stamina) + FOV punch + world-line WeaponTrail + delayed damage + hit-lag 100ms freeze frame; E hold=GUARD (10/s stamina, 80% damage absorption + sfxClang); HP/SP/STAMINA bars + 3-pip best-of-3 score + RECORD pill (W/L/BEST) + VS badge + ROUND N banner + damage floater + amber aim reticle (dims grey when exhausted) + click-to-aim hint + WEAPON pill + EXHAUST pill + KO/MATCH WON overlays. Adaptive azure CPU AI: aggressive (2200ms next tick) >70% HP, defensive (40% Dodge_Backward) <30% HP. Hit reaction variety (Hit_A/Hit_B/Block_Hit) + crimson low-HP rumble. Polyhaven HDRI_POOL rotation per match (5 dusk variants, async PMREM swap with toast). FP-specific FX: red damage vignette, hit-take shake 0.10 amp. Settings panel (P key) with FOV/sens/bloom/SSAO/mute/reset record live-bound to runtime + game pause. 2D-game WebAudio BGM+SFX integrated (gesture-gated, mute persistent). All preserved alongside the iter-1-18 cinematic scene (KayKit knights, Polyhaven dusk + bark log + snow ground + 2 arena torches with flicker+embers + 3500 snow particles, EffectComposer RenderPass→SSAO→OutlinePass→BokehPass→UnrealBloom→SMAA→OutputPass, 4-light rim setup). Loop self-paused — round 29+ candidates require either chrome-devtools-mcp reconnect for visual verify or substantial new direction: Cloudflare relay multiplayer (wss://lkb-relay), WebXR/VR hook, character roster (warden/ember tints), brand-new arena geometry. The current state IS a playable AAA-stylized FP fighter — open http://localhost:8765/3d/index.html, click 👁 VIEW to FP, click canvas, hold E or take a hit, press Space to attack.
- 2026-06-25 — Iter 66 FP loop tick 9 (round 27) — KO replay cinematic + Polyhaven HDRI rotation pool. KO_REPLAY fires ONLY on MATCH WON (winnerWins ≥ WINS_MAX), NOT every round KO. In triggerDeath's MATCH WON branch: savedViewMode = cameraDirector.viewMode; setViewMode('cinematic'); jumpTo(winner === 'crimson' ? CINEMA_SHOTS.CLOSE_CRIMSON : CINEMA_SHOTS.CLOSE_AZURE); all mixers' timeScale = 0.5 for 3.0s slow-mo; named restore via window._koReplayRestoreFn puts per-mixer timeScale back to its prior value (preserves any per-knight slow-mo states) and restores savedViewMode. Cancel-safe via window._koReplayHandle; resetMatch() also defensively flushes any pending replay so mixer slow-mo cannot leak into the next match. 6000ms resetFullMatch + 4000ms cheer setTimeouts unchanged. Existing MATCH WON overlay's "{WINNER} WINS THE MATCH" subtitle already covers the winner display. HDRI_POOL = 5 Polyhaven CC0 HDRI ids: ['pisztyk_polonyna', 'kloofendal_misty_morning_puresky', 'evening_road_01_puresky', 'fouriesburg_mountain_lookout', 'winter_evening']; hdriUrlFor(id) → https://dl.polyhaven.org/file/ph-assets/HDRIs/hdr/4k/<id>_4k.hdr. hdriRotate(idx) async-loads via RGBELoader, regenerates PMREM, disposes prior currentHdriPmrem, assigns to scene.environment + scene.background; logs + keeps-current on error (silent fallback rather than local-file). rotateHdriForNextMatch() = bounded random non-repeat picker. showHdriToast(id) DOM "ARENA: <id>" 2s. Wired into resetFullMatch BEFORE resetMatch via try/catch so a rotate failure can't block the match reset. Exposed window.hdriRotate / rotateHdriForNextMatch / HDRI_POOL for devtools. Default assets/env_dusk.hdr boot path untouched (currentHdriPmrem recorded after boot for safe disposal on rotate). /3d/index.html now 4337 lines (was 4154 at r26, +183). VERIFY: chrome-devtools-mcp STILL disconnected; grep-only — KO_REPLAY group 20 (≥1), timeScale=0.5 1 (≥1), HDRI_POOL group 13 (≥3), polyhaven 1 (≥1), HTTP 200, file not corrupted. Caveat: did not curl-probe each pool id at code-gen time — if a Polyhaven slug 404s at runtime the rotate is silent-skip; user can verify the live URLs themselves with `curl -I https://dl.polyhaven.org/file/ph-assets/HDRIs/hdr/4k/<id>_4k.hdr | head -1`. Loop continues — round 28 candidates: Settings panel (mute/FOV slider/mouse sens slider) + High-score tracker (localStorage best-of-N record), match-point BGM detune integration from 2D game (iter64), Cloudflare multiplayer hook to wss://lkb-relay endpoint.
- 2026-06-25 — Iter 66 FP loop tick 8 (round 26, FP loop's second stopping point) — F-key weapon swap + STAMINA system. FP_WEAPONS = ['1H_Sword', '2H_Sword'] + FP_WEAPON_TRANSFORM map (1H pos (0.3,-0.35,-0.7) rot (-PI/6,-PI/8,-PI/4); 2H pos (0.35,-0.55,-0.95) rot (-PI/5,-PI/9,-PI/4) because the 2H blade is longer and needs more forward+down) + fpWeaponIndex parsed from localStorage["lkb3d_fpweapon"] (bounds-validated). attachFpSword refactored to accept optional weaponName param (defaults FP_WEAPONS[fpWeaponIndex]), exact-name match first then falls back to /sword/i regex (round-20 behavior). swapFpWeapon: detach → advance modulo → persist → re-attach + 1.5s amber "WEAPON: N" pill (top:52px right:14px, fades in 0.18s). FP keydown handler caught case 'KeyF' with repeat-guard + preventDefault. Boot restore is automatic: the unmodified attachFpSword() (no-arg) call sites just default to the restored index. STAMINA system: gameState.{crimson,azure}.stamina = 100 (max 100); STM_ATTACK_COST=20, STM_GUARD_DRAIN=10/s, STM_IDLE_REGEN=25/s. playerAttack BLOCKS when stamina<20 (showExhaustPill 1s, plays cheap step sfx, no state change), else stamina -= 20. playerGuardEnter BLOCKS at stamina≤0 with same pill. Per-frame in animate(delta): guard state drains -10/s (auto-release crimson + pill at 0), else +25/s up to STM_MAX. reticle.exhausted class toggles (grey rgba(150,150,150,0.6)) when crimson stamina<20 signaling "can't attack". HUD: green-gradient .stm-bar (300×8, #4abb3a→#88e872) under SP bar for BOTH player cards + .stm-num "STM N/100" label. resetMatch also refills stamina between rounds. /3d/index.html now 4154 lines (was 3853 at r25, +301 — biggest single round of the FP loop). VERIFY: chrome-devtools-mcp REMAINED DISCONNECTED — grep-only verification: FP_WEAPONS group 11 (≥4), 1H_Sword/2H_Sword/KeyF 3 (≥2), stamina 26 (≥6), exhaust/EXHAUSTED 32 (≥2). Module re-validated with `new Function` after stripping imports: PARSE_OK 2654 lines. HTTP 200 from /3d/index.html. FP loop summary (rounds 19-26, 8 ticks, 16 git commits) — built a playable AAA-leaning first-person fighter on top of the iter-66 cinematic Three.js scene: CameraDirector.viewMode toggle (cinematic/fp/manual), self-mesh hide + KayKit 1H/2H sword viewmodel (F to swap, transform tuned per weapon), Pointer Lock mouse-look (YXZ, ±π/2 clamp, 0.0022 sensitivity), Space=ATTACK from a 4-clip random pool + 20 stamina cost + FOV punch + weapon-trail world-line + delayed damage + hit-lag freeze, E hold=GUARD with 10/s stamina drain + 80% damage absorption + sfxClang, F=weapon swap, ESC=release lock. Adaptive azure CPU: aggressive (2200ms next tick) >70% HP, defensive (40% Dodge_Backward) <30% HP. FP HUD overlay: HP/SP/STAMINA bars per knight + 3-pip best-of-3 score + VS badge + ROUND N banner + damage floater + aim reticle (amber cross + center dot, dims grey when exhausted) + click-to-aim hint + WEAPON pill + EXHAUST pill + KO overlay + MATCH WON overlay. FP-specific FX: red radial damage vignette on hit, 100ms mixer freeze + WeaponTrail.frozen flag, hit-take shake 0.10 amp, cheer animation on KO winner, sfxStep low triangle thud on attack windup. All preserved alongside the existing cinematic scene (3D KayKit knights, Polyhaven dusk HDRI sky + bark log + snow ground, 3500 snow flurry particles, two arena torches with double-sine flicker + embers, four rim spotlights, EffectComposer chain RenderPass→SSAO→OutlinePass→BokehPass→UnrealBloom→SMAA→OutputPass, 2D-game WebAudio BGM+SFX). /3d/index.html grew 2701→4154 (+1453 lines) across the FP loop alone, total iter 66 0→4154 across 26 rounds. Loop self-paused — round 27+ candidates: WebXR/VR hook for true VR play, multiplayer via 2D game's existing Cloudflare relay (Worker+DO at wss://lkb-relay.noppiki.workers.dev), random HDRI rotation per match (winter_evening / phalzer_forest / sunset variants), KO cinematic replay (cinematic cycle 3s slow-mo loop on match win), VFX upgrade (sword-strike spark sheets), or a brand-new arena map. Visual verification needs chrome-devtools-mcp reconnect or user runs locally; current screenshot pipeline broken until then.

- Round 50 (iter 66 FP): FP lock-on bracket — four corner L-shapes framing azure in screen space. `updateLockBracket()` projects azure root (feet) and head (+1.8 Y) each frame via `_lbProj.project(camera)`; computes pixel AABB with 55% aspect horizontal padding; sets container `left/top/width/height` in one element update per frame (no per-corner writes). Behind-camera guard: bail if `ndc.z > 1`. Off-screen guard: hide if center is off viewport. Amber at rest (`.visible`); pulses red (`.danger` + CSS keyframe animation) when `azure.state === 'attack'` — telegraph for incoming danger. GC-safe: single pre-allocated `THREE.Vector3` in `FpPolish._lbProj`. Wired in `animate()` after `updateFovPunch()` inside `try/catch`. /3d/index.html: 8350 → 8512 lines (+162).
- Round 49 (iter 66 FP): FP hit feedback — damage direction arrow on player hit. Vignette (A) was already fully wired (skipped). Added `#fp-dmg-arrow` (▲, z-index 25, CSS rotate) + `flashDmgArrow()` using `cameraDirector.fpYaw` to compute screen-space angle: `atan2(-cos(fpYaw), -sin(fpYaw))` so arrow points toward azure (world +X) regardless of mouselook orientation. Fires inside `applyDamage` adjacent to `flashDamageVignette()` — same FP+!guardAbsorbed gate. 600ms opacity fade-out; rapid-hit re-arm via clearTimeout pattern (mirrors vignette).
- Round 48 (iter 66 FP): Weapon trail — additive ribbon on player sword tip during attack swing (24-sample ring buffer, 200ms fade). World-space, visible after swing too. Visceral swing feedback. (Trail fully operational since round 23/33; this entry records the round-48 spec as delivered.)
- Round 47 (iter 66 FP): AI rebalance — azure damage 8→5, attack interval 3000→4500ms, match-start 2s grace, plus camera microshake on player-landed hit. Player can now trade evenly.

FP loop summary (rounds 19-22) — shipped a playable first-person fighter prototype on top of the iter 66 stylized AAA cinematic scene: CameraDirector.viewMode 'cinematic'|'fp'|'manual' with FP_SHOT at (-2,1.4,0) looking (5,1.2,0) fov 75; setCrimsonSelfVisible hides self body + helm meshes (combat ticks unaffected); attachFpSword clones the 1H_Sword to camera child at hand-hold offset (renderOrder 9999); 150ms retry chain + try/catch TDZ guard around combatants for boot-restore-with-fp-localStorage; Pointer Lock mouselook (YXZ rotation order, FP_MOUSE_SENS 0.0022, pitch clamp ±π/2); keyboard input (Space=random ATTACK clip + delayed damage + FOV punch, E=Block + 80% damage absorption + sfxClang); combatTick FP branch forces azure CPU; aim reticle + damage vignette + hit-take shake. 4 git commits across the FP loop (8 with logs). /3d/index.html grew 2701→3473 (+772 lines). Loop self-paused here — round 23+ candidates: weapon trail at attack frame, animation crossfade smoothing, opponent readability boost (closer FP_SHOT or wider FOV), 3rd opponent character variant, ramparts/torches/snow mound props for arena, in-FP HP regen and stamina/cooldown UI, multiplayer hook to the 2D game's existing Cloudflare relay.

Loop summary — iter 66 (rounds 1-18) shipped a parallel 3D port of the 2D Log Knight Battle as a stylized AAA cinematic vertical slice: Three.js r0.171 single-file + Polyhaven CC0 4K HDRI (pisztyk snowy dusk) + Polyhaven CC0 2K PBR (bark/snow/metal/fabric) + KayKit CC0 Adventurers Knight.glb ×2 (crimson + Hue-shifted azure, baked-in astride rest pose) + combat state machine cycling KayKit's 76 baked clips (4-attack random pool + Block + Hit_A + Cheer + Death_A) + HP/SP gauge HUD with engraved-metal player labels & 3-pip best-of-3 score & ROUND N kerning-in banner & damage floater & DOM mute toggle + EffectComposer chain (RenderPass→SSAO→OutlinePass→BokehPass→UnrealBloom→SMAA→OutputPass) for stylized cinematic post + ImpactSystem with safe anchor-snapshot camera shake (no drift, controls.enabled disabled-and-restored) & bloom flash & 60-100 amber particle burst & screen-shake & impact pause + 4-light rim (warm/cool flanking) + AmbientLight + DirectionalLight w/ shadow.mapSize 4096² + HemisphereLight + campfire flicker PointLights with ember particles + 3500 snow/dust particles with size variation + horizontal drift + CSS radial vignette + 2D-game-derived procedural WebAudio (single AudioContext + globalGain mute gate + sfxGain + arenaSendGain feedback-delay reverb + procedural Nordic-mood BGM drone+bell+accent + sfxHit/Swing/Horn wired to combat events, gesture-gated init) + CameraDirector cinematic cycle with combat sync. Total file growth 0→2701 lines across 18 rounds. 18 git commits (9 code + 9 log). 3d/assets/ git-ignored — regenerable via stashed workflow scripts at .claude/projects/.../workflows/scripts/iter66-aaa-round*.js. Loop self-paused here pending user direction.

- 2026-06-25 — Shipped 3D FP ROUND 55: CHARGE SWING METER. Space tap (≤299ms) = tier 0 (damage 8, normal). Space hold 300-799ms = tier 1 amber ring: damage 12, stamina cost +3, FOV punch +3°, heavier shake. Space hold 800ms+ = tier 2 cyan ring: damage 16, stamina cost +6, FOV punch +6°, screen-edge cyan flash on impact, extra sawtooth heavy-thud SFX, biggest shake. Implementation: (1) CSS `#charge-ring` — SVG arc centered on reticle (r=22, circumference≈138.23), stroke-dashoffset driven by JS 0→fill; `.tier1` amber, `.tier2` cyan-white with drop-shadow glow; `.charging` disables fade transition for instant show. `#charge-impact-flash` — radial cyan-white full-screen edge gradient, z-index 30, opacity 0→1→0 180ms. (2) JS input pipeline: keydown Space arms `playerInput._isCharging = true` + `_chargeStart = now` (no immediate playerAttack call); keyup Space resolves hold duration → `_chargeGetTier(holdMs)` → `playerAttack({chargeTier})`. No-arg callers (QA bridge, MP guest) default chargeTier=0 — backward-compatible. (3) `playerAttack(opts)` extended: reads `chargeTier`; computes `tierStmExtra` (0/+3/+6) → total stamina cost 20/23/26; captures `myChargeTier` into closure so timeout reads a stable value (race-safe); tier-scaled damage 8/12/16; tier-scaled FOV peak +0/+3/+6°; tier-scaled shake 0.12/0.15/0.20; tier2 fires extra sawtooth oscillator (180→60 Hz, 0.15 gain, 200ms). (4) Per-frame charge tick in animate(): drains `CHARGE_STM_DRAIN=12/s` while charging; auto-releases (cancel + exhaust pill) if stamina < STM_ATTACK_COST; otherwise calls `_chargeRingUpdate(holdMs)` for arc fill. (5) Cancel guards: `_chargeCancelSilent()` clears isCharging + ring + rumble on guard-enter, resetMatch, and pointer-unlock (via existing paths). Rumble: Web Audio oscillator 60 Hz sine, gain 0→0.05 over 1s while held, fade-out 50ms on release/cancel. Note: spec called for SP drain — corrected to STAMINA (the actual swing economy in this codebase; SP is special-meter only gained, never spent in swing path). r55 preserved all r46–r54 constraints: _swingId flow, parry, combo counter, telegraph, grace interval. /3d/index.html grew 8984→9234 lines (+250). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 54: COMBO COUNTER HUD. Victory polish (slow-mo KO_REPLAY cinematic, letterbox bars, matchWonOverlay, KO overlay, sfxHorn) and win-streak record were already fully implemented (r46–r53); round 54 pivots to combo counter — the one AAA-feel HUD element confirmed missing. Implementation: (1) CSS `#combo-hud` — fixed bottom-center (bottom:18%), flex column, z-index 29, `opacity:0 + scale(0.8)` → `.visible` fades to `opacity:1 + scale(1.0)` in 0.15s; `@keyframes comboPulse` on `.combo-count` scale 1.30→0.95→1.00 in 0.22s re-triggers each new hit via remove→reflow→add `.pulse`; crimson-accent palette (#e85c3a hit count, #d8a45a "HIT COMBO" label, multi-layer ember text-shadow). (2) JS state: `_comboCount`, `_comboResetTimer`, `COMBO_RESET_MS = 1500`; `_comboHit()` increments and shows at ≥2 hits; `_comboReset()` zeroes and hides. Exposed as `window._comboReset` for reset-hook. (3) Hook in `applyDamage`: fires `_comboHit()` when `defenderId==='azure' && !guardAbsorbed`; fires `_comboReset()` on guard-absorbed hit (breaks streak intentionally — partial block shouldn't sustain a combo). (4) Reset in `resetMatch()` — cleared between rounds. Parry-counter hit (`applyDamage(attackerLabel, 3)`) correctly excluded: attackerLabel is azure when player parries, so the `defenderId==='azure'` guard passes and increments combo — this is correct; the player landed a reversal hit. All existing constraints preserved: no combat-tick touch, no regressions, single HTML, idempotent. /3d/index.html grew 8879→8984 lines (+105).

- 2026-06-25 — Shipped 3D FP ROUND 53: HUD FP OVERLAY + PARRY/HIT GAUGE FLASH. Problem: game-hud was pinned `top:24px` in all view modes — in FP first-person perspective, gauges were stranded at the top of the screen where they compete with sky/HDRI, far from the player's combat focus zone. Fix: (1) `refreshFpOverlays()` now toggles `document.body.classList('fp-mode', isFp)` as the single source of truth — every viewMode transition (button, boot-restore, programmatic) updates the body class automatically. (2) CSS `body.fp-mode #game-hud` overrides `top:auto; bottom:32px; align-items:flex-end` to drop the two player-cards to bottom corners; bar widths grow to 360px, HP bar to 18px height for improved legibility. 3rd-person / manual revert cleanly when fp-mode is removed. (3) Parry SP flash: `triggerPerfectParry()` now grabs `sp-fill-crimson.closest('.sp-bar')` and applies `.parry-flash` class (removes → reflow → adds, stripped after 400ms) — `@keyframes spParryPulse` animates a cyan `0 0 24px 6px rgba(120,220,255,0.85)` outset box-shadow on the bar element (not the fill, which would be clipped by `overflow:hidden`). (4) HP hit flash: inside `applyDamage('crimson',…)` FP branch (after `flashDamageVignette` + `flashDmgArrow`), grabs `hp-fill-crimson.closest('.hp-bar')` and applies `.hit-flash` (250ms), `@keyframes hpHitPulse` fires red `0 0 18px 4px rgba(255,60,60,0.9)`. Both guard-absorbed hits are excluded (outer `!guardAbsorbed` check already present). Net: FP mode now has a purpose-built bottom-corner HUD and visceral gauge feedback for both parry success and player damage. /3d/index.html grew 8812→8878 lines (+66). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 52: PARRY TIMING WINDOW + COUNTER FLASH. Implemented perfect parry: E pressed in telegraph's final 200ms (elapsed 150–350ms) triggers `triggerPerfectParry('azure')` instead of normal guard. Parry flow: (1) bumps `azure._swingId` → pending 350ms callback self-cancels at the `attacker._swingId !== mySwing` guard; (2) clears telegraph overlays immediately; (3) resets azure to `idle` + plays idle clip so combatTick's windup-defer check never stalls; (4) `applyDamage('azure', 3)` counter-hit; (5) cyan-white full-screen flash `#fp-parry-flash` (rgba(180,230,255,0.55), 150ms, z-index 28); (6) `triggerFovPunch()` reused for camera dip; (7) `#fp-parry-text` "⚡ PARRY!" 600ms then fade (32px weight-900 cyan, z-index 29); (8) `sfxCounter()` (existing bright metallic cue). Telegraph records `attacker._telegraphAt = performance.now()` on windup arm, cleared to 0 when attack launches or parry fires — stale timestamp can't leak. `playerGuardEnter` checks `azure.state === 'windup' && azure._telegraphAt && elapsed ∈ [150,350]`; early press (elapsed < 150) falls through to normal guard (20% chip). QA hook `window.__triggerParryTest()` simulates 200ms-in parry for headless verification. /3d/index.html grew 8660→8812 lines (+152). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 66: SESSION STATS + PROGRESSION HUD. Extended `window.record` (localStorage `lkb3d_record`) with two new lifetime fields: `bestCombo` (best combo chain ever landed) and `parries` (total successful perfect-parries). A `_defaultRecord()` helper now unifies the schema across load fallback, reset-button handler, and `_lkbStatsReset()` console utility, preventing undefined→NaN corruption. Old-shape records (pre-r66) are backfilled in-place so existing wins/losses survive. `triggerPerfectParry()` increments `record.parries` in the real-parry path only (after the feint early-return — baited feints grant nothing). `_comboHit()` updates `record.bestCombo` when the current chain exceeds the stored best, saving immediately. Session counters (`_sessionWins`, `_sessionLosses`) are in-memory only (reset on page reload) and are bumped in the match-end block at `winnerWins >= WINS_MAX` — the correct match-level hook, not the per-round `triggerDeath`. Post-match stats: `#post-match-stats` div added inside `#match-won-overlay`; `refreshPostMatchStats()` fills it with "Session: NW NL · Lifetime: NW NL · Best Combo ×N · N Parries" (18px, opacity 0.75, warm-grey #c8b98a). FP streak HUD: `#fp-streak-hud` (11px, bottom-left at `bottom:14px left:18px`, below mp-bar at `bottom:70px`) is `display:none` by default, revealed by `body.fp-mode` gate — shows "STREAK: N  BEST ×N" during play; updates live on combo extension, parry save, and match-end. `refreshStreakHUD()` also called from reset-button and boot paint. /3d/index.html grew 10319→10436 lines (+117). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 58: DAMAGE FLOATER KIND DIFF + CRITICAL SHAKE. `showDamageFloater(id, amount, opts)` gains a third opts arg `{ kind, caption }` (backward-compatible; 2-arg callers unaffected). `applyDamage(id, amount, opts)` similarly extended — auto-forces kind='taken' when `defenderId==='crimson'`; forwards kind/caption to showDamageFloater; caller-supplied kind passes through for azure hit paths. Five modifier classes added: `.kind-normal` (#fff 32px), `.kind-charge1` (amber #ffb060 36px bold), `.kind-charge2` (cyan-white #c6f0ff 42px weight-900 + outer glow), `.kind-parry` (cyan italic #66ddff 28px), `.kind-taken` (red #ff5555 32px). Parry riposte also gets a `.caption` "RIPOSTE" sub-line (0.55em italic-cancelled, letter-spacing 0.08em). Call-site wiring: `playerAttack` L7910 passes `{ kind: hitKind }` computed from `myChargeTier` (normal/charge1/charge2); `triggerPerfectParry` L4716 passes `{ kind:'parry', caption:'RIPOSTE' }`; azure AI hits and fallback callers omit opts (defaults 'normal', auto-flips 'taken' on crimson). Critical shake: existing tier-2 shake block (r55) bumped from amplitude 0.20→0.26, duration 0.18→0.22s — no new shake writer, no duplicate block. /3d/index.html grew 9296→9352 lines (+56). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 62: AI ATTACK PATTERN VARIATION (FEINT + DOUBLE STRIKE). Azure AI was single-pattern (telegraph→strike→idle repeat); this round adds two new patterns gated behind `_aiPattern` flag set per-attack in combatTick. FEINT (20%): telegraph fires with identical vignette+⚠ GUARD alert+sfxHorn as a real attack (player cannot tell it apart). After 350ms, instead of calling runCombatSwing the telegraph callback checks `attacker._aiPattern === 'feint'`, resets azure to idle, plays a brief 1800Hz sawtooth whiff cue (80ms), and fires `setTimeout(combatTick, 3000)` to maintain pressure — azure presses the advantage after baiting the guard. If the player presses E during the feint telegraph: `triggerPerfectParry` intercepts first with a feint guard at its top — bumps swingId, clears overlays, resets azure to idle, consumes the flag, and returns WITHOUT applying the 3HP RIPOSTE counter (the feint worked, player got nothing for the parry attempt). `playerGuardEnter`'s early-guard path (elapsed < 150ms) falls through to normal guard and is covered by the feint callback consuming the attack on 350ms expiry. DOUBLE STRIKE (10%): 1st hit fires normally (5 damage). At 1st impact, if `_aiPattern === 'double'`, swingId is bumped immediately (cancels the 105%-dur idle-restore for the 1st swing; the captured `mySwing` check in the idle setTimeout self-cancels cleanly), azure.state held at 'attack' (blocks combatTick's windup/attack defer from racing a 3rd attack during the 300ms gap). After 300ms gap: a short 150ms mini-telegraph (vignette+⚠) arms a new parry window (`_telegraphAt = performance.now()`), making the 2nd strike parryable — press E in that 150ms and `triggerPerfectParry` fires normally with RIPOSTE. 2nd strike lands at 3 damage (weaker follow-up, "畳み掛け" feel), with a detuned 300Hz sawtooth cue distinct from the 1st hit's sfxSwing. 2nd-swing idle-restore uses the new `doubleSwing` id so all guards remain correct. NORMAL (70%): unchanged. Race analysis: feint consumes `_aiPattern` at resolution site (not creation site) — no double-consume risk across parry path vs. timeout path because triggerPerfectParry early-returns before the timeout callback can inspect it (swingId bump makes the timeout's `mySwing !== doubleSwing` check fail). Audio uses `window.actx` (the shared AudioContext) rather than creating a new one. All r46–r61 constraints preserved: _swingId flow, grace interval, 4500ms setInterval, telegraph overlays, parry timing, hit-stop, combo counter, charge swing. /3d/index.html grew 9778→9939 lines (+161). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 71: ENVIRONMENT TIME-OF-DAY COLOR CYCLE. Time-of-day cycle was absent — scene lighting had been static amber since round 2. Added `class TimeCycle` (defined before sky-dome section so `new TimeCycle()` fires synchronously after `sun` and `hemi` are created). 75-second sinusoidal oscillation between two palette poles: warm (sun #ffc88a amber / hemi sky #8aa0d8 / hemi ground #443a3a — exact original values) and cool (sun #d0aacc muted lilac-dusk / hemi sky #7090b8 slate-blue / hemi ground #2a2a44 purple-earth). Phase computed as `(sin(t×2π−π/2)+1)×0.5` — cosine-shaped ease, no linear pop at period boundary. Weather system cross-check confirmed disjoint: `_weatherApply` writes only `hemi.intensity` and `ambientFill.intensity`, never `.color`; TimeCycle owns color, weather owns intensity — no frame-order conflict. Sky shader uniforms are empty GLSL constants; modification skipped per task spec (PMREM/envMap also untouched). `timeCycle.update()` inserted in `animate()` immediately before `composer.render()` inside a try/catch guard. `restore()` method saved on the instance for future hard-reset scenarios. Net performance cost: 3 `lerpColors` calls per frame. /3d/index.html +60 lines. 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 57: ENVIRONMENT FX DENSITY BOOST IN FP VIEW. Selection rationale: damage floaters were confirmed fully implemented (showDamageFloater at L6436, CSS .dmg-floater, hooked in applyDamage at L6839); enemy stagger skipped (Hit_A/Hit_B/Block_Hit + pickHitClip already at L5423); footstep camera bob not applicable (no WASD movement in FP — camera is pinned to FP_SHOT.getPosition()); BGM combat layer skipped (risk of reopening the r63/64 detune-leak just stabilised); motion blur tier2 skipped (r55 screen-edge cyan flash already covers tier2 impact). FX density boost chosen: most isolated, zero regression risk to combat systems. Problem: in FP view the camera sits ~1–2 m from the combat volume rather than the ~7 m cinematic distance, so the same 500-particle dust/snow field looks visually sparse — particles that are visible in cinematic mostly fall outside the FP FOV or at sizeAttenuation-reduced apparent size. Fix: (1) two constants `_FP_DENSITY_BOOST = 0.8` and `_FP_SNOW_PUFF_SCALE = 1.6` added just before `_weatherApply`. (2) In `_weatherApply`, the per-frame density computation reads `cameraDirector.viewMode === 'fp'` and adds `_FP_DENSITY_BOOST` to `cur.snowDensityMul` before computing `frac` (the immutable `base` snapshot is untouched; `cur` from weather state machine is never mutated). Effect: calm → 0.7+0.8=1.5 (all 500 particles visible), windy → 1.4+0.8=2.2 (clamped to 1.5 = still all 500), blizzard → 2.2+0.8=3.0 (clamped to 1.5). In all cases FP mode maximises the visible particle count; cinematic view retains weather-proportional density. (3) In `SnowPuffSystem.spawn`, COUNT is scaled by `_fpPuffScale` (= `_FP_SNOW_PUFF_SCALE` when `window.cameraDirector.viewMode === 'fp'`, else 1.0) → 29–48 particles per footstep puff vs the base 18–30, making each attack-windup footstep feel heavier in close-quarter FP. Guard: `typeof _FP_SNOW_PUFF_SCALE === 'number'` check protects against TDZ if spawn is called before the constant is parsed. No dust system, weather state machine, FP input, camera, combat, or HUD code touched. /3d/index.html grew 9272→9296 lines (+24). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 56: HIT-STOP / HITLAG with charge-tier scaling + re-entry freeze fix. Hit-stop was already present (L6874, r48-era) but had two problems: (1) fixed 100ms regardless of charge tier, (2) rapid double-hit (azure counter + player hit within the 100ms window) captured `timeScale=0` into `saved[]` and restored to 0 permanently — animation frozen for the rest of the round. Fix: (A) `triggerImpact(hitPos, chargeTier)` gains a second parameter (tier 0=80ms, tier1=120ms, tier2=180ms; legacy callers omit it → default tier0). (B) Re-entry race fix mirrors the KO_REPLAY handle/restoreFn pattern: `window._hitLagHandle` + `window._hitLagRestoreFn` pair; on each new hit-stop call, `clearTimeout(_hitLagHandle)` + immediately fire any pending `_hitLagRestoreFn` BEFORE capturing `saved[]` — ensures we never snapshot zeroed timeScales. (C) `playerAttack`'s impact callback now calls `window.triggerImpact(hitPos, myChargeTier)` so charge-tier duration flows through. (D) `resetMatch` defensively cancels any in-flight hit-stop (same pattern as KO_REPLAY flush) so a round-reset mid-freeze doesn't carry `timeScale=0` into the next round. combatTick is NOT gated (preserves MP cadence, r46–r55 swing/grace/parry timing). All existing paths (azure-vs-player L7233, L7482) keep `triggerImpact(hitPos)` with no tier arg → 80ms default. Effective feel: tier0 normal hit = subtle 80ms stutter; tier1 charge = 120ms meaty freeze; tier2 heavy = 180ms definitive stop — matches SMASH/Sekiro hitlag progression. /3d/index.html grew 9234→9270 lines (+36). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 51: ENEMY ATTACK TELEGRAPH. Problem: Azure CPU attacks fired instantaneously — FP player had zero reaction window to press E-guard, making the guard mechanic cosmetically useless. Fix is two interlocked changes. (1) TELEGRAPH SYSTEM: `triggerEnemyTelegraph(attackerLabel, defenderLabel, runAttackFn)` — fires only in FP mode when azure is the attacker. Sets `attacker.state = 'windup'` synchronously (blocks re-entrant combatTick from stacking a second attack during the 350ms window), bumps `_swingId` for the standard cancel-guard, shows amber radial vignette `#fp-tg-warn` (CSS: `rgba(255,160,40,0.38)` gradient, z-index 23 — below the red damage vignette at 24 so a simultaneous hit is visually distinct) and `#fp-tg-alert` "⚠ GUARD" pill (CSS: amber gold, 22px weight-700, top:calc(50%−120px), transition scale+opacity) for 350ms, plays `sfxHorn()` as the low warning cue (reusing the existing sawtooth horn rather than hand-rolling a new oscillator), then after 350ms clears the overlays and calls `runAttackFn(mySwing)`. (2) FP GUARD DEFERRAL: In the `runCombatSwing` closure (shared by telegraph-delayed and non-FP paths), `skipAutoGuard = fpModeNow && attackerLabel==='azure'` suppresses the automatic `defender.state='guard'` + BLOCK clip that previously fired regardless of player input in FP mode. Now in FP mode the player must actually press E during the telegraph window; crimson's guard state at impact time genuinely reflects player intent. Non-FP (AI-vs-AI) keeps auto-guard. (3) RE-ENTRANCY FIX: combatTick now defers (setTimeout 800ms) if `azure.state === 'windup' || 'attack'` — prevents the aggressive 2200ms one-shot from stacking a second telegraph while the first is pending. Impact and idle-restore timeouts inside `runCombatSwing` guard against `_swingId !== mySwing` and `state !== 'attack'` before resolving, matching the `performAttack` pattern from round 46. (4) DOM: two new elements `#fp-tg-warn` and `#fp-tg-alert` inserted before `#fp-damage-vignette`. FpPolish gains `tgWarnEl`, `tgAlertEl`, `tgFadeTimer`. TELEGRAPH_MS = 350 constant. Net effect: player sees amber vignette flash + ⚠ GUARD alert + low horn cue ~350ms before azure's sword connects; pressing E in that window (or holding it) sets `combatants.crimson.state='guard'` → `applyDamage` reduces hit to 20% chip; not pressing E = full 5 HP loss. This completes the read-react skill loop the FP mode was designed for. /3d/index.html grew 8512→8660 lines (+148). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 70: AZURE IDLE MICRO-MOTION (guard state) + REMATCH HOTKEY DISPLAY. (1) IDLE BREATHING EXTENDED: `updateIdleBreathing()` already existed (r55-era, bob + yaw on state==='idle'). Extended to also run on state==='guard' — azure now breathes visibly while blocking, adding life to defensive standoffs. Added a restore path: when a combatant leaves idle/guard (enters attack/hit/dead), `_idleBreathApplied` flag triggers an explicit reset of position.y and rotation.y to their captured base values so the sin offsets don't freeze in place mid-combat. Scale-Y pulse from the task spec was intentionally skipped — the pivot-compensated scale system (root.scale shifts baked child positions off the log top) makes root.scale.y unsafe at runtime; the existing bob already conveys the same rhythmic life signal. (2) REMATCH HOTKEY DISPLAY: added `#match-hotkey-hint` (fixed bottom-centre, z-index 27, engraved-metal kbd pills, pointer-events:none). Shown 3.2s after MATCH WON (`_matchEndShown = true` fires immediately; hint visibility deferred so it appears after KO_REPLAY cinematic completes). A global `keydown` handler gated on `_matchEndShown` handles: R → `clearTimeout(resetTimerHandle)` (cancels the 6s auto-reset) + `resetFullMatch()` + hides hint; Esc → navigates to `../index.html` (the 2D lobby back-link target) — gated to skip if the settings panel is open (avoids double-close with existing Esc panel handler). `resetFullMatch()` clears `_matchEndShown` + hides hint on entry so R/Esc cannot fire mid-next-match. Multiplayer guest path (applySnapshot) left untouched — host-authoritative; wiring client-side R→resetFullMatch there would desync from the host. /3d/index.html grew 10707→10775 lines (+68). 2 git commits.

- 2026-06-25 — Shipped 3D FP ROUND 69: DIFFICULTY LEVELS (Easy/Normal/Hard). Problem: every match was a fixed challenge — AI had hardcoded attack interval 4500ms, damage 5, and parry window 150–350ms with no way to tune for newcomers or veterans. Fix adds three interlocked layers. (1) DIFFICULTY CONST: `DIFFICULTIES` object (easy/normal/hard) encoding `{ damage, interval, parryEarly, parryLate, label }`. Easy=3dmg/5500ms interval/0–350ms parry; Normal=5dmg/4500ms/150–350ms (unchanged existing defaults); Hard=7dmg/3500ms/200–350ms. `window._lkbDiffCfg` is the live pointer; `window._lkbDifficulty` holds the tier string. Persisted to `localStorage["lkb3d_difficulty"]`; survives matches, rounds, and resets. `bootDifficulty()` IIFE runs at parse time before combatTick is armed. (2) SETTINGS-PANEL SELECTOR: new `sp-row` with three `<button class="sp-diff-btn" data-diff="...">` buttons (EASY/NORMAL/HARD). Active tier gets `.selected` class (gold border + glow). Click calls `setDifficulty(tier)` which updates `_lkbDiffCfg`, saves to localStorage, refreshes button highlights, restarts `combatLoopHandle` with new interval, and updates the FP pill. Panel's `syncUiFromSettings()` re-highlights the correct button on every open. (3) AI PARAMETER WIRING: `maybeStartCombatLoop()` `setInterval(combatTick, …)` now uses `_lkbDiffCfg.interval`; `applyDamage(defenderLabel, 5)` in the 1st-strike impact path now uses `_lkbDiffCfg.damage`. Double-strike 2nd hit (fixed 3), player-attack damage (8 / tierDamage), and riposte (3) are all left untouched per spec. Parry window in `playerGuardEnter` changed from `elapsed >= 150 && elapsed <= 350` to `elapsed >= _dc.parryEarly && elapsed <= _dc.parryLate`. (4) FP DIFFICULTY PILL: `<div id="fp-diff-pill">` positioned below the control hints (bottom: 200px right: 24px), visible only in fp-mode, showing the current tier label in amber at 11px. Updated on boot and every `setDifficulty()` call. Adaptive AI's aggressive `setTimeout(combatTick, 2200)` at high azure HP is intentionally left untouched — it fires a bonus tick regardless of difficulty, providing pressure that scales naturally with the faster Hard baseline. /3d/index.html grew 10595→10707 lines (+112). 2 git commits.
